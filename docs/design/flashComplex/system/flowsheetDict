/*--------------------------------*- Choupo -*--------------------------------*\
|  flashComplex/system/flowsheetDict -- topology only (state lives in 0/)     |
\*-----------------------------------------------------------------------------*/

units
(
    {
        name        flash01;
        type        isothermalFlash;            // Q6 SETTLED (Vitor,
        // 2026-07-27): NO new unit type.  A flash is a flash -- hold T
        // and P, split into whatever phases the DECLARED thermo world
        // admits.  The phase repertoire grows in the thermoPhysPropDict
        // formulation, never in the unit -- so no gatekeeping
        // classifications deciding which components "may" enter which
        // flash, and only one flash to teach.
        in          feed;
        outputs     ( vapor  aqueousLiquid  organicLiquid
                      calciteCake  bicarbonateCake );

        operation   { T 308.15 K;  P 2.0 atm; }
    }
);

# Architecture decision records — the index

> **AUTHORITY: LEVEL 1** for the INDEX (which decision is recorded where, and
> what its status is).  The records it points at keep their own levels.
> Authority map: [`README.md`](README.md).  Ratified 2026-08-05.
>
> This closes the one viewpoint
> [`architecture-description.md`](architecture-description.md) §3 listed as
> having **no view** (V8), and with it requirements R11, R14, R15 and R16 of
> the [42010 conformance assessment](conformance-42010.md), plus
> correspondence C5.

---

## 1. Why an index, when 124 records already exist

The decisions were recorded.  What did not exist was any way to ask **"has
this been decided, and where?"** — and that question is the whole reason the
constitutional layer exists.  Without it a decision is discoverable only by
someone who already knows it happened, which is precisely the reader who does
not need it.

Three concrete costs, all observed:

- The same question was reopened from outside twice this week, and the
  rebuttal had to be reconstructed from memory rather than cited.
- `project-philosophy.md` §5 lists eleven CLOSED decisions and, until this
  file, could not point at the argument for any of them (correspondence C5,
  recorded UNVERIFIABLE).
- Of 124 design records, **78 state a rejected alternative and 46 do not** —
  so for two in five, the reasoning that would prevent re-litigation is
  absent, and nothing said so.

## 2. How to read the table

**Kind** — what the record *is*, which is not the same as what it is called:

| kind | meaning |
|---|---|
| **ADR** | a decision, its alternatives, and why they were rejected |
| **SCOPE** | a programme's boundary: what is in, out, and named-as-deferred |
| **FORUM** | a structured deliberation; the decision is its conclusion |
| **SPIKE** | a vertical proof; the finding is the record |
| **STUDY** | evidence gathered to inform a decision, deciding nothing |

**Alt** — does the record state a rejected alternative? `yes` / **`no`**.
A `no` is not a defect in every kind (a STUDY decides nothing), but a `no` on
an ADR or a FORUM means the decision is recorded without its reasoning, and
that is the shape that gets re-litigated.

**Status** — `SHIPPED` (in the engine, with a gate), `CONTRACT` (written, not
built), `OPEN` (awaiting a decision), `HISTORICAL` (superseded or a snapshot).

**OWED ≠ AWAITING** (standing distinction, ruled 2026-08-08).  An item on the
curation ledger — a citation, a primary-source datum, an INTERIM promotion —
is OWED: reserved to Vítor, standing, blocking nothing, with no deadline and
no queue entry.  Only an item that blocks work pending an architectural
ruling is AWAITING.  Listing an owed item as awaiting recreates the
over-capacity queue this index was just cleared of; conflating them in either
direction is an error.

---

## 3. The records

### Settled and shipped

| record | kind | alt | status |
|---|---|---|---|
| [`which-line-is-a-gates-claim.md`](../design/which-line-is-a-gates-claim.md) | ADR | yes | **SHIPPED 2026-09-06** — tasks #108/#100.  *Which* line of a gate's output is its CLAIM had two homes that disagreed unnoticed: `gate_manifest` took `line[0]`, while `bin/runTests` captured a PASS row per site (of 201 rows: 100 `head -1`, 89 `tail -1`, 3 `grep -m1`).  Five gates confirmed — the manifest recorded a heading, a path, one anchor row and two skipped-case lines as those gates' own accounts of themselves, and `check_equipment_pinned`'s coverage record named precisely the case it had SKIPPED.  ADOPTED: **the claim is the line the gate MARKS as one** (`<label>: OK …` / `[<label>] OK …`, label resolving to the gate's name), one home in `bin/curate/gate_claim.py`, importable AND runnable so the shell reads the same rule; **a gate that marks none gets none** (`claim: null`, censused; `--only` refuses).  Chosen from a measurement: 139 of 191 gates already marked one.  REJECTED: "the last line" (the same guess pointing the other way); matching the bare word `OK` (it would promote `check_ion_pins`' per-anchor row, the line already being mis-filed); donating a heading, a first or a last line to an unmarked gate; refusing to write the whole manifest while 50 gates are unmarked (it would be unregenerable for a month); routing all ~190 `runTests` sites through the helper today (it would blank a quarter of the suite's PASS rows to suit a tool).  The #100 rule is written down once here — a claim states what was SCANNED and what was NOT — with the census recounted (144 quantify, 102 state a limit, 29 neither) and the finding that **the census is itself contaminated by the defect**: two of the 29 have real claims that do both.  NOT done: the 50 unmarked gates and the 29 domain-less claims are NAMED, not rewritten (a campaign, Vítor's decision); no new gate |
| [`a-tool-that-reported-what-it-could-not-see.md`](../design/a-tool-that-reported-what-it-could-not-see.md) | ADR | yes | **SHIPPED 2026-09-06** — a tool that could not LOOK must REFUSE, not report what it did not see: `bin/drive-app` demands a positive sentinel (the app's own chrome in the rendered DOM) before judging anything, and exits 2 naming the URL, Chromium's navigation error and the remedy; and a STALE result DIMS (fingerprint of the declared case stamped on the run, one home, **all of them or none** — never a per-value claim).  Rejected: annotating a stale number (still read as a number), requiring the version badge as the sentinel (absent on a partial mirror = a false refusal), treating the scratch-edit count as staleness (a run does not clear the overlay), and folding the display preset / frozen boundary state into the fingerprint (would make every result read as stale) |
| [`the-student-walkthrough.md`](../design/the-student-walkthrough.md) | SCOPE | no | **WRITTEN 2026-09-02** — the programme behind legs 4–8 had no record of its own: legs 4 and 5 named themselves in their records, 6 in a commit body, 7 and 8 shipped the same day, and no leg below 4 is numbered anywhere in the tree (stated, not guessed).  One table, each leg cited to its commit or record; the method (drive the artefact a student meets, fix at the source) and the named next leg (output legibility) |
| [`equilibrium-parameterisation-identity.md`](../design/equilibrium-parameterisation-identity.md) | ADR | no | SHIPPED — D2 migration closed 2026-07-26 |
| [`computational-seal-migration.md`](../design/computational-seal-migration.md) | ADR | no | SHIPPED — `sealSchema computational`, 328/0 |
| [`basis-reconciliation-spike.md`](../design/basis-reconciliation-spike.md) | SPIKE | yes | SHIPPED (spike only); **mass migration UNAUTHORISED** |
| [`reactive-second-liquid-proposal.md`](../design/reactive-second-liquid-proposal.md) | ADR | no | SHIPPED — §14 records three failed designs |
| [`pcsaft-association-proposal.md`](../design/pcsaft-association-proposal.md) | ADR | no | SHIPPED with three ratified amendments |
| [`batch-temporal-utilities-proposal.md`](../design/batch-temporal-utilities-proposal.md) | ADR | yes | SHIPPED — form B |
| [`pinch-programme-scope.md`](../design/pinch-programme-scope.md) | SCOPE | no | SHIPPED P1+P2; **P3 area-cost UNAUTHORISED** |
| [`fixed-bed-thermal-a5.md`](../design/fixed-bed-thermal-a5.md) | SCOPE | no | SHIPPED — T1, T1.5, T2 |
| [`williams-otto-reference-case.md`](../design/williams-otto-reference-case.md) | SCOPE | no | SHIPPED — all four anchors |
| [`klemola-c4-splitter-reference-case.md`](../design/klemola-c4-splitter-reference-case.md) | SCOPE | no | SHIPPED — column14 anchor + column15 control arm; three of four key rows agree, the fourth is a recorded disagreement |
| [`restricted-speciation-network.md`](../design/restricted-speciation-network.md) | ADR | yes | SHIPPED |
| [`general-salt-reconstruction-proposal.md`](../design/general-salt-reconstruction-proposal.md) | ADR | yes | SHIPPED — slice 1 |
| [`aqueous-stream-basis-proposal.md`](../design/aqueous-stream-basis-proposal.md) | ADR | yes | SHIPPED |
| [`normalized-residual-convergence.md`](../design/normalized-residual-convergence.md) | ADR | yes | SHIPPED 2026-08-09 — OpenFOAM-style normalized residual, ONE home (`src/solver/Convergence.H`); wired to `ReactiveVLE`'s outer Newton, §4 records what is NOT wired and why; rounding, a case-specific tolerance and a second bare threshold are the recorded rejected alternatives |
| [`model-boundary-energy-ledger.md`](../design/model-boundary-energy-ledger.md) | ADR | yes | SHIPPED 2026-08-09 — the enthalpy STEP at a per-unit `thermo {}` boundary is ACCOUNTED, not charged to the unit: three quantities (raw imbalance / declared step / remaining) kept apart on both surfaces, the verdict taken on the third, the step credited only when an INDEPENDENTLY assembled package reproduces it under `solver/Convergence.H`'s declared criteria; absorbing the step into T, pricing the report in each unit's world, and letting the report ask the auditee for its own dH are the recorded rejected alternatives |
| [`sour-water-stripper-scope.md`](../design/sour-water-stripper-scope.md) | SCOPE | no | **S1–S3 ALL SHIPPED (S2+S3 on 2026-08-23)** — the effective stage K, the eight-tray stripper (`stripper01`, the mechanism strict across a decade of loading + the pinned free-ammonia surge), and the Edwards Table 7 anchor (`edwards02`, the vapour side as the speciate op's `vapour {}` block, 18 anchors on the paper's own predictions).  S4 (H₂S) stays the named gap, a curation slice |
| [`curation-backlog-estimated-records.md`](../design/curation-backlog-estimated-records.md) | SCOPE | no | ONGOING |

### Contract written, not built

| record | kind | alt | status |
|---|---|---|---|
| [`provenance-semantics-five-axes.md`](../design/provenance-semantics-five-axes.md) | ADR | yes | **RATIFIED 2026-08-12, contract only** — `origin` / `method`(+`methodVersion`) / `from` / `provenance` / `reviewStatus`+`validation`, one responsibility each.  Retires `derived` AS AN ORIGIN (it only ever said a value has parents — that is `from`) and the scalar `provenance <word>;` shorthand (6 of its 22 uses answered the question the axis is named for; the BLOCK form already did this right).  The calculated/predicted line is *would perfect inputs give an exact result?*, with `K_f` kept as the permanent exhibit: exact arithmetic, inexact physical model.  Rejects storing a confidence tag (compute it from the graph) and rejects re-encoding input status into `origin`.  No implementation authorised |
| [`standard-state-transfer-adr.md`](../design/standard-state-transfer-adr.md) | ADR | no | CONTRACT ONLY — D3, no implementation authorised |
| [`model-declared-record-homes.md`](../design/model-declared-record-homes.md) | ADR | yes | **RATIFIED 2026-08-08** (queue ruling D2) — direction + recommended shape approved; build sequenced after the validation work |
| [`where-a-finding-record-lives.md`](../design/where-a-finding-record-lives.md) | ADR | yes | **DECIDED + IMPLEMENTED 2026-08-05** — debt D7; records to `core`, audit to the engine; checked against DWSIM, which supplies the pattern (`DWSIM.Interfaces`, zero deps) and the counter-example (its solver's compile path to WinForms) |
| [`problem-divergence-contract.md`](../design/problem-divergence-contract.md) | ADR | yes | **RULED + IMPLEMENTED 2026-08-11** — an approximation nobody authorised is REFUSED; one the case authorised runs and RIDES the result on three surfaces, above the KPIs and apart from the advisories.  Rejects filing the authorisation inside `activityModel {}` (a scientific concession is not an object's parameter) and rejects a new AdvisoryLog severity (a divergence is not a qualification).  Records the sabotage that caught nothing and what was changed because of it |
| [`destructive-gate-contamination.md`](../design/destructive-gate-contamination.md) | ADR | yes | **SHIPPED 2026-08-18** — a destructive meta-gate killed by SIGKILL (the signal `subprocess.run`'s timeout sends, against which no `finally` runs) left a POISONED BINARY and 7 later gates recorded false FAILED claims into a manifest that was then committed; 12 such entries were already in the committed history.  A destructive gate now declares itself in a disk JOURNAL that only a verified restore removes, every evidence-producing harness REFUSES while it stands, and `gate_manifest` refuses to write on any failure or timeout because a failing gate makes no claim.  Rejects a lock (the failure is one run dying, not two colliding), automatic repair (a mechanism that silently fixes a tree it does not understand destroys real edits), and reordering the walk so the destructive gate runs last (that hides the class rather than detecting it) |
| [`where-the-catalyst-pellet-lives.md`](../design/where-the-catalyst-pellet-lives.md) | ADR | yes | **RULED 2026-08-18, not built** — the pellet is an asset (`kind catalyst;`, mirroring `Adsorbent`), D_eff is a (catalyst, species) PAIR and never a field on the catalyst, and in the ordinary case it is stored NOWHERE: derived as `(epsilon_p/tau)*D_molecular` and announced with its rule, a `parameters/` record being for a MEASURED value only.  A missing route REFUSES rather than defaulting a tortuosity.  Rejects D_eff on the catalyst record (one number silently standing for every species), a `catalyst {}` block inside the reactor's `operation {}` (two reactors, two copies, drift), a default tortuosity, and kinetics on the asset |
| [`dynamic-unit-chaining-proposal.md`](../design/dynamic-unit-chaining-proposal.md) | PROPOSAL | yes | **RATIFIED AND BUILT 2026-08-24** -- one-step-explicit forward routing over the steady grammar's own `in`/`outputs` words (no new grammar; the draft's assumed `connections` block was never needed); witness `ctrl19_tanks_in_series` lands the Erlang-3 sigma2 at 0.006 % of the closed form with the 2-step transport delay announced and priced; 21-case ctrl corpus byte-identical.  Was: DRAFT 2026-08-23.**  The ctrl driver routes nothing unit-to-unit, so tanks-in-series (the second half of the RTD lesson) cannot run; the one-page proposal is one-step-explicit forward routing over the connections the flowsheet already declares, announced, with refusals and an Erlang-3 closed-form witness.  Rejected within it: implicit-in-time coupling (a DAE architecture question), back-pressure, flow networks |
| [`heuristics-as-a-third-kind.md`](../design/heuristics-as-a-third-kind.md) | ADR | yes | **RULED 2026-08-18, not built** — a heuristic selection aid is NEITHER a method-construction nor a property surface, so the third kind is NAMED (`kind: "selection"`) rather than bent into an existing box.  Heuristics are CURATED RECORDS (claim + primary citation with page + validity domain + what it does not cover); an uncited one is REFUSED, on the unsourced-must-not-become-falsely-sourced rule.  The tool DISPLAYS conflicting authorities and never resolves them by an unpublished weighting — the disagreement is the pedagogy.  Three layers: cited rules and the RGA from the reader's own MESH column are buildable; the dynamic witness is NOT (verified: no column under control in the corpus and `choupoCtrl`'s dynamic tier is DynamicCSTR alone).  Rejects a third top-level surface, a scoring recommendation engine, uncited rules, a fabricated dynamic response, and filing it under `construction` to avoid adding a kind |
| [`electrolyte-authoring-seams-scope.md`](../design/electrolyte-authoring-seams-scope.md) | SCOPE | yes | **ALL SIX SEAMS CLOSED (S3+S5 executed 2026-08-24 on the architect's close-the-seams ruling; the rest 2026-08-23).**  S5 took option (b): the davies-only guard was the ONLY code between the declared and the served grammar; the reactive slice now serves pitzerHMW, with edwardsPitzer refused on standard-state coherence and the composite routes kept davies-only (witness `flash21_brine_co2_pitzer`).  Rejects narrowing the grammar to buy consistency; success criterion was the measured re-run round-count |
| [`interim-review-2026-08-24.md`](../design/interim-review-2026-08-24.md) | EVIDENCE | no | **The delegated INTERIM review, 2026-08-24** — three independent blind-protocol reviewers over the primaries in hand, every finding audited before landing.  Edwards set exact against the paper (one real find: two Eq 14 windows claimed Figure 4's span, corrected); 13 Antoine blocks verified against their cited distribution (six windows clamped, one fit-form word fixed); Millero V0 and the acetic dimer honestly out of reach.  Promotion executed for the verified group ONLY, on the owner's delegation; every mark that still tells the truth kept |
| [`glass-box-reading-benchmark-2026-08-24.md`](../design/glass-box-reading-benchmark-2026-08-24.md) | EVIDENCE | no | **Four fresh READERS, the output alone, 2026-08-24** — the mirror of the authoring benchmark, measuring the half the STUDENT owns: given only a case's output, can a reader understand, verify and correctly distrust it?  Three readers re-derived the engine's own arithmetic by hand (Antoine + Rachford-Rice to ten digits; Davies charge balance and ionic strength on four trays; Erlang-3 moments off the E-curve) and the divergence block correctly stopped one from quoting any separation number.  Six findings, each audited before recording: an impossible inlet phase label that costs 808 kW and is never checked though the engine prints the test (F1 — and it CORRECTS this session's own earlier misdiagnosis); a caveat surface defeated by 99 unattributed copies of one warning while the one describing the answer hides among them (F2); chemistry records with no validity domain at all, two of whose K-routes disagree by a factor of 2.22 (F3); an identity case whose golden pins no speciation number (F4); five small truths the run knew and did not say, four now FIXED (F5); and three case narratives that contradict their own runs while every number beside them is pinned to 1e-4 (F6).  Decides nothing by itself: it is the evidence the next slice stands on |
| [`block-tridiagonal-mesh.md`](../design/block-tridiagonal-mesh.md) | ADR | yes | **SHIPPED 2026-08-25** — the MESH declares its Jacobian block-tridiagonal (`opts.blockTri`) and the solver AUDITS the declaration on the first iteration, refusing by name when false; Curtis-Powell-Reid 3-color FD (~6·nv evaluations per Jacobian, stage-count-independent) + block-Thomas.  Measured: 48-stage reactive stripper 62.7 s → 28.9 s, identical convergence, Jacobian evaluations ÷16; all 20 column tutorials byte-identical.  The structure was MEASURED on the corpus before the solver was built on it (off-band exactly 0).  Declaring the bordered fullMESH clean-tridiagonal, and trusting the declaration without the dense first-iteration audit, are the recorded rejected alternatives |
| [`correlations-as-objects.md`](../design/correlations-as-objects.md) | ADR | yes | **SHIPPED 2026-08-25** — the `HeatTransferCorrelation` discipline (declared validity window, `verify()` against a published anchor, a bench that prints the deviation beside the source) applied to a SECOND family: the Darcy friction factor.  Blasius/Colebrook/Haaland/Churchill leave `Pipe.cpp`'s free functions and gain `citation()` — *a correlation whose source is a comment is one the reader cannot check*.  The anchors are deliberately of DIFFERENT KINDS (arithmetic / iteration-arrival / the author's own published claim / an exact limit), and the witness publishes TWO spreads (25.74 % over all four, 1.72 % over the three inside their windows) because collapsing them would say the correlations disagree when one was simply misused.  Pipe's goldens byte-identical.  Ranking the correlations, and a single headline spread, are the recorded rejected alternatives; no correlation is checked against MEASURED data and the gate says so |
| [`transport-correlations-as-objects.md`](../design/transport-correlations-as-objects.md) | ADR | yes | **SHIPPED 2026-09-05** — the correlations-as-objects discipline applied to the FIVE transport families (Chung, Eucken, Fuller, Andrade, Vogel, chemsepEq101, SatoRiedel, chemsepEq16) and the shared Wilke φ_ij, which `ThermoPackage.cpp` had transcribed TWICE.  Every anchor states its KIND — *theory* (Eucken's monatomic (15/4)Rμ/M, Wilke's φ_ii = 1) or *arithmetic* (the closed form at a stated point) — because the catalogue holds NO measured transport data.  Four honesty fixes rode with it, numbers unmoved: the Theory Guide's `Sutherland`/`lennardJones` listing (keys no engine ever read), Chung's Vc-estimate and non-polar truncation now ANNOUNCED per component, the spray dryer REFUSES without a transport model instead of reporting a drying time of zero (and refuses a declared liquid model it cannot price instead of substituting water), the membrane module ANNOUNCES its legacy μ/D constants.  Rejected: ranking the models; an anchor on Fuller's paper example without the paper in hand; inventing LJ σ/ε to build Chapman-Enskog — that is a CURATION campaign (Svehla 1962, NASA TR R-132), named and not started here.  **§8, 2026-09-06: the campaign RUN** — Svehla's Table I(a) transcribed from the page image into ONE committed home (`bin/curate/svehla1962/`), `propose_lennard_jones.py` deriving fragments into `data/local/` by parsed formula + MW cross-check (56 matched, 20 excluded-only, 1 ambiguous; REFUSES `data/standards/`), `ChapmanEnskog` (Svehla eq. 1) and `modifiedEucken` (eq. 2) registered on the Neufeld Ω^(2,2)* fit's one home, the `lennardJones {}` record block with PER-VALUE origin (`measuredFit` registered as the `regressed` rung), witness `transport02_chapman_enskog` with seven case-local adopted records and the first real spreads; the reproduction anchor DECLARES its 1 % tolerance because Svehla's Hirschfelder Ω tables and the Neufeld fit disagree by 0.1–0.8 %.  NOT built, said plainly: Chapman-Enskog D_AB (Ω^(1,1)* not citable from the tree) and Sutherland (no S); no block promoted into the public catalogue |
| [`the-licence-of-a-sigma-profile.md`](../design/the-licence-of-a-sigma-profile.md) | ADR | yes | **SHIPPED 2026-09-06** — Vítor asked whether the COSMO-SAC parameters could join the standard base "if it is legal"; the three primary texts were read that day and quoted verbatim (usnistgov/COSMOSAC's README: the profile folders are under a LESS permissive licence than the code, academic non-commercial by BioVia permission; the UD folder's own Readme: redistribution prohibited; the original Virginia Tech page: a download link under an "Open Literature Resources" heading and NO rights statement at all).  Verdict under §10's licence policy: a dataset with no licence statement is no-grant, a NonCommercial permission is out, and only a WRITTEN grant from Y. A. Liu (VT) or Ian Bell (NIST) changes that — a human act.  The 2026-07-26 separation stands; NO value added.  Two corrupted memories found and fixed: `CosmoSac.H` credited the profiles to "the LVPP open database" with "LVPP == the NIST benchmark data", and `CLAUDE.md` called VT-2005 "US-gov public domain via the NIST bundle".  LVPP is MIT and thermodynamically unusable under the 2002 constants (the 2026-07-24 study: the sign of G^E flips), so a `variant "2002"` label — the ONLY thing the engine's guard reads — is a claim about origin; `check_cosmo_scrub` arms (v) and (i) refuse a 2002 label on a foreign or unnamed source and `installed true;` on any VT2005 set.  The first draft's arms ran on NOTHING (a non-overlapping block regex consumes every leaf) and a hand sabotage caught it.  Rejected: reading a page heading as a grant, shipping "for academic use", shipping LVPP under the 2002 label, a `via` route through NIST.  NOT done: no LVPP variant (reserved, task #95); `lvpp_sigma_profile.py` still emits the 2002 label (a finding, reported); VT-2005 values found TRACKED under `data/tmp/_sources/` (16 CSVs + the DMol3 ethanol pair), outside the gate's scan — reported, not deleted |
| [`the-key-nobody-read-in-the-postdict.md`](../design/the-key-nobody-read-in-the-postdict.md) | ADR | yes | **SHIPPED 2026-08-27** — `DictAudit` has caught unread keys since 2026-08-14 at exactly ONE site, each unit's `operation {}`, and `postDict` was outside it: a `costing {}` declaring `targetYearCEPCI 800;` priced a plant at the DEFAULT index of 820, 2.5 % of the capital cost, on a number the author believed they had set.  `childDictsUnnoted()` + `auditTree()` reach the nested blocks; the walker reads `entries_` DIRECTLY because an auditor going through `subDict()` would mark every block it visits as read and then report that nothing was unread — measured honestly as LATENT today, since `auditTree` audits before descending.  Where edit distance has nothing to say the finding carries the MENU of names the block's reader asked for.  The corpus run found `method` in the economics block DECORATIVE and **`ammonia02_full_plant` publishing a €196.5 M NPV discounted over a one-year construction while declaring `constructionPeriod 2;`**.  REJECTED: refusing a non-1 construction period (my judgement overriding an author who meant 2, and the fix that MOVES an NPV is reserved) — and §7 records that the first version DID refuse, on an inference from three flagged cases generalised to nine, then verified by reading zero findings from a CRASHED run as success |
| [`a-cost-you-can-defend.md`](../design/a-cost-you-can-defend.md) | ADR | yes | **SHIPPED 2026-08-27** — leg 4 of the student walkthrough: take €166,653 off the costing table and answer *"where did that come from?"*  From the source every step is right; from the OUTPUT it came from nowhere, with eleven numbers and five decisions between `F_M 3.05` and the total.  The costing pass now prints its own arithmetic (correlation, coefficients, size driver, CEPCI ratio, EUR/USD, B1/B2, the 1.18, Turton App. A) and `VesselSize` publishes the design rule it used to compute with three careful branches and then discard as `(void)basis`, beside the fact that its driving `Q = N R T / P` is IDEAL GAS whatever thermo the case declared.  **A provenance line too coarse to reproduce is worse than none**: the first version printed `B1, B2 = 2.2, 1.8` (significant digits on a fresh stream) and a reader redoing the arithmetic lands 2.6 % out and blames themselves — caught only because it was printed, and again one field over in `Q`.  REJECTED here and RESERVED for Vítor: renaming the model, which is registered as `Guthrie` while every coefficient in it is Turton's — an attribution ruling, and no value moves either way |
| [`process-safety-audit.md`](../design/process-safety-audit.md) | EVIDENCE | no | **2026-08-26** — the ten-question audit of what Choupo can and cannot say about process safety today, plus the inherently-safer-design section (Kletz's intensification / substitution / attenuation / simplification, and the hierarchy inherent > passive > active > procedural).  Records that the engine holds inventories, duties and phase states but names no hazard, and that API 520/521/526/537/2000 are copyrighted no-grant sources in the same class as DIPPR and CRC, so no relief-sizing correlation can be transcribed here.  Decides nothing: the chapter (S0) and `inventoryReport` (S0b) await Vítor's go-ahead |
| [`the-pellet-runs-hot.md`](../design/the-pellet-runs-hot.md) | ADR | yes | **SHIPPED 2026-08-27** — the owner's students "sentem mal o módulo de Thiele" and wanted a 2D temperature field.  Two facts came first: the 2D picture ALREADY existed (the tool paints rings and slabs from the published field) and the engine REFUSED temperature with a stated reason (coupled to Arrhenius the pellet has up to three steady states, so its answer is a SET).  PRATER's relation closes the gap without touching that refusal: the two balances carry the same reaction term, so eliminating it gives a first integral, `T - T_s = beta·T_s·(1 - c/c_s)`, and the temperature field costs no second BVP.  **And it is EXACT here, which is the opposite of the obvious reading and what the first draft of the record got wrong**: this op takes `rateConstant` as a NUMBER, not an Arrhenius law, so the equations do not couple and the isothermal profile IS the profile.  Witness `thiele02_prater_temperature` is `thiele01` plus one block, so the difference IS the temperature: β = 0.4017, centre 736.65 K against a surface at 573.15 K, **163.5 K hotter inside**.  GUI: one toggle repaints the same discs, REJECTED being a second strip of discs (the strip height is a measured budget) and a per-shape colour normalisation (it would paint three different pellets identically).  NOT done and still announced every run: the coupled non-isothermal pellet and its multiplicity |
| [`entropy-glass-box-trace.md`](../design/entropy-glass-box-trace.md) | STUDY | yes | **SHIPPED 2026-08-30, all four phases same day** — the owner asked "how exactly is an entropy value calculated here?" and a grep answers nothing, because the physics lives in seven names (`s_298` → `s_formation` → `s_pure_ig` → `S_ig` → `S_residual` → `S_real` → `dS_gen`) and the word only in prose.  The trace (file:line per claim) became the spine of the "What is entropy?" EduTool page (which re-adds the engine's own published lines on screen), the `explainProperty` bench op (§8 — the ledger as an engine surface, refusing when its explanation and the engine disagree), and the guides addendum.  Rejected: silently adding PC-SAFT's missing `+R·ln Z` (a numeric change to a published surface — flagged in §6, reserved to the owner) and fixing the ambient-precision result-JSON emitter in passing (a corpus-wide golden migration, recorded as its own task).  Found on the way: `sAq` carried but unconsumed (40/53 records), the aqueous world running on K(T) with no code naming ΔS°, and the vaporisation leg's Gibbs term being a fifth of water's standard-state step |
| [`a-noncommercial-compilation-in-the-public-tree.md`](../design/a-noncommercial-compilation-in-the-public-tree.md) | ADR | yes | **SHIPPED 2026-09-01** — looking for a clean source of S6/S8 for the sulfur tutorial, `S2.dat`'s banner read "(BURCAT.THR)" while `bin/curate/import_gibbs_nasa.py` had said in capitals since 2026-06-14 that Burcat is licence-EXCLUDED (NonCommercial) and `check_source_licence` — the gate whose job is exactly this — had never heard the name.  **One decision, three homes: a tool that forbids it, a tool that performs it, and a gate that does not know it exists**, so the exclusion was a sentence in a comment while the import was a fact in the tree (36 records).  The new rule is deliberately NOT the aggregator rule: `via` rescues a primary read through NIST, but for a compilation the arrangement IS what would be copied, so there is no via branch — only a CROSS-CHECK marker, and `OH.dat` is the single record that uses it that way (sabotage S2, "via Burcat", is the arm an implementation written by analogy would have got wrong).  Nothing deleted and no number written: the 36 are the whole radical/sulfur/chlorine pool six Gibbs tutorials stand on, and whether "Burcat 2005 / JANAF" cites a citable paper or the database under another name is a legal reading with two defensible answers, **reserved for Vítor**.  §2 measures what the remedy costs (`bin/curate/verify_against_nasa.py`, a TOOL not a gate): 48 records against NASA TM-4513, **34 agree within 0.5 kJ/mol, 5 differ by more**, and three of those five are pinned records — so discharging those pins moves a value by 10 to 89 kJ/mol while the other 23 change nothing a reader would notice.  Two mistakes recorded rather than tidied: an entropy "correction" applied on the strength of cantera's 1 atm LABEL manufactured exactly the 0.109 J/(mol·K) systematic error it was meant to remove (measured raw, the median is 0.0022), and CaO's 679 kJ/mol "disagreement" was a heat of sublimation, our datum being on the solid rung against an ideal-gas table |
| [`what-a-promoted-record-must-carry.md`](../design/what-a-promoted-record-must-carry.md) | ADR | yes | **SHIPPED 2026-09-03** — slice 1 of the curation campaign: the binary-pair `.dat` a student MOVES into `constant/parameters/` printed the evidence, the held-out AAD, the band declared before the fit and the verdict, and then wrote a file naming NONE of them — with the ORIGIN word in the `source` slot (`source fitted;`), which `thermo/PairAudit.H` reads as a LEGACY source word and deprecates, so the tool built to help a student curate warned against itself on every promotion.  Fixed on the axes ratified 2026-08-12 and in the layout the curated corpus already uses; the write MOVED after the held-out pass, because the verdict does not exist before it.  **The rejected alternative is this slice's own first draft**: `origin` at the record's TOP LEVEL, on the reasoning that an axis deserves its own home — it ran, it loaded, and it resolved `unattributed` silently, because an axis is a RESPONSIBILITY and not a nesting level.  Found on the way: `ResultEmitter` emitted three keys BY HAND beside the ONE shared formatter that emits them (duplicate JSON keys on every pair resolution, in every `choupoSolve` result), and a `.gitignore` rule outlived its directory rename by seven weeks.  NOT done: the ~95-value corpus vocabulary migration, which no record authorises.  Gate `check_promoted_pair_record`, 5 sabotages; the artefact is REBUILT and never tracked (its `fitDate` would go dirty daily), and the gate's own first probe stripped its input with a DOTALL regex that matched the word inside a header COMMENT, deleted the liquid phase, and blamed the engine |
| [`a-rule-enforced-in-one-place.md`](../design/a-rule-enforced-in-one-place.md) | ADR | yes | **SHIPPED 2026-09-04** — slice 2 of the curation campaign, and a measurement of a claim this repository makes in four places: *the engine refuses to write under `data/standards/`*.  ONE code site enforced it (`estimateComponent`); 50 lookups across 34 files take an output path from a dict and 32 of those files open a stream with it.  **Reproduced before anything was written**: `fitParameters proposal` pointed into a standards path wrote a 2663-byte PROMOTABLE PAIR RECORD there — carrying the `provenance {}` block `PairAudit` reads as authoritative — and exited 0.  One home now (`records::refuseStandardsWrite`, beside `standardsRecord()` which already computes that path; a separate `core/ProtectedPaths.H` was rejected as a second home for it).  TWO rules whose DISJUNCTION is the contract: a literal `standards` path component (kept because it is the one that still fires for a SEALED case, which has no catalogue root to compare against) and weakly-canonical CONTAINMENT (catches the symlink the literal scan cannot see).  Deliberately NOT guarded: the outer drivers' `path` keys, which are DICT paths — the misclassification a mechanical sweep invites.  Gate `check_standards_write_guard`, 6 sabotages, **one survived**: deleting the guard where `proposal` is READ left the gate green because the same op guards the path `auto` resolves to, so the gate now requires no `Final chi2` ahead of the refusal — the early guard's purpose, made checkable.  Its single exemption carries its premise as a CHECK, not a claim |
| [`what-seven-readers-found-in-one-morning.md`](../design/what-seven-readers-found-in-one-morning.md) | STUDY | yes | **2026-09-04** — seven read-only agents, one morning, one subject each: the student's first hour · the absences the preceding 48 hours should have buried · the refusal messages a student actually hits · tutorial headers against their own runs · the GUI's dead surfaces · the manuals against the engine · the curated data against itself.  All seven could run binaries on copies in /tmp and could not modify one byte of the tree — the 2026-08-31 shape, and the constraint is what makes it work: a wrong report costs five minutes, a wrong write costs hours and contaminates what the other six are reading.  **The through-line, which one of them phrased better than the brief did: the failures are all GENERALISATION failures — the good message exists somewhere and was never wired to its siblings.**  The unread-key audit is wired to four dicts and not the thermo one; the near-name join exists for components and not for stream names or unit types; the rich non-convergence diagnosis exists in `Mixer`, under a comment describing this exact defect, and not in the flowsheet fallback or seven sibling throws; the `feedQuality`-vs-`vf` contradiction is refused by name on one column path and ignored on the other.  Worst finding, and two lines of shell: `runCase` and `runApplication` both end in a FAILED branch that `set -euo pipefail` made UNREACHABLE, so a student's first typo printed three lines that look like a normal start and then nothing, while the engine's own excellent message sat in the log.  The REJECTED alternative is the tempting one throughout: apply findings straight from a report.  Every one acted on was re-verified by hand first, and the audit's own membrane finding is RESERVED rather than applied, because rewriting a header to match the engine would pin a possibly-wrong answer as the reference |
| [`the-verdict-that-reached-everyone-except-the-machine.md`](../design/the-verdict-that-reached-everyone-except-the-machine.md) | ADR | yes | **SHIPPED 2026-09-04** — slice 3 of the curation campaign.  Three fit ops decide a CURATION VERDICT from a held-out pass against a band declared BEFORE the fit, and that word reached the console, the curation dossier and the promotable record — and NOT the result JSON, the only channel a PROGRAM reads.  Measured on curate02: 48 diagnostic keys, three on the held-out axis, no verdict and no band.  Two consequences, each the inverse of a rule already written here: the GUI's fit panel could not show it and drew the IDENTIFIABILITY verdict instead (a different question — are the parameters separately determined?), and no golden row could reach it, so a number a reader acts on was published to a human and pinned by nothing.  `PropertyOperation::curation()` is the maturity axis as WORDS, kept apart from `diagnostics()` (numbers) and `provenance {}` (where the data came from), and named `curation` because `validation` ALREADY names that document's overlay AAD table — a second `validation` in one document is the `T_top` defect verbatim.  **The decision is taken ONCE**: `verdictOf` is the one home for the RULE and was CALLED three times, one of those inside `if (verbosity >= 2)` — a verdict that depends on how loudly the run was asked to speak is not a verdict.  New golden row kind `verdict`, compared as a WORD with `exact`.  Traps paid for: `--record-append` carries a hard-coded kind allowlist that DROPS an unknown kind in silence (it reported "refreshed" over an unchanged file), and drawing the five verdicts in the fit panel made a FOURTH hand-written home for that vocabulary, added to `check_verdict_parity` in the same commit.  NOT unified: the two op families print the verdict in two different console shapes, recorded in the gate rather than papered over.  Gate `check_fit_verdict_channel`, 4 sabotages; its strongest arm requires every op that CALLS `verdictOf` to publish one, because `heatCapacityFit` has no witness and nothing else would notice it deciding in silence.  One sabotage PROVED NOTHING and is recorded: a `git checkout` restore reverted an UNCOMMITTED golden row, so the failure reported was about the golden and not the sabotage |
| [`the-hierarchy-that-only-existed-in-the-name.md`](../design/the-hierarchy-that-only-existed-in-the-name.md) | ADR | yes | **SHIPPED 2026-09-04** — Vítor: the design report must show a HIERARCHY of sectors and unit operations, and economics must use it.  `Flowsheet::flattenNode` knew each leaf's owning sector exactly (`nsPrefix` IS the parent chain, held in hand at the moment the leaf is emitted), concatenated it into the qualified name and kept only the string — so every reader downstream got a flat list of dotted names and no structure, the design table printed `CONCENTRATION.Crystcrystalliser` (the name overflowing its column into the equipment type, on exactly the cases whose hierarchy the table exists to show), and "the concentration sector is 93 % of the capex" could only be reached by adding dotted names up by hand.  The REJECTED design is the cheap one: `name.rfind('.')` is right for every case in the corpus today and is **name identity**, which the F2 contract bans — it misfiles silently any unit whose name carries a dot for another reason.  ADOPTED (Vítor, "faz como recomendas"): the sector is STAMPED where it is known and travels as DATA — `FlatUnit::sector` → `EquipmentSizing` → `CostBreakdown`, one origin, four carriers, no re-derivation.  The hardest rule, and the acceptance condition: **empty is not a sector called "root"** — a flat case gains no dict key, no banner, no CSV column, no capital-by-sector block, and was verified byte-identical against a worktree build of the previous commit on all seven flat sizing/costing cases and both their CSVs.  The name column is floored at the 14 it has always been, so only the two fractal plants widen (measured: the longest unitName in every flat case is 11).  Gate `check_sector_hierarchy`, 6 sabotages; its arm (d) is a SOURCE arm because arms (a)–(c) cannot tell a correct stamp from a correct name split on a corpus where every sector name is exactly the first dotted segment — the design decision has to be checked where it is written.  NOT DONE: `design`/`economics` still do not run by default (RESERVED for Vítor), the GUI does not read the sector yet (the Plot tree is its own slice), and nesting deeper than one level is carried but unexercised |
| [`the-specification-sheet-a-project-is-audited-from.md`](../design/the-specification-sheet-a-project-is-audited-from.md) | ADR | yes | **SHIPPED 2026-09-04** — Vítor: a student arrives with a final-year project and he must be able to see, in the dictionary tree, PER UNIT OPERATION, a specification sheet (inlets, outlets, sizing), because "isso faz parte de um bom projeto e devia de ser facil de auditar".  Both architecture decisions were DELEGATED to me the same day.  Measured before deciding: the ports existed per unit only inside `iterations/` (opt-in, off by default, numerical history), the sizing only in two flat CSVs, and `design/` was a directory NAME ratified 2026-07-06 that nothing had ever written.  ADOPTED: `design/<SECTOR>/<unit>/<equipmentTag>`, ONE dictionary per physical item, regenerated whole every run like `converged/`.  REJECTED, each with its reason: (1) keying on the UNIT alone — hardens the 1:1 equivalence §2.8 deletes by name, so the shape is 1:N with N = 1 and a column that yields five items gains siblings while nothing above changes; (2) the `{design,geometry,rating}` TRIPLE this project had itself ratified — three files per pump is fragmentation and an auditor opens one page; (3) putting the sheet inside `sectors/<S>/unitOperations/<u>/`, which reads better but serves 2 of 423 cases (measured) and would make the economics ENUMERATE equipment by a tree walk whose result depends on an authoring choice; (4) the `[M L T Theta N]` exponent vector as the unit form — correct but noise on a sheet a student hands in, so named units, with the null vector used ONLY for the dimensionless case because omitting the unit is indistinguishable from forgetting it; (5) converting the sizing map to canonical SI — a scientific change riding on a formatting one, and it moves every golden that pins a size.  The durable half is D4, OpenFOAM's rule that an object declares its own dimensions: `EquipmentSizing::set()` is the one door, 45 assignments across 8 sizers, and the migration found the values were never uniformly SI (kW, bar, µm, rpm; six keys with the unit inside the KEY NAME).  Three traps paid for and recorded: a port mass computed as `F·Σz·MW` drops the crystals (the flash21 defect, in a new surface, with `StreamMass.H`'s own header describing it); the first gate draft patched a source and rebuilt the engine, which is the 2026-08-18 tree-poisoning shape (replaced by a source arm; refusal fired by hand under the journal, which then found a `throw` leaving a PARTIAL tree that lies by omission); and `**/design/` swallows `docs/design/`, silently, for every new record.  Gate `check_design_sheet` — its load-bearing arm reads a report this writer does not produce.  NOT DONE: no `system/designDict` (no reader anywhere), no new sizing content, and — until slice 2 the same day — it did not reach the GUI.  **Slice 2 (2026-09-05):** the case tree is recursive (`gui/src/ui/caseTree.ts`, tested), the worker harvest selects run-output trees from one `OUTPUT_ROOTS` list, and the sheets ride their own `designFiles` channel so an absent sizing pass cannot read as a failed solve.  **Slice 3 (2026-09-05):** the tree draws by KIND — `caseTree.kindOf()` the one home for declared / 0/ / sector / run output (five homes before, none in the tree; `converged/` read as a fifth sector), outputs dimmed and badged, sectors glyphed, order by kind, `CaseIntro` on the same home; REJECTED a synthetic Sectors heading and hiding outputs; NOT VERIFIED with real students |
| [`a-basis-nobody-stated-and-a-volume-in-kilomoles.md`](../design/a-basis-nobody-stated-and-a-volume-in-kilomoles.md) | ADR | yes | **SHIPPED 2026-09-05** — Vítor saw two evaporators at 60.00 m² with no design basis under a Reports header that promised one.  Of eight sizers only `VesselSize` stated the rule that produced its size; the other seven drew `(not stated)` on every surface.  RULE: every sizer states its basis at the site where the rule is applied, a pass-through says it is one, and `EquipmentSizing::basis` is the one home (console, `sizing.csv`, the sheet and the GUI read it; none invents one).  WRITING THE BASIS FOUND A DEFECT: `CrystalliserSize` multiplied `liquorFlow` (the unit's MOLAR flow, kmol/s, labelled so in its own source) by `residenceTime` and called the product m³ — 18.46 'm³' on a vessel declared 1.0 m³, costed on the flagship plant an order of magnitude high, at exit 0, with a golden pinning the wrong number.  ADOPTED: the crystalliser publishes its declared working volume (`V_magma`) and the sizer passes it through, keeping `liquorFlow` under its true unit and adding `throughput`.  REJECTED: deriving V as `throughput × residenceTime` — it reproduces the declaration to round-off and hides that it is one.  Gate `check_design_sheet` arms (i) SOURCE every sizer assigns `d.basis`, (j) INDEPENDENT a crystalliser sheet's `V_magma` equals the case's declared `operation.volume`, read from the dicts never the run — the arm that would have fired for the defect's whole life.  NOT gated: whether a basis string is TRUE of its sizer; only a reader can check that, which is exactly how this was found |
| [`one-unit-five-items-the-column-gets-a-size.md`](../design/one-unit-five-items-the-column-gets-a-size.md) | ADR | yes | **SHIPPED 2026-09-07** — task #79, authorised by Vitor.  The distillation column was the only major unit with NO sizer, NO cost and NO specification sheet: `SizingPass` found nothing registered for `distillationColumn`, refused by name and listed it under `notSized`.  Measured first: 30 tutorial cases declare a column and ZERO shipped a `system/postDict`, so no column had ever been sized or costed; and `TrayHydraulics` had computed the hydraulics all along into a console print — a column cannot be SIZED from a printed table.  ADOPTED: the 1:N shape the 2026-09-04 sheet writer chose for exactly this case, with five items (shell, trays, condenser, reboiler, reflux drum); `EquipmentSize::size()` returns a VECTOR (one door, not two — a second virtual returning a single record would let a sizer answer one and not the other), and `EquipmentSizing::itemId()` is the ONE home for the item identity, equal to the unit name wherever a unit realises one item, which is what kept every existing case byte-identical.  NO NEW PHYSICS and NO NEW DATA: the diameters are the hydraulics' own rule applied per section, the duties are the column's own KPIs, and the shell/exchangers/drum all reach coefficient sets already in `Guthrie`.  REJECTED, each with its reason: (1) inventing tray cost coefficients — a CURATION act inside what is reserved, and an invented set turns `uncosted` into `falsely costed`, which no reader and no gate can detect, so `sieveTrays` REFUSES by name and the total reads INCOMPLETE; (2) substituting a default U for a condenser or reboiler — it would be priced into the capital cost as though somebody had chosen it; (3) sizing the reflux drum with `VesselSize`, whose volumetric flow is the IDEAL-GAS N R T / P and is wrong on a condensate by three orders of magnitude; (4) deciding the SWAGE — it is an economic comparison and Choupo prices no transition, so it publishes both section diameters and their gap and builds straight; (5) throwing for a missing auxiliary item, which would cost the column its whole sheet.  Four traps paid for: `nStages - 2` trays was WRONG BY ONE (the stage list carries the reboiler and not the condenser) so the count comes from the pass that ENUMERATES trays; the golden `equipment` kind resolved on the UNIT and collapsed five items into one row per key; `sizing.csv` was not a CSV once a `basis` sentence carried a comma; and `check_design_sheet` arm (j) — the only arm anywhere that can see a size that is WRONG — had NEVER been able to fire, reading a header key the sheet does not write.  Gates: `check_design_sheet` arm (m) (7 sabotages, landed BETWEEN the run and the check because the arm regenerates `design/`) · `check_equipment_pinned`.  NOT DONE: the tray cost (reserved) and 29 of the 30 column cases still declare no sizing pass.  The column schematic it deferred (task #134, deliberately after the cost) SHIPPED the same day — see the row below |
| [`the-column-draws-itself.md`](../design/the-column-draws-itself.md) | ADR | yes | **SHIPPED 2026-09-07** — task #134, Vitor's idea, authorised the same day, riding on #79.  Of the column's five specification sheets the condenser and the reboiler needed nothing, because THE DATASHEET FOLLOWS THE EQUIPMENT KIND AND NOT THE UNIT TYPE and both are `shellTubeHX` items; what had no drawing at all was the column's own equipment, the tower and the tray stack.  ADOPTED: one registry keyed on the sheet's own `equipment` word — the same registered-type word the sizers use — so a kind is one entry and no reader changes; a kind with no drawing gets a LABELLED BOX with its ports and its sizing table, because there is no generic outline that is nearly right for a crystalliser, a cyclone and a compressor, and a labelled box beats a wrong picture.  The tower is keyed on `sieveTrays` and NOT on `vessel`: `vessel` is the shell's own word AND the reflux drum's AND every flash drum's in the corpus, so keying it there would put fourteen trays inside every drum — the trays are the fact that makes it a column.  THE DECLARED/COMPUTED LINE is the substantive half: omit `hydraulics { diameter }` and the pass DESIGNS the tower, declare it and the pass RATES the trays against a tower the author chose, and publishing the second as the first would be the falsest line on the page — so the drawing reads the engine's OWN `diameterDesigned` KPI rather than inferring the mode, marks every dimension in two channels (ink AND the word, because colour does not survive a greyscale print), and says *provenance not published* rather than guessing when the run states neither.  NO transition cone is ever drawn, in any case: `ColumnSize` sizes the shell STRAIGHT and publishes ONE `D`, so the narrower section is a DASHED outline at scale — 8.08 % of 1.32 m is two pixels on the witness, annotated rather than exaggerated, because a dashed line pushed apart to be visible would draw a gap that is not there.  Traps paid for: PROSE DOES NOT GO IN AN SVG (no reflow — two captions were rendered and silently cut mid-word, and both moved to HTML); a connector's label is PLACED, never guessed from its midpoint (*overhead vapour* landed on the condenser box); `check_design_sheet` arm (l)'s whole-file fixture search went BLIND the day one test file held three sheets carrying identical port blocks — the sabotage SURVIVED and forced a per-template-literal match, split on UNESCAPED backticks because the sheets' own header comments contain them; and arm (l)'s deliberate refusal to hold the PORT blocks ("no GUI reader reads them") expired the moment this reader did, whereupon the stirred-tank fixture turned out to carry none at all.  Found by reading the witness rather than the brief: `column09`'s header taught that the stripping trays are NOT the tight ones, which its own printed table contradicts on both counts (the vapour velocity RISES across the feed, 1.044 -> 1.092 m/s, and the flood approach climbs monotonically to 80.0 % on stage 14) — corrected in the header, the generated guide and the rebuilt PDF, no number moved.  Gate: `check_design_sheet` arm (l), 3 by-hand sabotages, one of which survived first contact.  NOT DONE: the mechanical/handoff block (task #142 — real data sheets MARK the cells somebody else fills and that needs its own rulings), packed columns (no packing model exists anywhere in the engine — new physics, Vitor's), a corpus witness for the RATING label (only column09 ships a `postDict` and it runs in DESIGN mode, so that arm is a stated CONSTRUCTION), and any schematic for the other seven equipment kinds, which get the labelled box |
| [`a-first-law-the-gui-computed-for-itself.md`](../design/a-first-law-the-gui-computed-for-itself.md) | ADR | yes | **SHIPPED 2026-09-05** — Vítor, live site, flagship plant: "a violação à primeira lei parece-me exagerada" (372.5 kW, 1.79 %).  The engine's own energyBalance report closes the same run at 34.4 kW (0.163 %); the GUI had summed utility-ALLOCATED duties itself and missed both cooling duties no utility served — 372.5 − 34.4 = 74.8 + 263.3 exactly.  A second home for a balance the 2026-07-19 doctrine makes engine-owned.  ADOPTED: the report stamps `globalEnergyBoundary` on the result, ONE object on ONE line; the three GUI surfaces draw it and say "the report did not run" when absent; `energyBalance()`/`unitEnergy()` deleted with their tests; `boundary` golden kind, rows appended corpus-wide (adds only), `check_energy_boundary_pinned` both directions; the residual pinned only when ≥ 1 W (column13 lesson), threshold in ONE home (the generator).  REJECTED: a GUI fallback computation when the block is absent (the second home under another name — the exact path that showed 372.5 kW); splitting the engine's net Q into heating/cooling bars from the allocation.  FOUND ON THE WAY: the flagship's golden was never in the full suite (fractal `children` cases skipped on a reason settled 2026-06-08) — in the walk now; three units genuinely do not close (Evap1 98.45 %, Cryst 110.66 %, Fermentor 82.02 %), left as the engine's own stated finding |
| [`main-is-a-sector-and-the-views-repeat-the-plant.md`](../design/main-is-a-sector-and-the-views-repeat-the-plant.md) | ADR | yes | **SHIPPED 2026-09-05** — Vítor brought a reading of the flagship's full tree (ChatGPT): sectors in CAPS, but `JuiceSplitter/` — a unit — sat at the plant root beside them, and three plant-level streams with it.  CONVENTION ADOPTED: in a fractal case a CAPS folder is always a sector and a plant-level unit lives in one, `MAIN/`; every view (0/, converged/, design/, internalStates/) repeats the one geography; system/ and constant/ at every level, never inside it; the nine-line student language recorded.  The flagship migrated; `0/MAIN/` and `converged/MAIN/` fell out of the EXISTING ownership rule (predicted, then observed via choupo-lint); the golden passed unchanged.  REJECTED: forcing MAIN/ on flat cases (no mass migration; empty is not a sector called root, 2026-09-04); the engine inferring sector-ness from capitals (name identity); a GUI-invented MAIN.  NOT VERIFIED with students |
| [`internal-states-are-a-projection-of-profiles.md`](../design/internal-states-are-a-projection-of-profiles.md) | ADR | yes | **SHIPPED 2026-09-05** — task #90: `internalStates/` was a view NAMED in the stream-state architecture, listed in the GUI's `RUN_OUTPUT_ROOTS` and drawn in the nine-line student language, that nothing wrote.  ADOPTED: `src/io/InternalStateWriter.cpp` writes `internalStates/<SECTOR>/<unit>/<kind>` as a PROJECTION of `SimulationResult::profiles` — no new physics, no new JSON block; the sector is the stamped `FlatUnit::sector`, a flat case grows no level; the kind is derived mechanically from the axis (`stage → stageProfile`, length axes → `axialProfile`, size axes → `sizeDistribution`, `componentIndex → swingTable`, anything else `profile` and announced).  THE BOUNDARY (Vítor, 2026-09-05): internal state is a field over a coordinate of the equipment or its inventory; a construction over a parameter sweep (van Heerden, Merkel) is an analysis and stays in the reports — so `T_K` profiles get no file and are announced, and the PSA/TSA swing table (loadings per component) IS internal state.  REJECTED: extending `check_design_sheet` (two unrelated subjects, two unrelated messages); a bare `**/internalStates/` ignore rule; filing `T_K` under `profile` with a comment; writing from inside the opt-in `ProfilesReport`.  Gate `check_internal_states`, four witnesses, five by-hand sabotages of the generated tree.  NOT DONE: tray hydraulics as its own item, batch adsorber transients, a declared kind for `module`/`chainLength`, any reader of the files |
| [`a-state-directory-is-a-restartable-snapshot.md`](../design/a-state-directory-is-a-restartable-snapshot.md) | ADR | yes | **SHIPPED 2026-09-06** — task #97, Vítor: *"em /0 deviam estar as streams e também o internalState... o OpenFOAM em 0/ tem ficheiros com as streams (condições fronteira) e com os dados internos"*.  In OpenFOAM one FIELD file carries `internalField` AND `boundaryField`, which is what makes a time directory a RESTARTABLE snapshot; Choupo's state views carried only the boundary half (one file per stream) while each unit's interior sat in the one-day-old top-level `internalStates/`.  ADOPTED: **in a state view a FILE is a stream and a DIRECTORY is a unit's interior** — `0/<SECTOR>/<unit>/<kind>` (AUTHORED) and `converged/<SECTOR>/<unit>/<kind>` (engine), `internalStates/` retired.  The substantive half is the SEED: `DistillationColumn.H` said *"Initial T-profile (linear between guesses)"* — the one solver aid in Choupo that was neither declarable nor announced, against the 2026-05-30 rule — so the column now READS a declared `stageProfile`, validates that it describes THIS column (stage count, component set, finiteness) and REFUSES by name otherwise, and ANNOUNCES both routes.  **A declared profile that does not satisfy the balances is a SEED, not an answer.**  `0/` stays TRACKED (a case may commit the state it starts from); the completeness contract still counts STREAMS and ignores directories; an ORPHAN unit directory and a kind NOBODY READS each refuse.  Witness `column16_declared_interior`: 108 outer iterations unseeded, 1 seeded, same answer.  REJECTED: a reader inside the retired view (state in two places); a second grammar for the declared half (the same `UnitProfile` travels both ways); validating a seed against the balances (that is a second, weaker solver); seeding silently on a mismatch; naming the kind vocabulary in the GUI (a fourth home — the case's own geography is passed to `caseTree` instead).  Gate `check_internal_states` (same name, changed claim), 12 arms, 10 by-hand sabotages of which 3 attack its own refusal arms.  NOT DONE: `iterations/` and the dynamic instants carry no interiors (different payload shape, and the instant derives its sector by SPLITTING the dotted name where the interior uses the stamp — two answers to one question side by side); no unit but the column reads one; nothing is written for a non-converged run.  **AMENDED THE SAME DAY (task #105, §9 of the record; a three-way reflection Vítor/ChatGPT/assistant, the second reading concurring, the third striking everything beyond it):** ONE FILE per unit, under its own root — `<view>/internalStates/<SECTOR>/<unit>`, one block per kind.  Measured: every unit publishes ONE kind, so every per-unit directory held one file (85 swept off disk); and a unit file BESIDE the streams would collide in silence with a homonymous stream.  IDENTITY IS (kind, sector, name), never name alone — the homonym is ANNOUNCED, not refused.  The reading: *the view shows the boundary directly; the interior is namespaced inside the same view.*  The resolvers were audited (D11): `StreamStateIO::readStateDir` FIXED to skip the subtree by name; `StreamOwnership::sectorOf` (split vs stamp) NAMED, not fixed; the GUI keep-list read the KIND and now reads the VIEW.  A MISFILED record (the retired shape) refuses instead of being skipped in silence.  NOT done: the symmetric `streams/` form.  The layout is CLOSED; what changes it next is a student, not a reflection.  **AMENDED 2026-09-07 (§10, task #133)** — Vítor: the interior of a unit that seeds itself is re-invented on every restart, so a state directory carrying only the streams is not the snapshot this record's own title claims.  Scope re-measured: 16 types assign an xAxis, 4 carry a `stage` axis, and the test is whether a solve READS one to START — for most publishers the interior is an OUTPUT and seeding it would seed a derivative.  `Extractor` was the one left, and it now reads its `stageProfile`, refuses by name a declaration that does not describe THIS cascade, and announces both routes; `choupo-init0` MATERIALISES the interior half, writing THE UNIT'S OWN seed (published through the same `seedCascade` the solve starts from — the file and the run cannot be two guesses) and nothing at all for a unit that reads none.  A DECLARED tree must be COMPLETE for the units that read one, as a check SEPARATE from the stream contract.  **The brief's unconditional form was REJECTED and the disagreement is recorded**: missing ⇒ fatal is a 31-case migration whose goldens all move, and it bans the honest announced auto-init the 2026-05-30 rule exists to permit — reserved for Vítor.  MEASURED against the brief: the absorber and the stripper are NOT seedless — both carry an outer T loop seeded at the mean feed temperature whenever the energy balance is active, which is all three of their corpus cases; left alone by decision, now for the true reason.  Witness `extract02_declared_interior` (1 sweep against 34); nothing moved on the undeclared path, proved per case by whole-output diff.  Gate arms (s)–(v), 6 sabotages, one of which SURVIVED: a source arm asking `"seedInterior" not in text` is satisfied by `seedInteriorXX` — *a name checked as a substring is not checked* |
| [`the-errors-a-student-makes.md`](../design/the-errors-a-student-makes.md) | ADR | yes | **SHIPPED 2026-09-06** — tasks #62–#66, the five open items of the 2026-09-04 seven-way audit, taken as ONE slice because they share a property no engine slice has: they are what a STUDENT meets first — the engine or the guide failing a reader who forgot a key, mistyped one, copied an example out of the manual, or believed a tutorial header.  ADOPTED: **a default must never name what the factory cannot build** (`SpiralWoundModule`'s `massTransfer {}` defaulted to `constant`, pruned from the factory on 2026-08-23, so a block that merely forgot its `model` key died naming a model the ENGINE invented — it now refuses and names both routes); the guides stop teaching `phase` where the parser reads `referenceState` (reproduced: `phase solid;` on sucrose moves the cooling duty 271.7 → 264.5 kW and collapses the heat of crystallisation to 0, at exit 0, in silence), and the two WRITERS that were sowing that key — `import_gibbs_nasa.py` and `EstimateComponent` — are fixed with the ten records that carried it; the Developer Guide's component-record example, wrong in six ways at once, becomes the real grammar; six tutorial headers are corrected to what their own runs say.  REJECTED: re-adding the pruned `constant` model (reversing a decision taken on good grounds); making an unread key inside a component record REFUSE (that is the reserved refuse-or-announce contract decision, and this slice must not pre-empt it); wiring the `thermoPhysPropDict` unread-key audit (MEASURED clean — 27 findings, 3 classes, no false positives — but it needs a grammar ruling on `root`/`fugacityRoute`/`apparentComponents` first); auditing the unit-level dict by read-tracking (MEASURED and rejected on the evidence: 806 findings, 723 of them `outputs`); rewriting `membrane01`'s flux header (the header may be right and the model wrong).  Gate `check_model_default_registered` (NEW, flagged for a ruling under September's no-new-gates rule), 4 sabotages.  NOT DONE: the `dGf_298` key 58 records declare and nothing reads; refusing a mistyped model name in the four if-chain units (`HeatExchanger`, `Crystalliser`, `SprayDryer`, `BatchStill`), where it still falls into the default silently |
| [`a-default-nobody-declared.md`](../design/a-default-nobody-declared.md) | ADR | yes | **SHIPPED 2026-09-06** — task #92, the SAFE half.  A student defending a wall thickness could not tell what they DECLARED from what the engine ASSUMED: `L_over_D 3;` read identically on the specification sheet either way, and `corrosionAllow` / `jointEfficiency` decided `t_wall` and appeared on no page at all.  Measured first: 32 `lookupScalarOrDefault` sites in `src/postProcessing/`; `CostingPass` printed `Year:`/`CEPCI:` by RE-READING the dict with its OWN defaults while `Guthrie` read the same keys with ITS own and did the pricing — the literals agreed, so the banner was ACCIDENTALLY true — and `Guthrie.H` carried the same four a THIRD time as dead in-class initialisers.  ADOPTED: a default that is USED is announced (site + `AdvisoryLog` + caveat block + result JSON, the CatalystPellet posture), a DECLARED value announces nothing so silence keeps meaning "nothing was assumed", the specification sheet grows an `assumed ( ... )` list written only when something WAS assumed, and the header DRAWS `CostingModel::pricingFactors()` — the 2026-09-05 first-law rule, with no fallback computation when a model publishes none.  13 of the 32 sites wired.  REJECTED, each with its reason: reconciling L/D 2.5 (StirredTank) with 3.0 (VesselSize) — same key, same question, different silent answer — which is a CURATION decision RESERVED for Vítor and moves every un-declaring vessel's D, H, t_wall, weight and cost, so the one home carries BOTH and records that they disagree; writing the assumed VALUES into the sheet (one number twice in one file); a `defaults {}` postDict block (makes the assumption easier to make and no easier to see); a conditional announcement; a new gate script.  STOPPED AT THE MEASUREMENT: the 14 remaining `EconomicsPass` defaults are a campaign and announcing all of them is the wall of text the caveat block exists to prevent — what an appraisal should do with undeclared financial assumptions is not decided here; `refuseOnMissingPrice` stays a NUMBER (6 postDicts, 2 READMEs and the Tutorials Guide document `0`/`1`) and only gains a voice.  PROVED, not asserted: 8 of the 9 sizing cases byte-identical on stdout and on every artefact; `ammonia02_full_plant` — the ONLY shipped case that takes any of these defaults, on a 220 bar separator — differs by ADDITIONS alone.  Gate `check_cost_provenance` +6 arms, 8 by-hand sabotages under the journal, none survived; arm (k) is a SOURCE arm because no output can separate a re-read from a draw when both find the same number.  FINDING reported not fixed: the banner prints `Method: Guthrie` on eight cases that all declare `method Turton;` |
| [`a-dryer-that-ended-wetter-than-it-started.md`](../design/a-dryer-that-ended-wetter-than-it-started.md) | ADR | yes | **SHIPPED 2026-09-04** — Vítor opened the flagship sugar plant on the live site and the mass-balance plot showed 22032 kg/h in against 24980 out, then asked the right question: *a case with a balance violation has to fail, doesn't it?*  It did not and could not.  **I answered wrongly first**, reasoning from the plant's non-sweep twin (which closes) to the case in front of me, and told him the GUI was double-counting boundary aliases; the boundary-edge sum alone reproduces his screen to the kilogram, so the GUI was right and the plant leaked.  ONE UNIT: `DRYING.SD` took 685 kg/h of water in and put 3633 out — closure 122.4573 % against +-0.02 kg/h on the other ten — because the residual-moisture model is a RATIO carried by the dry solid (`X_final * m_solid`) that knows nothing about how much water the feed brought, and the only clamp protected the VAPOUR from going negative while the surplus left as powder moisture.  Fix: a dryer cannot end wetter than it started, ANNOUNCED rather than silently clamped (the extrapolated-Antoine posture) — 122.46 % → 100.0000 %.  **The dryer is one bug; the substance is three silences**: under an outerDict driver the declared balance reports do not run (now announced by name), the case ships no golden, and nothing in bin/runTests looked at closure at all — a stable wrong answer passes a golden by construction.  New gate `check_mass_closure` (206 steady cases), which caught two defects in ITSELF first: a discovery that grepped the file for `choupoSolve` and swept in a choupoBatch and a choupoCtrl case mentioning it in a comment, and a first version that collapsed four states into two and ACCUSED THREE HONEST CASES (a closed Rankine cycle has no material boundary; two userOps tutorials do not run without a user-compiled type) — a gate that accuses the innocent teaches the reader to ignore it.  3 sabotages.  DECLARED LIMITATION: no corpus case at its shipped feed triggers the dryer bug, so with the fix reverted the gate PASSES; what catches it is the conjunction of gate + announcement + the report actually running.  RESERVED for Vítor: whether a sweep should report on its final point.  NOT CHECKED: energy closure, per-unit closure (two units can cancel) |
| [`what-a-passing-suite-does-not-say.md`](../design/what-a-passing-suite-does-not-say.md) | ADR | yes | **SHIPPED 2026-09-08** — Vítor: *«Como foi possível teres andado a dizer que a suite corria bem com violações graves de balanços energéticos?!»*  The answer is structural, not "too few tests": **conservation of energy had NO gate that refused.**  A golden pins what a run PRINTS; `check_energy_boundary_pinned` requires the residual PUBLISHED and PINNED, never SMALL, so a 16 % violation satisfies it for ever; the per-unit alarm returns exit 0; and `energy-T1/T2:plant` held the RIGHT rule (< 1 %) on ONE case — the same shape as `check_element_balance`, which asserts atom closure on one case.  `check_mass_closure` had NAMED the energy gap in its own "WHAT THIS DOES NOT CHECK" four days earlier: **an absence that is named and not scheduled is an absence.**  RULE: **a suite's PASS covers what its gates ASSERT and nothing else** — name the gate that would have failed, or the PASS says nothing.  New gate `check_energy_closure` (T2's rule over every steady case; residual READ from the engine, MEASURED pin list via `--seed`, RATCHETED both ways), wired into both tiers: **32 of the 89 cases with a global first law do not satisfy it** — esterification2sector 170.98 %, gibbs*_wgs_cooled 150.7 %, cavett01 88.02 %, column01 24.68 %, ammonia02 16.14 %.  THE PHYSICS IS ONE FAMILY: **a unit solves its energy equation on one enthalpy surface while its streams are priced on another** — ε-NTU on `cpIdealGas` against SRK streams (and it copies `vf` across a dew point, charging zero latent heat), the ADIABATIC `gibbsReactor` on `h_pure_ig` while its own ISOTHERMAL mode uses `H_stream_formation`, and `column01` losing 631.96 kW.  THREE DEFECTS INTRODUCED BY THE FIX ITSELF, and they are the durable half: **the numerical FLOOR is not a scale** (a real −6162.5 kW residual published as −6.16e14 %; absent a scale the kW stands and the ratio is UNAVAILABLE), **a comment describing an arithmetic the code does not do** (duties summed ALGEBRAICALLY beside a sentence promising MAGNITUDES — a column boiling 1279 kW and condensing 1281 kW recorded as exchanging 1.74 kW), and **a basis changed without finding who reads it** (0.163 % → 3.17 % on an unchanged residual, turning `energy-T2:plant` red unnoticed).  NOT FIXED: all the physics; only the exchanger MEASURES its own gap (`H_closure_gap_kW`).  Sabotage status is recorded IN the gate's docstring rather than assumed |
| [`three-silences-at-exit-zero.md`](../design/three-silences-at-exit-zero.md) | ADR | yes | **SHIPPED 2026-09-07** — tasks #127/#128/#121, three defects with one rule: the engine promises to refuse or announce, and in these three places it did neither.  Two were found by transposing ONE CHARACTER in a corpus case.  (1) Four unit ops read `model` through an if-chain whose ELSE IS A CATCH-ALL, so `MSMRP` ran the equilibrium crystalliser (yield 0.395776 against the population balance's 0.277787, twelve PBE KPIs gone) and `langrishKocke` ran the lumped spray dryer with EVERY KPI BYTE-IDENTICAL — visible only in a profile.csv that changed shape, so **no golden could ever see it**.  A fifth site (a side draw's `phase`) was found by scan, RUN to confirm, and fixed: `vapou` drew liquid where vapour moves the reboiler duty 10 %.  All five refuse through `registryRefusal::message`, which gained one parameter (the list LABEL) so an if-chain's enumeration reads `Accepted:` and the sentence keeps one home.  (2) `choupoSolve`'s only `printAdvisorySummary` call sat inside the `else` of `if (outerDict)`, so 23 corpus cases printed neither the caveat block nor the divergence banner — the exact state `AdvisorySummary.H` forbids in its own words.  DECIDED AND STATED: the block reports ONE NAMED PASS (the representative one where the driver has it, otherwise the LAST), never the union — the header's own *THE PATH IS NOT THE ANSWER* and *a PARTITION, NOT A DEDUPLICATION* both rule the union out.  CLAUDE.md's claim that problemDivergence is written *ALWAYS* was FALSE under an outerDict and is corrected.  (3) A `chemistryDict` solid phase no record owns gave saturation 0 and yield 0.999999999868 against 0.18832, at exit 0, seal verified.  MEASURED BEFORE REFUSING: two corpus cases legitimately declare an owned phase with NO calorimetric anchor (`calcite` on CaCO3, a mass-action Ksp), so the test is OWNERSHIP, not completeness — an owned phase with no anchor is ANNOUNCED instead.  Gates EXTENDED not multiplied: `check_model_default_registered` arm (d) closes the gap it named in its own blind-spot list, `check_caveat_surface` arm (e) RUNS an outer-driver case (its arm (d) is a source check and cannot see an enclosing `if`), `check_v2_refusals` gains the solid-phase arm.  9 by-hand sabotages, two of which found defects in the work: a failure message quoting one site's fact for all five, and a probe that could not reach exit 0 and so could not tell a silent acceptance from an unrelated refusal.  RESERVED for Vítor: whether a sweep writes `converged/` (task #77).  NOT COVERED: two further if-chains (`HeatExchanger`, `BatchStill`), and the anchor announcement has no corpus witness (said plainly) |
| [`a-size-distribution-with-no-size-axis.md`](../design/a-size-distribution-with-no-size-axis.md) | ADR | yes | **SHIPPED 2026-09-07** — tasks #124/#126, two defects with one rule: every surface a student reads must be true of what the engine does.  (1) `Crystalliser.cpp` built its size grid `Lcol` at both profile sites, declared `xAxis L_micron`, and never assigned it into `columns` — so a crystal-size distribution was published without the size it is distributed over in ALL FOUR surfaces at once (result JSON, `profile.csv`, the spreadsheet, the GUI plot, each resolving the axis through `columns[xAxis]`), and the interior file it wrote was one the engine's own reader refuses: copying `converged/internalStates/cryst` into `0/` gave exit 2.  Measured: 5 of 87 interior records unreadable, ALL crystallisers; 21 source `xAxis` sites, the crystalliser the only offender (`SpiralWoundModule` and `PSA` subscript `columns` through a reference, which is the same act).  ADOPTED: the unit stores the axis it declares; and the WRITER REFUSES a profile whose declared axis is absent — at the seam that already partitions the profiles, to stderr and to `AdvisoryLog`, and deliberately WITHOUT throwing, because the call site has ruled a writer failure said-and-never-fatal and a throw in that loop takes every OTHER unit's interior with it (the 2026-09-04 partial-tree lesson).  What settled the refusal: nothing a CASE declares can reach it — only a unit's own C++ can publish an axis it does not carry — so it is a contract on unit authors and costs a student nothing.  SWEEP REPORTED, as the brief required: after the fix the refusal would catch NOTHING else in the corpus.  (2) Every interior file told its reader to COPY it into `0/internalStates/`; `distillationColumn` is the only type that reads one, so the instruction was fatal for every other — reproduced on the flagship's spray dryer, exit 2, and BOTH units that publish an interior in that plant are in the group.  Same defect shape as d4e173fb0 (a pop-out telling every reader to edit a file the run rewrites).  ADOPTED: the sentence is CONDITIONED on the unit; the fact is asked of the CLASS (`UnitOperation::interiorKindsRead`, one home, `Flowsheet`'s hand-rolled `New`-in-try/catch routed through it) and handed to the writer as DATA, because `io` must not include `unitOperations` and `applications/` is where the planes meet.  DOCS: the retired one-directory-per-unit shape removed from `docs/ai/stream-state.md` and `docs/ai/patterns.md` (which also still carried the PRODUCER ownership rule replaced the day before by 8816c47c3), plus three further homes of that rule found on the way (`CHOUPO-CONSTITUTION.md`, `SimulationResult.H`, `choupoSolve/main.cpp`).  CHECKED RATHER THAN ASSUMED: `docs/ai/stream-state.md` was NOT stale on ownership — 8816c47c3 had already given it the lowest-common-ancestor rule, the MAIN amendment and the sector/crossing columns.  MEASURED FALSE and corrected in three homes: *every view repeats the geography* — `design/` has three levels to the state views' four (MAIN's only unit is a splitter, which realises no sized item) and `iterations/` repeats it INSIDE each instant; no `design/MAIN/` was invented.  REJECTED: throwing from the writer; making all four consumers refuse (four homes for one rule — the writer sees every published profile on every converged run); naming `distillationColumn` in the writer or tabulating type→kinds in the gate (the gate DERIVES it from the `readsInteriorKinds()` overrides joined to `registerBuiltins()`); regenerating the round-trip witness whole (only its banner was spliced, numbers unmoved).  Gate `check_internal_states` EXTENDED with arms (o)(p)(q)(r) and a docs half of (k) — its arm (c) compared the written block against the JSON and both were missing the axis EQUALLY, which is why it passed every day of its life.  7 by-hand sabotages, **one SURVIVED**: deleting one of the crystalliser's two axis assignments left arm (o1)'s presence test green, so the arm now COUNTS sites (corpus margin: stage 3:2, V 4:2, T_K 2:1).  The sabotages also found the gate reading STALE gitignored `converged/` trees as today's engine; (o2) and (q) now read only what the run produced plus what git tracks.  NOT DONE: no golden moved and none could — no golden row kind reads a profile column, so a published profile column is pinned by nothing, and closing that would be a new row kind; `T_K` profiles stay outside the writer's guard (it declines them first); the GUI comments naming the retired ownership rule; `bin/runTests` still reads this gate's claim with `tail -1` rather than `gate_claim.py` |
| [`a-sector-recovered-by-splitting-a-name.md`](../design/a-sector-recovered-by-splitting-a-name.md) | ADR | yes | **SHIPPED 2026-09-06** — task #106, finishing the 2026-09-04 hierarchy slice on the STREAM side, plus an addition Vítor asked for by being confused.  That slice converted the four design/costing readers and left TWO copies of the rejected design alive where it matters more than a table heading: `StreamOwnership::sectorOf` decides WHICH SECTOR FOLDER a stream's state file lives in and `SolutionWriter::sectorOf` buckets the per-sector `converged/` views — both splitting the FIRST dot of the unit NAME, which is why arm (d), written for a LAST-dot substring, could not see either.  ADOPTED: **`core/FlatUnit.H::topLevelSector` is the ONE home for reading a stamped chain's head**, `ownershipPath` takes a `SectorOfUnit` map, `runInit0` supplies it from the flattened dicts' own `sector` key.  Reading the CHAIN is not the banned act; guessing a fact out of a name that never promised to carry it is.  Measured: 60 units / 32 dotted / **0 disagreements**, output byte-identical across 12 sectored cases, `choupo-init0 --force` resolving 25 of 25 streams to their existing files.  PART B: `streamTable.csv` gains `sector` and `crossing` (`FROM->TO`) on a sectored case — the flagship's three crossings on one screen, answering *why does `0/MAIN/` hold three streams* (a stream lives with its PRODUCER, which is what makes drill-in work).  A bare report label is resolved through the DECLARED alias bridge, never by matching its tail; a stream the topology does not know gets an EMPTY cell, not `(no sector)`.  **Empty is not a sector called root**: a flat case gets NEITHER column, verified against a build with part B reverted — 2 files differ out of 36 cases, both of them the sectored ones.  REJECTED: leaving the split (silent failure, `converged/` blast radius); putting the whole chain in the path (moves a stream file, ratified 2026-07-06); a `crossings.csv` of its own (a second home for the topology).  Gate: `check_sector_hierarchy` arm (g) — two ROLES (reads the head vs supplies the stamp; requiring the call of the supplier failed the gate on correct code), a PROBE that must still be flagged, 4 by-hand sabotages of which **S1 survived its first form** (a `topLevelSector` named only in a BLOCK comment satisfied the "calls it" half).  NOT done and named: `gui/src/ui/LogWorkspace.tsx` still splits the qualified name it parsed out of the run LOG — it genuinely has only a name there — and nesting deeper than one level stays unexercised |
| [`one-row-per-physical-stream.md`](../design/one-row-per-physical-stream.md) | ADR | yes | **SHIPPED 2026-09-07** — task #145, found by Vítor on the LIVE SITE as Streams rows with no PFD number.  The blank number was a consequence: the flagship's `streamTable.csv` drew **52 rows for 25 physical streams**, because `result.streams` is keyed by every NAME a stream answers to — qualified identity, the bare sector label the relabel pass mints, the plant's own boundary label — and the table emitted a row per KEY.  MEASURED: 15 of the 52 rows carried no number, every one a bare sector label no view's `connections {}` block mentions, so it belongs to no numbering class: *the numbering was right, the row list was wrong* (nothing in `streamNumbering.ts` was touched).  ADOPTED: **the ROW is the QUALIFIED IDENTITY, the plant label is a COLUMN** — measured over the 21 duplicated classes, the identity is present for 21 and collides for 0, a plant label is absent on 12, and the bare label COLLIDES (`DRYING.Vapour` vs `FERMENTATION.Vapour`, the name-identity crossing settled against on 2026-09-06).  The row set is then EXACTLY `choupoSolve --manifest`'s keys, so the table and the `0/` tree cannot disagree about how many streams a plant has — the state model had counted 25 correctly all along.  ONE new fact: `boundaryAliasOf` already closed all 27 alias names but cannot say which is the PLANT'S own (9 of 27; the other 18 name no boundary), hence `boundaryOutletLabelOf` (identity → declared outlet name).  THE ENGINE DECIDES, THE GUI DRAWS: the JSON marks each entry (`aliasOf` / `boundaryLabel`) and the adapter drops label entries at ONE seam, so eight surfaces that tabulate or sum streams count each pipe once with no filter of their own; the dropped names become an alias MAP `findRunStream` consults FIRST — its leaf-match fallback would have sent `EthanolVapour` to whichever `*.Vapour` came first, and there are two.  MEASURED: 52 → 25 rows, all 25 numbered (`gui/tests/streamNumbering.test.ts`, against the real dicts); `phases.csv` 16 → 7; **78 of 82 corpus cases byte-identical across their WHOLE run-output tree** against a build of the previous commit, the 2 that differ being the 2 sectored plants.  Traps: a renamed product's ROLE lives on the LABEL in `reporting::Topology` (right for the balance reports, so nothing moved there), so the identity had to learn that a declared outlet is a product or nine products would have read `intermediate`; and the gate's first cut shadowed arm (a)'s `declared` SECTOR list, PASSING while its own OK line called the nine outlets the plant's four sectors.  NOT DONE and named: `Topology`'s products set still holds the boundary NAME, so one stream is `DRYING.ExhaustClean` in the stream table and `Stack` in the balance reports (the `label` column is the join); the GUI's own role inference stays a second home; and the CYCLONE recovering EXACTLY zero dust — `RecoveredDust`, a declared boundary outlet, is an empty product — is a separate finding RESERVED for Vítor.  Gate: `check_sector_hierarchy` arm (l), 6 by-hand sabotages between the run and the check |
| [`a-stream-belongs-to-the-graph-that-contains-both-ends.md`](../design/a-stream-belongs-to-the-graph-that-contains-both-ends.md) | ADR | yes | **SHIPPED 2026-09-07** — a REPLACEMENT of the ownership rule ratified 2026-07-06, not a correction of it: the old rule did what it said, and what it said picks ONE ENDPOINT OF AN EDGE and calls it the owner.  THE ARGUMENT AND THE RULE ARE VÍTOR'S, raised after he could not find the inter-sector streams of his own flagship: *a unit is a NODE and belongs to a sector; a stream is an EDGE, and an edge between two subgraphs belongs to neither — it belongs to the graph that contains both.*  ADOPTED, one sentence replacing four and recursive at any depth: **a stream's state file lives at the LOWEST LEVEL of the case whose subtree contains EVERY ENDPOINT of that stream.**  THE ARCHITECT'S AMENDMENT, marked as one because it changes what his text said: **`MAIN/` is the DOMAIN'S OWN LEVEL, not a sector like the others** — plant-level units live there (2026-09-05, unchanged) and plant-level streams now do too, so `MAIN` keeps ONE meaning; REJECTED with it, the crossing streams loose at the view ROOT, which is the mixed level 2026-09-05 was adopted to remove and which `sugarPlantEconomicsSweep` was still in.  MEASURED BEFORE ANYTHING MOVED: 24 files move across 6 sectored cases (the flagship 4 of 25), every flat case untouched, and **byte-identical stdout on all 12 cases run before and after** plus every artefact — paths moved, numbers did not; the one output-content change is the `streamTable.csv` `sector` COLUMN, which was a RESTATEMENT of the rule under a comment claiming the two could not disagree, and disagreed the moment the rule changed.  M2, the instability, measured rather than argued: 51 of 78 streams are ONE cross-sector edge from rising, which is inherent and NOT a veto because the engine ANNOUNCES the move by name and both addresses and refuses until it is made, because the edit that triggers it is the edit that changes what the stream IS, and because the retired rule had the same instability more quietly.  A PLANT OUTLET DOES NOT RISE, and the asymmetry was measured: read symmetrically, `DRYING.Vapour` and `FERMENTATION.Vapour` both resolve to `MAIN/Vapour`, one file for two streams, which the manifest refuses by construction — an outlet is the plant's LABEL for a stream the sector owns, and a label never gets a file.  RIDING WITH IT: `bin/choupo-drill`, already broken on 2 of the flagship's 4 sectors (it looked the parent's state up by the CONSUMING PORT's basename, and DRYING worked by a coincidence of naming), now asks `choupoSolve --manifest` for both manifests and joins on IDENTITY — all five drilled sectors solve, and a crossing in the parent is a BOUNDARY in the child at the CHILD's own level.  NEW READ-ONLY MODE `choupoSolve --manifest`, published before the 0/ completeness check so a tool can ask where a file goes while the tree is still incomplete.  Gates EXTENDED not multiplied: `check_sector_hierarchy` arms (h)-(k) + a third role in arm (g); `check_drill_in` gains CONCENTRATION and FERMENTATION (*a gate whose only witness satisfies the rule by accident is a gate nothing tests*).  7 by-hand sabotages, one of them a source sabotage run under `destructive_session.py`'s journal; two produced the same coarse line and it is recorded verbatim, because it says something true — on a migrated corpus the ENGINE refuses a misplaced file before any arm can speak, so (h)/(i) catch a moved RULE, not a moved FILE.  NOT DONE: nesting deeper than one level (unexercised — the ownership rule reads the chain WHOLE while the converged/ views and design sheets keep its HEAD, and no case makes them differ); the GUI, where three comments still describe the retired rule |
| [`what-a-citation-requirement-found.md`](../design/what-a-citation-requirement-found.md) | ADR | yes | **SHIPPED 2026-08-28/29** — the owner asked that every EduTool symbol be defined; measured at **133 symbol uses across sixteen lessons where the LETTER never appeared outside a formula**.  Writing 133 definitions is the WRONG response — *a missing definition is visible and a wrong one is not* — so the constraint came first: every symbol needs a `file:line` citation into the ENGINE, verified mechanically, eleven agents, none allowed to write a file.  **The requirement changed what the readers read.**  A definition written against a page paraphrases the page; one that must carry a line number forces somebody to open the unit that computes the answer, and then they notice the page above it is wrong.  Eight defects, two of them in files this session had already read that day: `merkelLesson` said "L/G … is the slope of the operating line" while its OWN formula two lines below already carried `cp_L`; `CoolingTower.H` declared an "arithmetic mean of the relevant inlet/outlet temperatures" the code does not implement (and the real rule hid a defensible decision AND an honest limitation); and the Bjerrum page claimed **"there is no pH input anywhere in Choupo"** — false — in six places, one of them a green test asserting it, *a test over a false claim being worse than no test because it converts an error into a maintained invariant*.  Three prose drifts against the golden, and a fourth reported drift that was NOT one, recorded as such.  FUG got the same treatment (three absences returning as numbers), and its `feed_stage 0.0` branch is pinned as LATENT rather than credited with coverage.  NOT fixed at the time, named: the pump and the pipe used liquid densities 12 % apart on one diagram, so heads could not be compared across the operating point (**CLOSED 2026-08-29**: the package now anchors Rackett on the record's own declared `Vliq`, both sides read 996.95 kg/m3 on the witness, and heads may be compared -- this row and the EduTool page both went on saying otherwise until the 2026-08-31 audit, which is the same stale-absence class the audit was looking for); and the distillate is published at its own DEW point while `vf = 0` calls it a liquid (measured 0.58 K on column01) — both reserved, both moving published answers |
| [`the-deliverable.md`](../design/the-deliverable.md) | ADR | yes | **SHIPPED 2026-08-27** — leg 5 of the student walkthrough: after "where did this number come from?" comes "can I get it out?"  Sizing and costing printed their tables and left NO file, so the equipment list and the capital cost lived only in terminal scrollback.  Measured first: of twelve registered reports, **`design` and `economics` are declared by ZERO cases** (against 74 for `streamTable`) — the two a final-year project needs most are the two nobody had run, which is why the defects survived.  Three found: both reports RE-RAN their pipeline pass out of their own dict, so the equipment list and the cost basis each needed TWO homes (they serialise `result` now, and refuse naming the remedy when it is empty); one failing report killed every report after it in silence, "never reached" reading as "produced nothing"; and the CSVs carried bare numbers, so leg 4's traceability stopped at the screen — `sizing.csv` now carries the design BASIS and `costs.csv` reproduces its own total from its own columns (166,652.66 both ways).  REJECTED: making `design`/`economics` default reports (a corpus-wide behaviour change, reserved).  NOT done and named: the twelve report kinds are still undiscoverable from a run's output, and `kind;` still refuses saying what is wrong and not what is right |
| [`held-out-pressure.md`](../design/held-out-pressure.md) | ADR | yes | **SHIPPED 2026-08-25** — a bubble point is (x, P, T) and `fitParameters(T_bubble)` read one scalar `residual.P`, so a study measuring three isobars could only be fitted by discarding two.  `EvidencePoint` gained an optional third coordinate (quiet NaN when undeclared, never zero); the reader takes BOTH transcription forms — a `Pressure` COLUMN (ThermoML Variable) and a held-constant SCALAR (Constraint) — because the two halves of one partition must be priced alike.  Four named refusals, the instructive one being that a held-constant with no unit is read as pascals and CONVERGES, so the gate demands the named refusal rather than any failure.  Witness `fitNRTL02_thermoml_isobars` returns a finding that contradicts its own first draft: an excellent in-sample fit (reduced χ² 0.023) predicts 101.3 kPa 4.7× WORSE than the catalogue pair it started from, and 28 K of span buys two orders of magnitude of conditioning without buying identifiability.  Carries `reviewStatus` on a dataset (a citation says where numbers are supposed to come from, not that anybody looked) — **and §5a records the check being performed the same day**: Vítor supplied both articles and all 45 fit and 21 held-out points were read back digit for digit, so both datasets declare `checked`; the flip produced two rules, that `checked` must not be silent (else it is indistinguishable from an undeclared status) and that the now-unexercised unchecked branch needs a built probe and the 102-identical-paragraphs fix — *a guard scoped to an object the caller recreates in a loop guards nothing*.  Names the bubble-point Newton's unframed initial guess as NOT closed |
| [`thermoml-archive-assessment.md`](../design/thermoml-archive-assessment.md) | STUDY | no | **EVIDENCE 2026-08-25, decision reserved to Vítor** — the NIST/TRC ThermoML Archive's terms read from the source with verbatim quotes: the formal repository record grants the NIST open licence and types the archive non-SRD (`PublicDataResource`, `accessLevel: public`), the licence itself disclaims third-party IP rights in capitals, and the TRC page says the files are posted "with permission of the journal publishers" (ACS, Elsevier, Springer named via their five journals).  Three tiers: bibliographic index YES; point values cited to the original article YES (the constitution's own primary-source rule); bulk import into `data/standards/` reserved to Vítor with counsel.  Corrects the session's earlier "no downstream grant exists" reading.  Decides nothing by itself |
| [`advisory-attribution.md`](../design/advisory-attribution.md) | ADR | yes | **SHIPPED 2026-08-24** — an advisory carries WHICH STATE it is about (`status` accepted|trial, `where` = the innermost open `AdvisoryFrame`), both stamped by the SINK and not by the 111 sites that raise them; the default is `accepted`, so 326 of 365 corpus cases are untouched and none lost a caveat about its answer.  Closes F2 of the reading benchmark: `stripper02` printed 385 advisories and `column13` 107, nearly all describing states their solvers visited and discarded.  Frame the search WHERE THE SEARCH IS — one frame at `ReactiveVLE`'s outer Newton serves the column, the flash and the energy report alike.  Carries a data-race fix (the sink is written from `newtonND`'s parallel Jacobian) and TWO measured negatives: `walk.close()` in `ReactiveVLE` is today INERT, and sabotage 1 SURVIVED.  Deduplicating by message similarity, and summarising each search's numeric range by parsing its own sentences, are the recorded rejected alternatives |
| [`llm-authoring-benchmark-2026-08-23.md`](../design/llm-authoring-benchmark-2026-08-23.md) | EVIDENCE | no | **Five fresh agents, the llmctx kit alone, 2026-08-23** — the constitution's LLM-assembles/student-reads bet, measured: molecular surfaces converge in 1–2 rounds (no rollback indicated), the electrolyte authoring surface took 8 (three required keys in no document, the kit's own example refused by the engine, a missing standards record) and earns the make-it-one-thing-again treatment rather than more documentation.  Two spurious ENERGY BALANCE FAILED banners on correct cases and one silent phase-mislabel doc defect (hit twice independently) head the ranked fix list.  Decides nothing by itself: it is the evidence the fixes and the one grammar pass stand on |
| [`edutools-pareto-evidence.md`](../design/edutools-pareto-evidence.md) | EVIDENCE | no | **Search-backed curriculum evidence for the tool Pareto, 2026-08-19.**  Corrects a survey that ranked tools from memory while admitting it in its own SS9.  ITS FIRST SECTION IS ITS OWN LIMIT: the egress policy permits GitHub alone, so no catalogue or syllabus could be FETCHED; every line is marked [S] search summary / [T] tool summary / [U] URL only / [X] unreachable, and none claims a page anyone opened.  Contradicts the survey where it must: frequency-response control is the HIGHEST-recurrence item found and was filed 'Recommended OUT' on an engine argument welded to a curricular verdict, while control is the only core block with zero tools; MSMPR is over-ranked (no undergraduate syllabus); RTD is under-ranked and the engine has nothing.  Not an ADR: it decides nothing, it supplies the evidence a ranking should have had |
| [`ice-as-a-solid-phase-of-the-solvent.md`](../design/ice-as-a-solid-phase-of-the-solvent.md) | ADR | yes | **DESIGN DECIDED 2026-08-06** — one condition (mu equality), not six solvers; ice falls out of `gStd(T)` as an interface rather than a seventh special case.  Its own first draft is recorded as the rejected alternative |
| [`reference-rung-refusal.md`](../design/reference-rung-refusal.md) | ADR | yes | **DECIDED + IMPLEMENTED 2026-08-06** — `h_pure_ig`/`s_pure_ig`/`g_pure_ig` refuse a non-gas `referenceState`; revised its own plan on the first measurement (the field was already parsed), and names the water-liquid-datum gap as NOT closed |
| [`which-result-blocks-a-golden-can-read.md`](../design/which-result-blocks-a-golden-can-read.md) | ADR | yes | **FINDING + RULE, EXECUTED 2026-08-12** — a top-level result block the golden format cannot read arrives UNPINNED, silently, with no failure anywhere; three were found in one session (`validation`, `energyClosures`, `utilityAllocation` — the last on 77 cases) and closed with the `aad` / `closure` / `utility` kinds and a gate each (published ⇒ pinned AND pinned ⇒ published).  The rule is the Edwards lesson one layer up: add the kind in the same commit as the block.  §4 audits what is deliberately NOT pinned (residual histories, curves, words) with the reason for each.  Rejects a gate on the rule itself — a curve and a decision look identical to a script — and rejects regenerating goldens to add rows, which would silently rewrite every other row in 67 files |
| [`origin-vocabulary-conflict.md`](../design/origin-vocabulary-conflict.md) | ADR | yes | **FINDING RECORDED 2026-08-12, nothing executed** — `src/core/Origin.H` silently folds five of the eleven catalogue records declaring an `origin` word onto `unattributed` ("no provenance declared"), including fluorine's IUPAC-`definition` ΔHf° = 0, and folds `measured` into `literature` against the five-axis record ratified above.  The fold is the deliberate typo guard, which is the defect: an unknown word and a curator's word are indistinguishable.  Proposes a DELEGATE-WITH-DEFAULT — add `definition` (a category the enum lacks), `standard`→`literature`, `asserted`→`placeholder`, keep the `measured` fold as a STATED approximation, and refuse an unknown word by name.  Rejects a silent mechanical remap (it would lose the one fact `definition` carries) |
| [`vapour-or-gas-is-a-state.md`](../design/vapour-or-gas-is-a-state.md) | ADR | yes | **FINDING RECORDED 2026-08-06, implementation NOT started** — `noncondensable true;` stores a STATE relation (T vs Tc) as a substance flag; CO2's Tc is 31.0 °C and nine corpus cases run it at 298.15 K, six kelvin BELOW, while the engine announces "above Tc".  Ruled: announce the contradiction, as `role` vs `volatility{}` already does — never delete the flag, never silently re-route |
| [`edutools-curriculum-survey.md`](../design/edutools-curriculum-survey.md) | STUDY | no | **SURVEY 2026-08-18** — 26 classical constructions from the canonical ChemE texts, each classified by what the ENGINE can compute: 10 need ZERO engine work (g_mix common tangent, ternary tie-triangle/Hunter-Nash, MSMPR CSD, cyclone grade efficiency, Moody, Arrhenius, Langmuir linearisation, compressibility, per-stage flood approach, Ergun profile), 11 need a NAMED engine slice (Thiele/eta, packed-column dP and flooding, Fair capacity, Ponchon-Savarit, HTU-NTU, RTD, Rankine, u_mf, filtration, thickener, residue curves), 5 recommended OUT.  Top-ranked item is NOT a tool: the four reactors reading `catalystLoading` treat the pellet as a POINT with eta implicitly 1 and nothing saying so — announcing it moves no golden and removes a silent factor.  Thiele is IN with the non-isothermal pellet OUT of a first slice (three eta per phi; a Newton finds whichever the guess is nearest — the failure mode this project refuses — and Van Heerden already carries the multiplicity lesson one scale up).  Packings are a bigger B than Thiele and the blocker is DATA and CITATION, not physics; `TrayHydraulics.H` already ruled twice: a cited equation, never a digitised chart.  Mechanical finding that upgrades several: a chart is a family of curves and an `outerDict` GRID SWEEP already writes one, so a chart witness needs no new machinery.  Decides nothing by itself |
| [`one-tab-one-thing.md`](../design/one-tab-one-thing.md) | ADR | yes | **DECIDED 2026-08-17** (Vítor) — the hub tab stays open; a case, Explore and the EduTools each open a NEW tab, so the mode/view distinction becomes STRUCTURAL instead of a rendering rule inside one strip, and a case tab's menu carries only its own views.  Makes the credo's "the address bar is a shareable bookmark" true and testable.  AMENDED same day (Vítor): the tools are INDEPENDENT of the flowsheet and do not communicate with it — the `Classroom | Current run` coupling goes, a tool is fed by its own sealed witness and nothing else, and a teaching instrument whose answer depends on what happens to be open in another tab is not stable.  That removes the one fact on which a three-lens panel had kept EduTools in the case menu, so the entry becomes a plain duplicate and the owner's original instinct is vindicated.  REJECTED: cross-tab state sync, feeding a tool a foreign case, a tab per VIEW, and any standalone-vs-in-app fork of a tool.  SUPERSEDES the navigation half of modes-and-views; fires that record's own third falsifier exactly as predicted |
| [`modes-and-views-in-the-top-row.md`](../design/modes-and-views-in-the-top-row.md) | ADR | yes | **DECIDED 2026-08-17** (delegated) — the top row renders MODES (Explore, EduTools: no case needed, they build their own) and VIEWS of a case (Flowsheet, Streams, …) as peers, which is why it is 833 px long AND why collapsing it into a dropdown labelled with the current workspace reads as a lit tab rather than a menu, and filed Explore — a mode — under one called `Case`.  `MenuBar` already encodes the distinction (those two are in every set and are the only two surviving a blank boot); the UI does not express it.  Modes stay visible at every width; views collapse under a control labelled for what it OPENS (`Views: Case ▾`).  EduTools goes on the LANDING PAGE, generated from `METHOD_TOOLS` and never a hand-written list, reaching the existing `?workspace=methods&tool=` contract.  On a phone the Case workspace shows ONE panel at a time.  REJECTED: giving Explore a front door in the same decision (a compound entry point smuggled in behind a simple one), and any second registry list |
| [`engineering-heuristics-as-curated-data.md`](../design/engineering-heuristics-as-curated-data.md) | ADR | yes | **PROPOSED 2026-08-17** — rules of thumb become CURATED DATA (`data/standards/heuristics/`), with a declared `appliesWhen`, a `validity` saying where the rule FAILS, a primary source, and `conflictsWith` NAMED rather than inferred.  The map SHOWS and never FUSES: no score blends heuristics into one recommendation, because a collapsed contradiction hands a student a confident answer assembled from an argument they never saw.  Computed and advised sit in separate columns (working capacity from the curated isotherms beside the rules that fire) and are never combined.  REJECTED: the first response, which was to reframe the request as a pure computation and keep heuristics out — a cited heuristic is as legitimate as a cited correlation, and what is illegitimate is an ANONYMOUS one presented as computed.  Also rejected: invented citations, silent precedence between conflicting rules, and any rule firing on a fact the case cannot declare |
| [`the-gui-on-a-390px-phone.md`](../design/the-gui-on-a-390px-phone.md) | FINDING | no | **MEASURED 2026-08-17, NOT FIXED** — the phone defect under the report that started the EduTools work.  The app has a hard ~480 px minimum width and clips the excess with `overflow:hidden` and NO scroller, so on all twelve pages the colour-scheme button, the clipboard bridge and the Theory-Guide link are unreachable by any gesture, and psychro's setup bar reaches x=1495 in a 390 px viewport (it clips at 1400 too).  `document.scrollWidth` reports no overflow, because every overflowing edge is hidden — the obvious check calls it clean.  States what a fix must decide and the one thing it must not do: arm the phone gate before the defects are triaged.  No rejected alternative, because nothing was decided |
| [`help-opens-in-a-tab-not-a-download.md`](../design/help-opens-in-a-tab-not-a-download.md) | ADR | yes | **FINDING + SHIPPED 2026-08-17** — Help pointed straight at `docs/<guide>.pdf#nameddest=<x>`; the link was correct and it still failed, because whether a PDF renders or downloads is decided by response headers and the reader's own browser setting, and the site is on GitHub Pages, which gives NO header control.  A help system whose behaviour is a property of the student's Firefox profile is not a help system.  So Choupo draws the manual itself (`gui/public/guide.html`, pdf.js), the bytes never reach the browser's PDF handler, and there is no header to set.  REJECTED, on availability rather than taste: LaTeX -> HTML, the better answer for a phone reader — tex4ht is installed without its support files (no `tex4ht.sty`, no `tex4ht.env`, no `htlatex`, no `make4ht`) and `preamble.tex` uses pgfplots, so every figure would route through a second toolchain; it stays recorded as the route to take if that changes.  §3.2 records the check taking three versions and why the two weak ones are the finding: "the anchor moved the reader off page 1" PASSED while both anchors landed on page 208 against a document placing them at 215 and 231 |
| [`theory-in-class-structure-study.md`](../design/theory-in-class-structure-study.md) | STUDY | no | **LEARNING STUDY, no decision** — how OpenFOAM, Cantera and DWSIM embed thermodynamic theory and size distributions in class structure; commissioned before any change to Choupo's thermo spine |

### Awaiting a decision

**EMPTY as of 2026-08-08** — cleared in one pass by
[`queue-ruling-2026-08-08.md`](../design/queue-ruling-2026-08-08.md), whose
same-day addendum resolved the one escalated item (the Wilson standards pair
joins the standing curation ledger: explicitly owed until a primary source is
verified, blocking nothing).  The one scheduled future entry (C2's spike
returning for review) was DISCHARGED the same day: reviewed and PASSED,
ruling in the spike record's §7.  The five rows this section
used to hold were STALE — each already decided when it was listed here, which
is this index failing its own purpose; they move below with their rulings.

### Decided — rows formerly (and wrongly) listed as awaiting

| record | kind | alt | ruling |
|---|---|---|---|
| [`solid-equilibrium-spike.md`](../design/solid-equilibrium-spike.md) | SPIKE | yes | **REVIEWED AND PASSED 2026-08-08 (same day)** — the three classes demonstrated, no falsification clause fired, the reactive oracle reproduced untouched to 3e-10; target architecture **RATIFIED**, migration **AUTHORISED** under the four boundary rulings of the record's §7 (one transfer mechanism; solver owns policy, models own physics); the both-active eutectic stays a named curation gap |
| [`numerical-provenance-contract.md`](../design/numerical-provenance-contract.md) | ADR | yes | **CONTRACT DECIDED 2026-08-10** (Vítor's post-consolidation directive) — every reported number traceable result→equation→state→model→parameters→primary datum→iterations→residual; `WHY?` is the SERIALISATION of that graph, so the ENGINE substantiates and the GUI only exposes.  Rejects: a parallel provenance log (two homes, drifts), boundary-only provenance (today's state — answers "which file", not "through what"), on-demand re-run (a re-run is a different execution and cannot survive into a permalink), and `Tracked<T>` in hot loops (cost, and an unreadable graph is not an explanation).  Binds work in flight; implementation UNAUTHORISED |
| [`property-evidence-taxonomy.md`](../design/property-evidence-taxonomy.md) | ADR | yes | **CONTRACT DECIDED 2026-08-10** — downloaded is not curated, fitted is not measured, generated is not authoritative: `origin` ∈ curatedMeasured/importedMeasured/fitted/estimated + orthogonal `reviewStatus`, solver REFUSES unreviewed candidates absent a case opt-in, `choupoProps acquire` mints candidates that never self-promote.  Rejects: a `trusted` boolean (cannot separate unchecked-measurement from known-estimate, so the remedy becomes unguessable), trust-by-location (today's rule, and why unsourced values sat inside standards/), a numeric confidence score as primary tag, and tagging only at import (`fitted` is minted internally).  Implementation UNAUTHORISED |
| [`reproducible-permalink-sealing.md`](../design/reproducible-permalink-sealing.md) | ADR | yes | **SEALING DECIDED 2026-08-10** (surface is roadmap) — the hash seals CODE + DATA: engine version + the case's full dependency closure as addressed content; the promise is **same result within the declared residual, NOT bit-for-bit**; the released `.wasm` is archived because permanent executability is part of the product promise.  Rejects: hashing case files only (same dict over a different catalogue is a different calculation), data-by-version-tag (a mutable pointer breaks every minted link), bit-exact determinism (unachievable, and buys a property nobody needs at accuracy's expense), re-run-at-current-version (answers a different question), rebuild-from-source (depends on a toolchain surviving) |
| [`post-consolidation-roadmap.md`](../design/post-consolidation-roadmap.md) | SCOPE | n/a | **RECORDED 2026-08-10** — positioning (reproducibility infrastructure; the opponent is the black box in the Methods section), the non-negotiable sequence, the ThermoML pilot's machine-enforced fit/held-out split, the anchor assessment taxonomy with the anti-rationalisation rule (MODEL-LIMITED requires a PRE-DECLARED envelope; otherwise UNEXPLAINED, which is a CI failure).  Authorises nothing |
| [`operational-memory-stopping-proof.md`](../design/operational-memory-stopping-proof.md) | RECORD | n/a | **CAMPAIGN CLOSED 2026-08-10** (Vítor's operational-memory ruling) — the four artefacts (ownership index · codeMap · caseManifest · impact brief §6b of DEV.md) with their gates, the five construction-time finds, and the stopping proof: two impact briefs filled by artefact lookup alone and verified against the tree.  Future gaps are fixed by the change that finds them; no enrichment campaign |
| [`queue-ruling-2026-08-08.md`](../design/queue-ruling-2026-08-08.md) | ADR | yes | **RULED 2026-08-08** — C1 delegation (amended: immediate ship, silence is not a mechanism, reserved list), C2 one solid-equilibrium architecture with class-appropriate solid models + mandatory 3-case spike, C3 uniform `phases` direction (no mass migration), D2–D4, N1–N5 deferred-with-trigger; D1 escalated back on a failed premise |
| [`open-decisions-2026-08-03.md`](../design/open-decisions-2026-08-03.md) | ADR | yes | RESOLVED 2026-08-03 in its own §Resolution — A no-reseal (report restructure to build), B built, C splitter capability to build, D stamp refinement |
| [`role-vocabulary-forum-2026-08-02.md`](../design/role-vocabulary-forum-2026-08-02.md) | FORUM | yes | DECIDED + BUILT 2026-08-02 — panel option C (DEV.md §4b item 2) |
| [`unread-dict-keys-proposal.md`](../design/unread-dict-keys-proposal.md) | ADR | no | v1 DECIDED + BUILT 2026-07-31; end state RULED 2026-08-08 (queue ruling D3: warning stays, `strictKeys` opt-in on demand) |
| [`solid-equilibrium-migration.md`](../design/solid-equilibrium-migration.md) | SCOPE | no | **CAMPAIGN OPENED 2026-08-08** under the C2 review's four boundaries (spike record §7 is the authority) — S1 production solver + S2 service seam SHIPPED (active-set + simultaneous Newton; provider-resolved sinks, `check_solid_service` sabotage-verified); S3–S5 sequenced, one slice per suite cycle |
| [`stream-transport-reconciliation.md`](../design/stream-transport-reconciliation.md) | ADR | yes | **RULED + EXECUTED 2026-08-08** — the result JSON's F/composition/H become the OVERALL material (one stream, one semantics, equal to `converged/` component by component); the "F (fluid)" relabel is the recorded rejected alternative; gate `check_stream_transport_closure`, sabotage-verified; goldens that pinned the fluid-only rows re-recorded |
| [`tp-stream-energy-coherence.md`](../design/tp-stream-energy-coherence.md) | ADR | yes | **RATIFIED 2026-08-09** (Vítor) — R-E1..R-E5 + O1: an unpinned TP inlet's ENERGY means its equilibrium state (as material already does); a pin is a declared constraint with a visible price; ONE solid rung on every surface; phase enthalpies are consistent VIEWS of one thermochemical definition (the binding precision — never pairwise-curated routes, refuse on missing transition data); one stream-H convention.  Implementation sliced (rung → inlet resolution → coherence); blast radius MEASURED before any golden moved.  **ALL THREE SLICES SHIPPED 2026-08-09** — flash19 closes at 100.00 % with Q = 0, only duties moved corpus-wide (ten goldens, each with its reason; cavett01 changed SIGN on two drums), gates `check_inlet_resolution` + `check_mass_closure`, both sabotage-verified |
| [`aqueous-analysis-inlet-scope.md`](../design/aqueous-analysis-inlet-scope.md) | SCOPE | yes | **A1 SHIPPED 2026-08-09** — a laboratory aqueous analysis is a first-class inlet with three separately recorded layers (measurement in `0/`, reconciled inventory in `converged/` under `calculated {}`, equilibrium in the answer).  Vítor ruled the three open questions: `aqueousAnalysis` is a SEVENTH exclusive canonical form (*inventory is not measurement* — the recorded rejected alternative was extending `speciesMolarFlows`, which would have made one block mean two things about the same numbers); the record goes to `converged/` only, never back into the authored file (O1); with no rule declared, an analysis inside the closure tolerance passes through ANNOUNCED and one outside it REFUSES naming both remedies — never a silent adjustment.  Ships `as <formula>` reporting bases through the shared `ElementComposition` parser, carried per-entry uncertainties, an explicit density route (iterative density refused BY NAME as the declared gap), and one reconciliation method (`adjustSingleSpecies` + its `adjustChloride` sugar) under a mandatory `maximumCorrection`.  Blast radius as predicted: ZERO corpus goldens moved.  Witness `analysis01_water_analysis_inlet` (+0.7813 % ion balance → 0, chloride +5.1208 %); gate `check_aqueous_analysis`, sabotage-verified.  **A2 SHIPPED 2026-08-09** — `method weightedLeastSquares;` minimises `Σ ((x−m)/σ)²` over the measured quantities subject to the laws the case DECLARES in `enforce ( electroneutrality elementalConservation )` plus non-negativity, solved as a convex QP by the existing hand-rolled `solver/ActiveSetQP`.  **The ruling's one-way arrow is STRUCTURAL, not a comment**: the reconciler is its own translation unit including nothing from `thermo/`, so it cannot name a speciation solver or an activity model — checked as a compile fact (the object references exactly ONE Choupo symbol).  The per-law attribution of every correction is a KKT identity the solver asserts before publishing.  Adds the ELEMENT-TOTAL row (a redundant determination carrying no material — which is what makes `elementalConservation` real), `maximumCorrection` in sigma beside per cent, and every correction reported with reported/adjusted/σ+weight/correction-in-σ/constraint-responsible/closure.  **`genericWaterAnalysis-v1` was REFUSED, not shipped** — an uncertainty belongs to a laboratory and a method, not to an analyte, so a versioned table would be invented and would look authoritative because it carried a version; a missing σ refuses naming both one-line remedies.  **pH stays refused and A2 found the reason**: adjusting it needs an activity coefficient, hence the speciation — it is not deferred, it is on the other side of the boundary.  Witness `analysis02_weighted_least_squares` (four quantities move, worst 0.2813 σ, against A1's single 5.12 % shove); gate `check_aqueous_reconciliation`, four sabotages, one of which proved the object-level boundary reading is blind to header-inline reach (stated, not discovered later).  ZERO existing goldens moved |
| [`solverdict-consolidation-scope.md`](../design/solverdict-consolidation-scope.md) | SCOPE | no | DECIDED 2026-08-04 by Vítor — Option A, recorded in its own §5 |
| [`seal-divergence-forum-2026-08-02.md`](../design/seal-divergence-forum-2026-08-02.md) | FORUM | no | DECIDED 2026-08-03 — no re-seal (counsel ruling in open-decisions §Resolution); report restructure is implementation |

### Deliberations (the decision is the conclusion)

| record | kind | alt |
|---|---|---|
| [`thermo-grammar-professors-forum-2026-07-04.md`](../design/thermo-grammar-professors-forum-2026-07-04.md) | FORUM | no |
| [`thermo-grammar-students-forum-2026-07-04.md`](../design/thermo-grammar-students-forum-2026-07-04.md) | FORUM | no |
| [`mixing-rules-forum-2026-07-04.md`](../design/mixing-rules-forum-2026-07-04.md) | FORUM | yes |
| [`flash-eos-vs-raoult-forum-2026-07-04.md`](../design/flash-eos-vs-raoult-forum-2026-07-04.md) | FORUM | yes |
| [`world-selection-forum-2026-07-04.md`](../design/world-selection-forum-2026-07-04.md) | FORUM | yes |
| [`mesh-stabilization-forum-2026-07-03.md`](../design/mesh-stabilization-forum-2026-07-03.md) | FORUM | yes |
| [`cyclone-iozia-leith-forum-2026-07-04.md`](../design/cyclone-iozia-leith-forum-2026-07-04.md) | FORUM | yes |
| [`pneumatic-conveying-forum-2026-07-03.md`](../design/pneumatic-conveying-forum-2026-07-03.md) | FORUM | yes |
| [`gibbs-map-forum-2026-07-02.md`](../design/gibbs-map-forum-2026-07-02.md) | FORUM | no |
| [`under-relaxation-forum-2026-07-03.md`](../design/under-relaxation-forum-2026-07-03.md) | FORUM | no |
| [`solution-directories-forum-2026-07-03.md`](../design/solution-directories-forum-2026-07-03.md) | FORUM | no |
| [`property-dict-review-2026-07-04.md`](../design/property-dict-review-2026-07-04.md) | FORUM | no |
| [`comfort-loop-2026-07-04.md`](../design/comfort-loop-2026-07-04.md) | FORUM | no |

### Evidence, deciding nothing

| record | kind | note |
|---|---|---|
| [`solid-migration-witness-data.md`](../design/solid-migration-witness-data.md) | SCOPE | staged 2026-08-08, reviewStatus interim — the authorised migration's validation targets, transcribed from the two owner-provided primaries (Archer 1992: invariant points + dihydrate formation props; Marcilla 1995: LLE tie-lines + the LLS tie-triangles); locates anchors, curates nothing |
| [`state-of-the-art-property-study-2026-07-17.md`](../design/state-of-the-art-property-study-2026-07-17.md) | STUDY | ordered after two AIs decided a day's architecture in a self-ratification loop without studying the field |
| [`audit-2026-08-05-arity.md`](audit-2026-08-05-arity.md) | STUDY | fleet audit, I1 |
| [`audit-2026-08-05-silent-fallbacks.md`](audit-2026-08-05-silent-fallbacks.md) | STUDY | fleet audit, I5 |
| [`audit-2026-08-05-provenance.md`](audit-2026-08-05-provenance.md) | STUDY | fleet audit, I3 |
| [`dwsim-architecture-manual.md`](../design/dwsim-architecture-manual.md) | STUDY | a consultable dev manual: 5 patterns to copy, 6 to avoid, each measured from source.  Rejects DWSIM's presentation-first object base (6 of 9 mandatory members are GUI; `Calculate` is optional) |
| [`dwsim-solids-study.md`](../design/dwsim-solids-study.md) | STUDY | ordered "if openfoam is not the way, try dwsim"; DWSIM has ONE solid mechanism (fusion K, chemistry entering via gamma) and corroborates Choupo's crystal equation; decides nothing |
| [`openfoam-study.md`](../design/openfoam-study.md) | STUDY | ordered "go and learn first, no deliverable"; proposes a uniform `phases ( … )` list and one shared unknown-type refusal, decides neither |
| [`thermoml-campaign-plan.md`](../design/thermoml-campaign-plan.md) | PLAN | the ThermoML curation campaign: sync/index/search/extract, the evidence partition, the dossier boundary.  Sequencing only -- the scientific rulings live in the ADRs it points at |
| [`evidence-partition-slice-plan.md`](../design/evidence-partition-slice-plan.md) | PLAN | the FIT / HELD-OUT contract as an executable plan: the grammar, enforcement BY CONSTRUCTION, refusals R1-R5, and the dossier's home outside every resolver root.  R5 shipped 2026-08-11 WIDER than planned and the record says so |
| [`acetone-ipa-reference-case.md`](../design/acetone-ipa-reference-case.md) | ANCHOR | transcribed 2026-08-11 from two owner-supplied primaries (Luyben 2011: flowsheet, stream table, kinetics, UNIQUAC + the IPA/water azeotrope; Rioux & Vannice 2003: measured LHHW rates).  States that the two are at DIFFERENT SCALES and must not be blended, and lists the five data gaps a case would still have to close.  Locates anchors, curates nothing, builds no case |
| [`acetone-plant-closure-state.md`](../design/acetone-plant-closure-state.md) | BUILD RECORD | how the Luyben plant went from six isolated units to a closed flowsheet, 2026-08-13.  Six findings, each reproduced: a declared H2 approximation that was NOT locally contained (it diverges the MESH three units downstream); the plant's inability to honour the published product rate; a refusal that turned out to be ONE missing datum answered two ways rather than a missing feature; the two column ceilings compounding through the recycle; a convergence aid that cannot live on a unit object; and an importer closure written for NRTL that never grew.  Decides nothing about doctrine -- it records what building it cost |
| [`props-component-curator-assessment.md`](../design/props-component-curator-assessment.md) | STUDY | assessment of the proposed `curateComponent` op; concluded the natural output is a curation DOSSIER, not a resurrected `data/proposed/` tier.  Decides nothing by itself |
| [`verifying-the-catalogue-against-a-book.md`](../design/verifying-the-catalogue-against-a-book.md) | STUDY | 2026-08-25 — the component catalogue read back against Poling App. A from Vítor's own copy (`bin/curate/verify_against_poling.py`, matched by CAS never by name).  Of 158 CAS-bearing records the appendix lists 75: **238 values reproduce it to its printed precision, 105 differ by under 1 %, 37 by more**, and 19 Vliq comparisons are REFUSED rather than counted because the appendix states its volume at the measurement temperature.  Per quantity: MW agrees everywhere and **no Tc disagrees by as much as 1 %** — every notable difference is in Pc, ω or ΔHvap, the hard-to-measure and the derived.  Deliberately NOT a gate (the book cannot live here, so a skip-when-absent check would be permanently green — the retired `check_true_ions` shape) and deliberately CHANGES NOTHING: the notable disagreements are mostly ours being NEWER.  Puts evidence under the reserved CoolProp-provenance question; **decides it not at all** |
| [`properties-gui-task-orientation-2026-08-11.md`](../design/properties-gui-task-orientation-2026-08-11.md) | STUDY | measured the Properties GUI against a task-orientation critique: the flow is already substance-first and one-click-to-a-curve, and the real gap is that a component is not an inspectable object.  Item 1 (the Component Inspector) shipped from it |

### Historical

`docs/architecture/archive/`, `final-property-architecture.md` (superseded
2026-07-14), `propertyPackage-v2-constitution.md` and everything under
`docs/architecture/proposals/` (unratified drafts).

## 4. Where the CLOSED decisions were argued

Correspondence C5 requires each entry in
[`project-philosophy.md`](project-philosophy.md) §5 to point at its argument.
Six do.  **Five do not, and that is stated rather than papered over** — they
predate the design-record practice and their reasoning survives only in
`CLAUDE.md` §10 as a settled note.

| closed decision | argued in |
|---|---|
| C++17, no external libraries | **ARGUED** — `CLAUDE.md` §10 licence policy ("favours readable, local C++ over dependency expansion") and the COSMO reversal, which states what the rejection was *against*: bloat — heavy deps, quantum chemistry, bulk imports, a new architecture |
| Make, no CMake | **UNARGUED** — asserted, never reasoned, anywhere |
| Explicit factory, no auto-registration | **ARGUED** — `CLAUDE.md` §5 gives three reasons: pedagogical clarity, the static-init order fiasco, and pattern consistency |
| File-first dictionaries, never YAML/JSON | **UNARGUED** — asserted, never reasoned, anywhere |
| One binary per problem class | **PARTLY ARGUED** — the *boundary* is argued (do not split within a class; strategies coexist, selected by dict) but not the choice of one-per-class over one-binary-total |
| GPL-3.0-or-later, no CLA | `CONTRIBUTING.md`, `TRADEMARKS.md` |
| Brand casing | `CLAUDE.md` §10 |
| Flat `components/` | `CLAUDE.md` §7 |
| GUI is a runner, not an editor | [`../ai/gui-credo.md`](../ai/gui-credo.md) |
| Estimation is a curation problem | [`property-architecture.md`](property-architecture.md) |
| The repository is English (US) | `CLAUDE.md` §5, [`project-philosophy.md`](project-philosophy.md) §5 |

**This table was WRONG when first written, and the correction is the point.**
It claimed all five founding decisions had no record.  Re-reading the sources
instead of the index found that **two of the five are fully argued** and a
third is argued in part — the reasoning existed, in `CLAUDE.md` §5 and in the
licence-policy and COSMO-reversal passages of §10, which is simply not where
the index looked.

**Exactly two are genuinely unargued: Make-not-CMake, and file-first
dictionaries.**  Both are asserted and never reasoned, anywhere in the tree.

This is the same defect as the provenance audit's species list, in the
opposite direction: that one **undercounted** violations (seventeen named,
eighteen found), this one **overcounted** them (five claimed, two real).  One
cause — *a hand-compiled list is itself a hand-maintained derived fact*, and
an index that measures the corpus without re-reading it drifts from the
corpus.  A gate recounts; a list remembers what it was told once.

The two real gaps stay named.  They are the oldest decisions and the most
likely to be challenged by someone who was not there, and *"it was decided"*
is not an argument.

## 5. What this index shows that no individual record could

**46 of 124 records state no rejected alternative.**  For a FORUM or a STUDY
that is often fine.  For an ADR it means the decision is recorded without the
argument that would prevent it being reopened — and reopening settled
questions is the specific failure the constitutional layer exists to stop.

The pattern is temporal: the records with rejected alternatives cluster in
July, when forums were convened deliberately; the ones without cluster in the
August programme docs, written as scope statements after the decision was
already taken. **The practice decayed as the work sped up**, which is exactly
when the record matters most.

## 6. Actions

| id | action |
|---|---|
| **AD1** | The five founding decisions gain a record of their reasoning, or `project-philosophy.md` §5 states that their argument is unrecorded. Either is honest; silence is not. |
| **AD2** | An ADR states its rejected alternatives, or says why there were none. A gate can check the section exists; it cannot check it is true. |
| **AD3** | Every future record declares its **kind** and **status** in its own header, so this index reads them instead of a maintainer inferring them — a second home for a fact that the record itself should own. |

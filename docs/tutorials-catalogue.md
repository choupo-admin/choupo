<!--
  Choupo — Tutorials catalogue (DEV reference).
  The annotated index of bundled tutorials that used to live in CLAUDE.md §8.
  `listCases` enumerates them at runtime; each case's controlDict.description is
  the one-line label. This file is the curated "what does each one demonstrate"
  table. Relocated from CLAUDE.md 2026-06-06 to keep CLAUDE.md under its
  session-load budget.
  Audience: dev (same as CLAUDE.md). Case authors: see docs/ai/.
-->

# Tutorials catalogue

`listCases` lists every bundled tutorial; `runCase tutorials/<category>/<name>`
runs one (auto-dispatching by `controlDict.application`).  This table is the
annotated index of what each one demonstrates.

| Tutorial | Demonstrates |
|---|---|
| `adiabaticFlash01_benzene_toluene` | Nested Newton over T using Cp + ΔHvap(Watson) |
| `bubbleT01_ethanol_water` | NRTL γ + Newton-1D in T |
| `column01_benzene_toluene` | Distillation Wang-Henke, ideal system |
| `column09_tray_hydraulics` | The question a converged MESH never asks: **can the trays pass the traffic?**  Souders-Brown + Fair flooding, Sinnott pressure drop, downcomer backup, weep point.  Omit `diameter` and the block DESIGNS the column (D = 1.322 m, set by stage 14 at exactly 80 % of flood) |
| `column10_flooding` | The same column built 1.10 m wide.  Identical stream table, identical convergence, stage 14 at **115.6 % of flood**.  A perfectly good column on paper that would not work |
| `column11_murphree` | The trays stop being ideal.  `murphreeEfficiency 0.70` folds `y_j = E·y*_j + (1−E)·y_{j+1}` into an effective K; the distillate falls 98.12 % → 95.05 % and `nStages` now counts **real trays**, which is what the hydraulics sizes.  `E = 1` reproduces the ideal column bit for bit |
| `batch07_lhhw_methylAcetate` | The `cstr07` rate law in a BATCH vessel — one `RateLaw`, two reactors.  Climbs to X = 76.6 %, the activity equilibrium (K_a = 25.50 by Pöpken's independent Eq 17).  Water spiked into the charge halves the initial rate at unchanged reactant fractions: **product inhibition, seen** |
| `ctrl07_lhhw_inhibition` | `ctrl01`'s PID loop against a self-poisoning reaction (`r = k c_A / (1 + K_B c_B)`).  The exotherm fades as its own product covers the catalyst, so the loop rejects a shrinking disturbance |
| `cstr01_first_order` | Newton-1D in extent, Arrhenius kinetics |
| `cstr05_multiplicity` | Non-isothermal CSTR with **three** steady states (300.4 / 348.4 / 391.3 K).  The extents are eliminated at fixed T, leaving one equation `φ(T) = H_out − H_in − Q_ext`; the solver scans it, prints every root, and reports the one nearest `T_guess`.  Change `T_guess` and the same reactor answers differently — the physics, not a numerical defect |
| `dewT01_ethanol_water` | Same as bubbleT but Σy/K = 1 |
| `extraction01_waterButanol` | LL flash with NRTL via Gibbs-energy minimisation; converges to ~98 / 40 water split |
| `fitNRTL01_ethanol_water` | `propsDict type fitParameters;` (mode fit) — LM regression of the ethanol-water NRTL pair (a_ij, b_ij, a_ji, b_ji; α fixed) against 1 atm bubble-T data on the canonical fit engine; golden pins χ² and the fitted parameters.  (Its predecessor driver `fitBinaryPair` is retired — the factory throws.) |
| `flash01_benzene_toluene` | Raoult VLE, the canonical first run |
| `flash02_ethanol_water` | NRTL with Wegstein outer loop |
| `flash03_wilson_ethanol_water` | Wilson model side-by-side with NRTL |
| `flash06_sweep_T` | The landing-page flash, swept: `outerDict type sweep;` on `units[0].operation.T` 360→385 K — V/F S-curve across the bubble (~368 K) → dew (~374 K) window + the duty that holds each T.  Reference artifact for the GUI "sweep this parameter" concept (the dict a future per-unit button would synthesize) |
| `hx01_heater` | T_target → Q, or Q → T_target via energy balance |
| `pfr01_first_order` | RK4 manual integration; X = 1 - exp(-kτ) |
| `process01_reactor_flash` | First two-unit flowsheet (CSTR → flash) |
| `process02_reactor_hx_flash` | Three units with T propagation |
| `process02_with_design` | Same flowsheet + `postDict` with sizing + Guthrie costing |
| `process03_recycle` | Reactor-separator-recycle with Wegstein on tear stream |
| `process04_research_workflow` | Case-local `constant/binaryPairs/<model>/<pair>.dat` instead of inline pairs in `thermoPackage` — the "fit-promote" pattern target |
| `sensitivity01_column_reflux` | `outerDict` sweep, CSV output, R vs V_reboiler |
| `optim06_integer_stages` | The INTEGER variable by declared enumeration (Front 4c, forum #102.6/#103): `integer true;` on `nStages` makes the driver run the FULL inner problem (min V_strip s.t. x_D_LK ≥ 0.98 over R) at EVERY stage count 8..14 — each one a fresh run (a changed nStages rebuilds the column, never a late mutation on a sized object).  The table is the lesson: N = 8 REFUSED aloud (the feed cannot sit on the bottom stage — the guard this case exposed, replacing a live SEGFAULT), then V* falls 1.03 → 0.045 kmol/s with visibly diminishing returns; best marked at 14.  Golden pins the enumeration KPIs. |
| `pareto01_purity_energy` | The PARETO FRONT by ε-constraint (Front 4a, forum #102.6/#103): `type paretoSweep;` sweeps the purity bound `x_D_LK atLeast ε` (0.96→0.994, 5 points) while ONE full SQP run per point minimises `V_strip` over R.  The front is textbook: at ε = 0.96 the bound is SLACK (the unconstrained optimum at the R floor); from 0.9685 up it is ACTIVE — achieved x_D equals ε exactly, R* climbs 1.6→3.77 and V* 0.036→0.066 (the marginal energy price of purity = the printed Lagrange multiplier).  Warm start point-to-point is an execution aid (cold retry announced on failure); failed/infeasible points stay on the table, marked.  Golden pins the 5/5 converged split via the `pareto` KPIs. |
| `sensitivity02_feasibility_map` | The sweep as a FEASIBILITY MAP (Front 4b, forum #102.6/#103): the same reflux sweep with declared `constraints (...)` — the SAME shared grammar the SQP optimizer parses.  Every row gains per-constraint value/bound/signed-normalised-residual/satisfied columns + a final `feasible` verdict; NOTHING is filtered.  The lesson is the window: `x_D_LK atLeast 0.985` kills R < ~2.3, `V_strip atMost 0.065` kills R > ~3.5, leaving 4 feasible points of 10 (golden-pinned via the `sweep` KPIs). |
| `gibbs01_water_gas_shift` | Gibbs reactor, R = 1 reaction, equimolar CO/H2O at 800 K |
| `gibbs02_steam_reforming` | Gibbs reactor, R = 2 reactions, CH4 + 3 H2O at 1000 K |
| `gibbs03_methane_combustion` | Gibbs reactor, R = 3 reactions, CH4 + air at 1800 K |
| `gibbs04_wgs_temperature_sweep` | SweepDriver + GibbsReactor: y_CO2 vs T (Le Chatelier) |
| `gibbs05_adiabatic_flame` | Adiabatic Gibbs reactor: outer Newton on T with H_out=H_in (lean CH4/air → 1492 K) |
| `gibbs06_thermal_nox` | Gibbs reactor + NO species → thermal NOx via Zeldovich (206 ppm at 1800 K) |
| `optim01_column_reflux` | OptimizationDriver, KPI objective.  Nelder-Mead minimises `column01.V_strip` over R + feedStage; converges to lower bound of R (CAPEX-style boundary optimum) |
| `optim02_process_cost` | OptimizationDriver, `costTotal` objective.  Reactor V_R + heater Tout minimising sum of Guthrie totalModuleCost across all costed units; post chain (sizing + costing) runs per evaluation |
| `optim04_membrane_recovery_scaling` | **Constrained optimisation by hand-rolled SQP** (line-search, damped-BFGS on the Lagrangian, L1 merit, active-set convex QP --- N&W Ch.18/16.5).  Maximises brackish-RO `vessel.water_recovery` over feed pressure + (continuous) module count, subject to `maxSI_gypsum_wall <= 0` and `maxSI_calcite_wall <= 0` (wall saturation, Davies).  The calcite constraint is **active at the optimum with a positive Lagrange multiplier** (the shadow price of scaling head-room); recovery is driven onto the scaling limit.  Run-only (the optimum is not independently known --- pinning a numeric golden would pin a possibly-wrong answer); NaN/inf-guarded.  The trace prints the active set, the multipliers, the sim-noise-floor probe, and the 5-line KKT certificate.  Same scaling physics as `membrane07_scaling_si`. |
| `vlle01_waterButanol` | `phaseSet VLLE` --- VLLE branch of IsothermalFlash via Gibbs-energy minimisation on (β_V, β_α, y, x_α) with multi-start Nelder-Mead.  At T = 365 K, P = 1 bar the LL gap has just closed; the optimum is V + L (one liquid β = 0).  Demonstrates the algorithm and the 3-output flowsheet contract for VLLE. |
| `vlle03_audit_artificial` | Synthetic 3-component audit case (`compA`, `compB`, `compC` in `data/standards/components/`).  Psat_C ≫ Psat_A = Psat_B, NRTL τ_AB = 5 (strong LL gap), τ_AC = τ_BC = 0 (C dissolves freely).  By construction the global Gibbs minimum at T = 350 K, P = 1 bar is 3-phase: V rich in C, Lα rich in A, Lβ rich in B.  Confirms the VLLE solver correctness post the multi-start fix. |
| `membrane01_RO_NaCl_seawater` | Canonical seawater RO: 35 g/L NaCl feed at 55 bar through a SW30HR-style spiral-wound element (35 m², 1 m, k_film 50 µm/s).  Reports 99.78 % observed NaCl rejection, 15.6 % water recovery, J_w_avg ≈ 13 µm/s.  NaCl mass balance closes to <0.04 %.  First-membrane case, the benchmark. |
| `membrane02_NF_sugar` | Loose NF on glucose: 10 g/L glucose feed at 10 bar through a NF270-style element (5 m², 1 m, k_film 50 µm/s).  Reports 66.3 % observed rejection --- pedagogically the defining "partial rejection" feature of NF, contrasting with RO's near-total rejection.  Same solution-diffusion + film-model + van't Hoff transport model as the RO case, just with different membrane parameters. |
| `membrane03_concentration_polarization` | SweepDriver over the mass-transfer coefficient `k_film` from 10 µm/s to 200 µm/s on the seawater RO case.  10 points span an order of magnitude; recovery climbs 6 → 21 %, J_w_avg 5 → 17.6 µm/s.  The textbook concentration-polarisation curve: poor mixing means a thick boundary layer, c_m ≫ c_b, Δπ swells, J_w collapses.  Output is `sweep_kfilm.csv` for plotting. |
| `htbench01_correlations` | **Runs under `choupoProps`.**  The heat-transfer correlation bench (`heatTransferBench`): each registered convective correlation RUNS its `verify()` against a published Nusselt anchor — the heat-side twin of the membrane Sherwood story.  **DittusBoelter** Nu = 0.023 Re^0.8 Pr^0.4 vs Incropera & DeWitt 6th ed. Example 8.4 (Nu = 205 → AAD 2.2 %); **Gnielinski** vs the same example's alternate solution (Nu = 224 → AAD 0.12 %); **Kern** shell-side closed j_H form self-consistency-checked (the original pin is a chart, not a number — stated honestly).  Golden pins are the per-correlation deviations; AAD tolerance 5 % honours textbook rounding + the correlations' own scatter.  An unwired self-check would itself be a no-silent-crutch violation — this op is the wire. |
| `steam01_if97_verification` | **Runs under `choupoProps`.**  IAPWS-IF97 steam tables (regions 1, 2, 4 + B23; hand-rolled, every coefficient cited to R7-97(2012) table numbers).  The golden pins ARE the release's published verification values (e.g. region 1 at 300 K/3 MPa: v = 0.100215168e-2 m³/kg, h = 115.331273 kJ/kg; Tsat(1 bar) = 372.755919 K; psat at the triple point = 611.657 Pa) — `IF97::verify()` re-runs all 20 states on every run and refuses above 1e-8 relative.  Ops: `point`, `saturation` (T,psat,h_f,h_g,… CSV), `isobar` (h,s,v,cp vs T with the Tsat crossing announced).  Regions 3/5 refused loudly.  Integration into ThermoPackage deferred (kernel + bench only). |
| `isotherm01_eval_13x_co2` | **Runs under `choupoProps`.**  The adsorption-isotherm evaluation bench (`isothermEval`) on the curated CO₂/zeolite-13X record (`parameters/adsorption/equilibria/`): q(T,p) tabulated at 293/313/333 K, the Henry limit and the van't Hoff affinity b(T) per temperature, and FOUR physics gates run aloud — q/p → henryLimit as p → 0, monotonic saturation to q_sat, the loaded b(tRef) pinning the record's raw b_298 to 1e-12, and UNIT INVARIANCE (a case-local twin record declaring the same physics in Pa evaluates identically to the catalogue's bar form).  A failed gate FAILS the run; the golden pins b/henry per T + every gate flag. |
| `isotherm02_fit_jaschik` | **Runs under `choupoProps`.**  `fitParameters kind isotherm` — the joint multi-T van't Hoff Langmuir regression on REAL data: CO₂ on zeolite 13X, Jaschik et al. 2020 (CC BY, vendored; 132 points, 293/313/333 K, 0–1 bar) → q_max = 4.314 ± 0.052 mol/kg, b_ref = 4.07×10⁻⁴ 1/Pa, ΔH_ads = −37.4 ± 1.9 kJ/mol, with a schema-exact record PROPOSAL written to `data/proposed/` for human promotion.  Three sibling ops are the identifiability gates: zero-noise synthetic RECOVERY to 1e-6; single-T data REFUSES to quote ΔH_ads (structurally unidentifiable); Henry-regime data reports only the PRODUCT q_max·b_ref (the Henry slope — the factors are inseparable).  Golden pins parameters ± stderr, correlations and all gate verdicts. |
| `isotherm03_fit_refusals` | **Runs under `choupoProps`**, exits NON-ZERO on purpose (`.expect-nonconvergence`, like batch12).  The adversarial fit gates as a TAUGHT refusal: LM strangled at `maxIter 1` must drag `converged 0` → `identifiable 0` and REFUSE its values/CIs BY NAME — the old behaviour ("identifiable 1" stamped beside a NaN stderr) is the bug this case forbids forever.  A fit that did not EARN its verdict certifies nothing; the green demonstrations live in isotherm02. |
| `softener_brackish` | **Runs under `choupoProps`.**  Ion-exchange softener (PHREEQC `EXCHANGE` analogue): a Na-form strong-acid cation resin via full Gaines-Thomas mass-action (`exchange { resin SAC_Na; CEC 2.0 eq/L; }`) — divalent Ca²⁺/Mg²⁺ (equivalent-fraction-favoured) outcompete Na⁺ for the resin sites.  The brackish `scaling_ro_brackish` water raw vs softened: Ca 2.10e-3 → 2.5e-7 mol/kg (hardness 321 → 0.05 mg/L CaCO₃, 99.99 % removal), Na up +6.72 meq/L (the salt penalty, made visible), and the downstream SI_calcite flips from +1.43 (scaling) to −2.37 at r=0.85 — the scaling driver removed upstream.  Carries the EXCHANGE EQUILIBRIUM banner (limiting fresh-resin effluent, not a bed in service; breakthrough/regeneration are transient, deferred). |
| `precipitation_ro_brackish` | **Runs under `choupoProps`.**  Equilibrium precipitation (PHREEQC `EQUILIBRIUM_PHASES` analogue): `equilibrate { minerals (calcite gypsum); }` lets supersaturated minerals precipitate to SI=0 via an augmented-Newton active set, reporting how much (mol/kg, mg/kg, kg/day with `feedFlow`) + the water after.  The gypsum pin with Ca,SO₄ doubled returns exactly the over-added excess (machine precision); the recovery scan shows calcite at 12.6 kg/day while gypsum stays dissolved yet its SI_eq dips below the raw curve (calcite consuming shared Ca — multi-mineral coupling, visible).  Carries the non-suppressible EQUILIBRIUM CEILING banner (thermodynamic maximum, not a kinetic deposit prediction).  Sibling of `scaling_ro_brackish` (will it scale? → how much, at most?). |
| `membrane08_softened_scaling` | The treatment train: `feed → ionExchanger → spiralWoundModule`.  A Na-form softener (the `ionExchanger` unit op, Gaines-Thomas exchange) removes hardness upstream (Ca 99.99 %, Na +6.72 meq/L), and the membrane vessel — byte-identical to `membrane07` (8 in, 6 modules, same `scaling{}`) — now keeps its per-module wall SI_calcite negative through all 6 modules (max −3.68, never crosses) vs membrane07's crossing at module 3.  The scaling driver removed at the source, with zero change to the membrane.  `removal`/target-hardness keys are refused (results, not hardware specs). |
| `membrane07_scaling_si` | The speciation×membrane marriage: brackish groundwater (the `scaling_ro_brackish` analysis, acid-dosed to pH 7.0) on 6 × 8-inch SW30HR modules (`moduleDiameter 8 in; nModules 6;` — area from the standard-element table), 55 % recovery, with `scaling { minerals (calcite gypsum); }` running the aqueous SpeciationSolver per module on the bulk retentate AND at the wall (film-model c_m).  The wall crosses SI_calcite = 0 at module 3 of 6 while the bulk only crosses at module 6 — concentration polarisation seen as the scaling driver, three modules ahead.  Per-module SI profile plots in the GUI via the standard profile channel. |
| `membrane09_index_vs_rigorous` | **The industry-index vs rigorous-activity SI lesson.**  Standard surface seawater (S = 35, major ions, I ≈ 0.68) on 7 × 8-inch SW30HR modules, acid-dosed to pH 7.0, with `scaling { minerals (calcite gypsum); }`.  Because calcite is tracked and Ca + HCO₃ are present, the audit AUTO-emits the closed-form industry calcite indices — **LSI** (Langelier 1936), **Stiff-Davis** (1952, brine-corrected), **Ryznar RSI** (1944) — beside the rigorous Davies `SI_calcite`, bulk + wall, at each module exit.  The empirical indices use ION CONCENTRATIONS + an empirical ionic-strength correction; the rigorous SI uses real ACTIVITIES (γ·m).  Sharing `logK_cc(T)`, the ONLY difference is concentration-vs-activity: at the deepest module (high wall I) **LSI(wall) = +0.99 over-predicts the rigorous SI_calcite(wall) = +0.21 by +0.78** — the bag-of-indices shortcut is unsafe at the wall in a high-recovery concentrate.  Stiff-Davis (+0.78) pulls partway back via its empirical brine K but cannot reach the rigorous value (it is a chart fit, not the real γ).  The audit footer prints the gap explicitly.  Kernel + primary sources: `src/thermo/electrolyte/ScalingIndices.{H,cpp}`; LSI pinned vs a textbook worked example in `data/standards/parameters/electrolyte/VALIDATION.md`. |
| `ksp_temperature` | **Runs under `choupoProps`.**  K(T) MATTERS — one fixed hard water (Ca 0.010, Na 0.030, Cl 0.050, SO₄ 0.012, HCO₃ 0.005 mol/kg, pH 7.6 given) speciated at 10/25/40/60/80 °C.  Calcite dissolution is exothermic, so its **analytic** K(T) (PHREEQC `-analytical_expression`, anchored on logK25) falls with T and **SI_calcite RISES** 0.59 → 0.81 → 1.01 → 1.24 → 1.43 — **retrograde solubility** (why hot heat-exchanger / warm RO-concentrate surfaces scale with CaCO₃ although the cold feed looks safe).  Contrast halite (NaCl, no analytic in phreeqc.dat → constant-dH van't Hoff, dH > 0): **SI_halite drifts down** −4.59 → −4.66 (normal solubility).  At 25 °C every K(T) returns logK25 exactly (byte-stable); off 25 °C the run announces the form per entry (analytic / van't Hoff / flat) and warns when an analytic is used beyond its fitted range (e.g. the HCl ion pair, valid 0–50 °C, at 60/80 °C).  K(T) spot-check: calcite log K(50 °C) = −8.685 in the CO3²⁻ basis matches Plummer & Busenberg (1982). |
| `nox01_zeldovich_postflame` | **Runs under `choupoBatch`. THERMAL NOx + THE TWO-ENGINE HANDSHAKE** — hot burnt gas (lean H₂/air, 2125 K/1 atm, Homer & Sutton Flame 1 via Glarborg 2018) held isothermal, all nitrogen starting as N₂; the **Glarborg 2018 nitrogen chemistry** (PECS 67, 31, DOI 10.1016/j.pecs.2018.01.002 — extended Zeldovich Table 2 + NNH R20-24 + N₂O R15-19 with the Lindemann falloff, VERBATIM) on the Ó Conaire H/O core.  **The cross-engine pin: the kinetic plateau (5167 ppm at 6 s, 98.9 %) lands on the gibbsReactor equilibrium (5227 ppm)** — stiff batch integration and steady Gibbs minimisation, one thermodynamics.  t½ = 0.50 s, t90 % = 1.56 s: ignition takes µs, thermal NO takes SECONDS — the timescale gap that makes quench-based NOx control possible.  14 species incl. NNH (ΔHf +249.5 kJ/mol, the ATcT-consistent value the NNH route hinges on) — all from this session's radical database. |
| `ignition02_h2air_slack` | **Runs under `choupoBatch`. THE QUANTITATIVE SHOCK-TUBE PIN** — stoichiometric H₂/air at 1100 K / 2 atm constant-V, the reflected-shock condition of Slack 1977 as compiled in Ó Conaire et al. 2004 Fig 8 (Int. J. Chem. Kinet. 36, 603, DOI 10.1002/kin.20036).  Kinetics: the Ó Conaire mechanism, **Table I verbatim** (19 reactions → 21 entries: two Troe falloffs where the second Table line is the high-pressure limit, two summed DUPLICATE pairs, per-reaction third-body efficiencies; A in cm³-mol-s and Ea in kcal/mol with the engine converting + announcing all 21).  **τ_ign = 43 µs vs the measured ~40–60 µs window** — and the case teaches WHY there is no radical seed: this mechanism SELF-INITIATES (R5, H₂+M=2Ḣ, Ea 105 kcal — the slowness IS the induction), while GRI's H/O subset (ignition01) has no pure initiation channel and needs its trace-H; with the seed wrongly kept, τ collapses to 12 µs — *initial conditions are physics, not knobs*.  The engine now prints `tau_ign` (max dT/dt) in every igniting batch epilogue. |
| `ignition01_h2o2` (+ `_rk4`) | **Runs under `choupoBatch`. GAS-PHASE IGNITION WITH REAL KINETICS** — adiabatic constant-V H₂/O₂ ignition at 1100 K/5 bar, the **GRI-Mech 3.0 H/O subsystem transcribed VERBATIM** (28 reactions via `bin/curate/gri_subset_to_choupo.py`: third-body efficiencies, the explicit-collider channels that explain the zeroed efficiencies on H+O2+M, the 2OH(+M)⇌H2O2(+M) Troe falloff, both members of each DUPLICATE pair; every A/b/Ea stays in mol-cm-s-cal and the engine converts + announces — zero hand-converted numbers).  Replaces the June SYNTHETIC placeholder rates — the sign-off blocker dissolves.  **τ_ign ≈ 16 µs (max dT/dt), T_final = 2785 K**; needs the case-local NASA-7 Cp overrides (the 1500 K catalogue poly3 fits made reverse rates explode: λ_max 1e21 → 1e18 real).  Rosenbrock23 integrates it clean (0 give-ups); the `_rk4` twin (`.expect-nonconvergence`) blows up to NaN at t=0 with the same deltaT — the stiffness lesson, now on real chemistry (λ~1e15: explicit-stable step ~1e-15 s vs deltaT 1e-6). |
| `gibbs06_h2_flame_radicals` | **THE RADICAL DATABASE AT WORK** — stoichiometric H₂/air adiabatic flame by Gibbs minimisation over the full radical pool (H, O, OH, HO2, H2O2 + equilibrium NO; GRI-Mech 3.0 NASA-7 thermo, 17 new species imported via `bin/import_nasa.py --thermo`).  **T_ad = 2385.8 K vs literature 2380-2390 K** (Glassman/Law); equilibrium OH 0.55 %/H 0.18 %/O 0.06 %/NO 0.27 % all inside published ranges — NO is the Zeldovich ceiling.  Two lessons golden-locked in the header: (1) the radicals' work — without them the same reactor overshoots to ~2640 K (dissociation soaks ~250 K); (2) **the extrapolation trap** — the catalogue's 250-1500 K poly3 Cp fits go WRONG at flame T (N2's cubic turns NEGATIVE, −33 J/mol/K at 2641 K), so the case carries NASA-7 two-range overrides (`model NASA7`, valid 200-3500 K) for the four stable species; remove them to SEE the 250 K error. |
| `enrtl_multisalt_nacl_kcl` | **Runs under `choupoProps`.  THE MULTI-SALT eNRTL** — the multicomponent symmetric electrolyte NRTL (Song & Chen 2009, DOI 10.1021/ie9004578) on the paper's own demonstration: NaCl + KCl sharing water at constant 4 mol/kg (the classic Harned experiment), γ± of BOTH salts as KCl replaces NaCl.  Golden-locked: the **exact single-salt reduction pin** (pure endpoints reproduce the validated ENRTLSingleSalt kernel at ~1e-15); **Harned's rule EMERGES** (ln γ± nearly linear in salt fraction, R² > 0.999, zero electrolyte-electrolyte parameters); the paper's **Figure-1 τ fan reproduced TO THE PIXEL** with the exact Chen & Evans 1986 Table 1 parameters (case-local overlay, cited): all anchors ≤ 0.001 in log₁₀ — x=0 → −0.106 (fig −0.105), τ=0 → −0.216 (−0.215), τ=+1 flat with the same shallow dip, τ=−1 → −0.364 (−0.365); the figure's axis says "ln" but plots log₁₀ (documented).  γ_NaCl(4m pure) = 0.7831 = the H&W experimental 0.783.  The 1986 paper's own Table 2 validates τ_EE = 0 for NaCl-KCl (σ 0.019 % on Covington 1968 mixture data).  Standards K-Cl.dat = independent **REFIT to Hamer & Wu 1972** (AAD 0.27 %), landing within 0.2 % of the 1986 values — two roads, one answer.  The op PRINTS the declared Gibbs-Duhem inconsistency of the published multi-electrolyte expressions (FD oracle: ~1e-9 pure-salt vs ~0.5 mid-sweep), never hides it — **and ships the FIX side by side**: a second op with `formulation consistent;` (the refined-eNRTL idea, Bollas/Chen/Barton 2008, on the symmetric form) keeps the chain rule through the Y-dependent mixing rules, so its oracle closes (~1e-9) at EVERY composition and both reduce exactly to the single-salt kernel.  **THE EXPERIMENTAL VERDICT IS IN (Robinson 1961, isopiestic, DOI 10.1021/j100822a016)**: Harned coefficients at 4 m — α_NaCl exp +0.0243: published +0.0276 (+14 %) beats consistent +0.0173 (−29 %); α_KCl exp −0.0091: BOTH overstate (2.7×/2.1×). A single fitted τ_EE cannot close both (structural residual); Chen & Evans 1986's τ_EE=0 validation used mixture OSMOTIC data, where per-salt γ asymmetries cancel — the records agree. The op emits `harned_alpha_1/2` on the experimental convention, golden-locked.  **And the Bollas 2008 cross-check closes the loop** (their Figure 1 IS this system): their original-eNRTL panel matches our `published` to the pixel (τ fan solids at x=1: −0.216/−0.186/−0.157 vs read −0.215/−0.185/−0.15), and their refined panel shows the signature our `consistent` shows — the τ fan nearly collapses (exact derivatives are almost insensitive to τ_EE).  Sibling refinements of sibling Gex surfaces, same physics.  Kernel: `thermo/electrolyte/ENRTLMultiSalt.H`. |
| `pitzer_nacl_sp77_hot` | **Runs under `choupoProps`. HOT BRINE — the NaCl Pitzer surface to 200 °C** on the FULL Silvester & Pitzer 1977 temperature functions (J. Phys. Chem. 81, 1822: eqs 30-32, Table IV coefficients VERBATIM in the standards Na-Cl.dat, applied as a deviation from the validated 25 °C surface), with **A_phi(T) curated against the same paper's Table II** (quintic pinned to exactly 1 at 25 °C, max dev 0.17 % over 0-300 °C — the old closed form drifted +67 % at 300 °C).  Three ops (100/150/200 °C), each validating γ± at the SATURATION molality against the paper's own Table V: **AAD 0.29 / 0.04 / 0.43 %** — even at the declared m-extrapolation edge (m_satd 6.7-8.0).  The op announces T-validity; `temperature` is now a first-class pitzerActivity key.  Cross-check bonus: SP77's dβ⁰/dT at 25 °C (7.15e-4) agrees with the independent Parker calorimetric fit (7.58e-4) to 6 %.  Ripples, all re-recorded: model2 BPE 5.58→5.06 K (β⁰(T) peaks near 150 °C — a first derivative is not a function), NaOH L_φ AAD *improves* 4.97→4.76 %, K-Cl/Na-OH slots refit with the new A_phi. |
| `nh3_equilibrium_map` | **Runs under `choupoProps`. THE HABER MAP** — the forum-ratified Gibbs equilibrium map (design: docs/design/gibbs-map-forum-2026-07-02.md) on its canonical pilot: stoichiometric N₂+3H₂, equilibrium NH₃ mole fraction over 300-700 °C × 1-300 bar (25×25 = 625 element-potential Gibbs solves, **0 unconverged**).  New op `gibbsMap`: metrics restricted to moleFraction/elementYield (banned: produced-species conversion, extents), deltaT as a CSV **column**, unconverged cells emitted as NaN (the GUI must hatch, never interpolate), anchor KPIs embed the approach in their names.  Engine: `temperatureApproach` on the gibbsReactor + gibbsMap — REACTION equilibrium at T+ΔT, enthalpy/Psat/energy-balance at the physical T (the chair's convention), announced loud with the global-only limitation and the high-P fugacity confounding.  **Pins: 400 °C/10 atm → 4.09 % vs Larson & Dodge (1923) 3.85 %; 450 °C/10 atm → 2.22 % vs 2.11 %** (+5-6 %, consistent with JANAF ΔGf uncertainty amplified through Kp; ideal-gas validity declared ≤50 atm). |
| `pitzer_gamma_hamer_wu` | **Runs under `choupoProps`.  The BREADTH validation of the single-salt Pitzer catalogue** — six 1-1 salts' γ± and osmotic φ against the NBS critically-evaluated tables (Hamer & Wu, J. Phys. Chem. Ref. Data 1 (1972) 1047), ZERO refitting: the frozen Appelo/PHREEQC-lineage pairs vs 46 evaluated points, 0.1–6 mol/kg.  γ± AAD: **NaCl 0.11 % · KCl 0.13 % · KBr 0.23 % · HCl 0.37 % · NaOH 0.65 %** (independent validation, none of these pairs ever saw this data); φ AADs all ≤ 0.5 %.  The honest sixth: **LiCl 2.93 % — the WORST, on the salt the fit actually saw** — its pair was refit to this very table PLUS the Farelo saturation anchor at 19 mol/kg, and 3 Pitzer parameters cannot hold the most non-ideal 1-1 salt (γ± reaches 2.7 at 6 m) across both anchors; the case header spells out why that is a fit-consistency price, not validation.  Exercises the `validation{}` gamma/phi extension of `pitzerActivity` (dataset = case-local `constant/experiments/hamer_wu_1972.dat`, cited, 46 points); AADs golden-locked. |
| `pitzer_seawater_verify` | **Runs under `choupoProps`.**  The TIER-2 verification weapon for the multi-ion Pitzer-HMW model (binaries + ternary theta/psi mixing, slice S3).  Speciates a standard synthetic seawater (Na 0.4861, Mg 0.0547, Ca 0.01065, K 0.01058, Cl 0.5658, SO₄ 0.02926 mol/kg, I ≈ 0.72, major ions fully dissociated — the honest non-pairing HMW treatment) with `activityModel pitzer;` and pins the per-ion + mean activity coefficients against the PUBLISHED seawater-Pitzer range: γ±(NaCl) 0.671, γ±(KCl) 0.645, γ±(MgCl₂) 0.485, γ±(CaCl₂) 0.469, γ±(Na₂SO₄) 0.373, γ±(MgSO₄) **0.170** (the famously-low 2:2 value) — all inside HMW (1984) / Pitzer (1991).  A second op is the single-salt reduction pin: NaCl-only (every ternary sum empty) gives γ±(NaCl,1m)=0.6572, and the internal oracle (`PitzerHMW::verify`) stays at max-rel-dev 1.57e-14 over all 54 binaries AFTER the ternary wiring (ternaries provably vanish for a single salt).  Announces the **constant-theta** approximation (E_theta higher-order mixing deferred to v2).  Self-contained snapshot in `constant/electrolyte/`; mixing params Harvie-Moller-Weare 1984 via `bin/curate/parse_mixing.py`. |
| `pitzer_hmw_hot_agreement` | **Runs under `choupoProps`.**  The engine-agreement pin of the Pitzer-T fix: the multi-ion HMW speciation engine and the single-salt kernel evaluate the SAME NaCl 4 mol/kg state at 100 °C, both reading the same Na-Cl.dat record (25 °C virials + full Silvester-Pitzer 1977 T-functions + the same Aφ(T)).  For a lone 1:1 salt they must agree — and do, to 7 significant figures (γ± = 0.7309385).  Before the fix the HMW froze its virials at 25 °C and disagreed in the second decimal; this golden forbids the divergence forever. |
| `pitzer_calcite_brine` | **Runs under `choupoProps`.**  The slice-S4 GATE: the Pitzer-HMW **carbonate system** on surface seawater (the S3 major ions + DIC ≈ 2.3 mmol/kg as HCO₃⁻, pH 8.2, I ≈ 0.68).  Carbonate anions (CO₃, HCO₃) join the theta/psi ternary sums, and the **neutral CO₂(aq) gets γ > 1 via the lambda salting-out term** (`mixing.dat`, imported by `bin/curate/parse_mixing.py`).  Pins **SI_calcite = +0.724** against the published surface-ocean band (Ω_cal ≈ 4–6, SI ≈ +0.6–0.8; Mucci 1983, Millero 2007) and SI_gypsum = −0.875 (correctly far undersaturated).  A second op runs the SAME water under **Davies** to expose the divergence (SI_calcite +0.882, γ_CO₂aq 1.000 — Davies overshoots scaling and misses the salting-out): **Pitzer is the rigorous tool for brine**.  Single-salt oracle unchanged at 1.5658e-14.  Self-contained `constant/electrolyte/` snapshot. |
| `pitzer_vs_davies_ro_concentrate` | **Runs under `choupoProps`. THE FLAGSHIP** of the Pitzer×speciation feature (slice S6) — the payoff that makes the multi-ion Pitzer-HMW build worth it.  ONE feed (standard surface seawater: Na 0.4861, Mg 0.0547, Ca 0.01065, K 0.01058, Cl 0.5658, SO₄ 0.02926, HCO₃ 0.0024 mol/kg, I ≈ 0.68), TWO activity models, the engineering answer FORKS.  Two `scalingScan` ops (`activityModel davies;` vs `activityModel pitzer;`) concentrate the SAME water over recovery r = 0→0.80 (I climbs ≈ 0.68 → 3.3 mol/kg), pH solved per point.  **The headline:** Davies predicts gypsum scaling onset (SI_gypsum = 0) at **r = 0.58** and gypsum precipitates from r = 0.60; **Pitzer keeps SI_gypsum below 0 all the way to r = 0.80 (−0.14 at r_max) — no gypsum scaling at all.**  KPI `recovery_at_gypsum_onset` = 0.580 for Davies, ABSENT for Pitzer (it never crosses).  **The mechanism plot** (new `gamma_<ion>` columns via `diagSpecies ( Ca SO4 );`): Davies' charge-only γ_Ca = γ_SO4 runs away to 3.65 at high I, while Pitzer gives per-identity γ_Ca = 0.38 / γ_SO4 = 0.05 (the strong Ca–SO₄ 2:2 virial β₂ = −59.3 Davies cannot see) — the divergent γ product is exactly why the SI forks.  Davies is announced-untrustworthy past I ≈ 0.5; Pitzer (valid to I ≈ 6 for seawater) is the quantitative answer, credentialed by `pitzer_seawater_verify` + `pitzer_calcite_brine`.  Both models converge cleanly to r = 0.80 (≤ 7 γ-passes; NO convergence hardening needed).  At verbosity ≥ 3 the Pitzer **parameter dump** lists every active β/θ/ψ/λ for the present ion set — the glass-box anchor.  Single-salt oracle unchanged at 1.5658e-14.  Self-contained `constant/electrolyte/` snapshot. |
| `rankine02_water` | The REAL closed Rankine steam cycle on IAPWS-IF97 — pump → boiler → turbine → condenser → recycle, pure water with `pureFluids { water { method IF97; } }`.  The `boiler`/`condenser` are the new `phaseChanger` unit crossing the saturation dome (`outletState superheatedVapour` / `saturatedLiquid`); the turbine runs isentropically on IF97 (H_real/S_real now routed).  First-law closure \|ΣQ + ΣW\|/Q_boiler = 4.24e-12; η ≈ 18.5 %; condenser/boiler Tsat match the steam tables.  This is what `rankine01_water` (kept as the ideal-gas contrast) admits it cannot do — ideal-gas water has no two-phase dome. |
| `phasechange01_partial_condenser` | The generic-VLE backend of the `phaseChanger`: a benzene/toluene vapour through a `condenser` with `outletQuality 0.4;` → condense 60 %, the outlet landing INSIDE the dome (vf = 0.40, T = 367.5 K from the Rachford-Rice flash, Q = −630 kW).  The partial-condensation lesson + proof the same `phaseChanger` dict works on a multicomponent mixture as on IF97 pure water. |
| `condenser01_film_nusselt` | The `phaseChanger` (condenser) **`model geometry;`** path — the SEVENTH resolution, where the **duty EMERGES** from the condensing film instead of being specified.  Saturated steam at 1 bar through a horizontal-tube bundle cooled by a cooling-water film: a **Nusselt (1916) laminar film** coefficient h_cond = 0.729[k_l³ρ_l(ρ_l−ρ_v)g·h_fg′/(μ_l D dT_film)]^¼ (h_fg′ = h_fg + 0.68 cp_l dT_film, Rohsenow subcooling correction) is solved together with the wall + cooling-water resistances via a 1-D wall-energy balance (q_cond == q_cool, the wall T the unknown — bracketed Newton).  IF97 supplies Tsat, h_fg and the saturated-liquid transport (ρ_l, μ_l, λ_l).  Converges to T_wall ≈ 323.6 K, dT_film ≈ 49 K, h_cond ≈ 2850 W/(m²·K), Re_film ≈ 15.5 (laminar), the **condensing film controlling**; duty Q ≈ −528 kW with the **triple cross-check** agreeing (h·A·dT_film == feed enthalpy drop == U·A·ΔT to ~0.02 %); outlet wet vf ≈ 0.53.  v1 is FILM CONDENSATION only — a `boiling{}` block errors loudly (pool/flow boiling deferred to v2).  Reference: Incropera & DeWitt Ch. 10 (Ex. 10.3/10.4); the `NusseltFilm` h̄ is pinned in the `heatTransferBench` vs the textbook values (AAD ≤ 15 %, honest for laminar film theory).  Self-contained (own `constant/components/water.dat`). |
| `hxWorkflow1_design_from_duty` | **HX DESIGN WORKFLOW, Part 1 of 2 (the standard design sequence).** **DESIGN mode (`model design;`) — detailed sizing, glass-box.**  The inverse of `model geometry;`: instead of GIVING the tube count + shell diameter, you give the **duty target** (cool the hot shell 330→315 K) and the engine SOLVES for the geometry.  Glass-box Kern procedure: fix the tube choices (OD/ID/length/pitch/passes/material), compute Q_req + LMTD from the target by energy balance, then **bisect the tube count** so U·A = Q_req/LMTD, with the shell diameter from the Kern/Sinnott bundle correlation (the tube pattern — triangular/square — is specified and drives both D_e and the bundle constants).  It then rates the designed bundle exactly like a given one (same Gnielinski + Kern) and reports U, area, ΔP.  **Sizes 105 tubes (triangular pitch, 2-pass U-tube), shell ID 0.306 m; rated U = 4133 W/(m²·K); delivered hot outlet 315 K = the target** — the design loop sizes N by the DELIVERED DUTY via the eps-NTU (incl. the 1-shell/2-tube-pass 1-2 penalty, F<1), NOT merely U·A = Q/LMTD, so a 2-pass U-tube actually reaches the target (U·A-only would under-size it) (the eps-NTU on the designed geometry reproduces the duty).  This is the workflow that matches industrial practice: converge the balance with a duty target, THEN design the exchanger — you do NOT invent the geometry up front.  v1: single-phase, F_correction = 1.  Contrast `heatExchanger02_geometry_U` (the RATING inverse: geometry → U). |
| `hxWorkflow2_rate_designed` | **HX DESIGN WORKFLOW, Part 2 of 2 — RATING the designed bundle (detailed rating).**  Takes the geometry Part 1 SIZED (105 tubes, 0.306 m shell, 2-pass U-tube) as a FIXED given (`model geometry;`) and rates it: **delivers hot outlet 315.0 K = the Part-1 target** (a touch over-surfaced — Part 1 rounds the tube count UP, honest margin), U = 4133 W/(m²·K).  The verification a real project always does after sizing.  Header invites the **off-design study**: drop the cold-tube flow and re-run — less coolant → the hot stream leaves warmer, shell ΔP falls; the rating question ("will it cope on turndown?") the design step cannot answer.  Deep theory: theoryGuide §sec:hx-design. |
| `heatExchanger02_geometry_U` | The `heatExchanger` **`model geometry;`** path: the overall U and area are COMPUTED from the tube-bundle geometry + per-side convective correlations, not specified (the glass-box answer to "where does U come from?").  Water-water clean shell-and-tube: tube-side h_i via **Gnielinski** (Incropera & DeWitt 6th ed.), shell-side h_o via the **Kern** method (Process Heat Transfer 1950) on the bundle equivalent diameter D_e and crossflow mass flux G_s; IF97 water rho/mu/lambda at each stream T.  U = 1/(1/h_o + r_o ln(r_o/r_i)/k_wall + r_o/(r_i h_i)) = **2470 W/(m²·K)** (in the textbook clean water-water band ~2000–3000), area 46.6 m², tube-side controlling; the computed U+area then feed the *unchanged* eps-NTU duty (Q = 1913 kW, U·A·LMTD cross-check exact).  The mandatory SEE prints Re/Pr/Nu/h per side with validity ticks + the resistance split.  **PRESSURE DROP now computed too (Kern, both sides):** tube f = 0.079 Re^-0.25 (Blasius) → ΔP = (4 f L n_p/d_i + 4 n_p)(ρu²/2) = 9.2 kPa; shell f = exp(0.576 − 0.19 ln Re_s) over 15 baffles → ΔP = 35 kPa (shell > tube — baffled crossflow dissipates more; the duty-vs-pumping trade-off, not just the area).  Contrast `heatExchanger01_water_water` (the default `model epsNTU;` U-spec rating). |
| `compare_transport_water` | **Runs under `choupoProps`.**  Water transport computed TWO WAYS on the same water over T = 280–370 K (saturated liquid), so the curves overlay and the gap is visible: (a) the IAPWS reference via `pureFluids { water { method IF97; } }` → R12-08 viscosity + R15-11 conductivity, vs (b) the generic correlations (Andrade viscosity, SatoRiedel conductivity) via a per-op `transport {}` override — the SAME property key (`viscosity_liquid` / `thermal_conductivity_liquid`) emitted both ways.  **The lesson:** Andrade tracks IAPWS within a few % (≈7 % @ 280 K, ≈2–4 % mid-range, ≈3 % @ 370 K) — fine for a Reynolds number, not for a precision steam duty; SatoRiedel FAILS for water (≈50–60 % under-prediction — water is the textbook SatoRiedel failure), so for water conductivity use IAPWS R15-11.  Generic = cheap + general (any compound), IAPWS = exact but water-only.  CSVs `mu_iapws/mu_generic/k_iapws/k_generic.csv` overlay via the existing compare machinery.  Sibling of `compare_visc_liquid_water` (which contrasts two GENERIC viscosity fits, Andrade vs Vogel). |
| `psa01_h2_psa` | Equilibrium pressure-swing adsorption for H₂ purification (the STEADY twin-bed abstraction: one column adsorbs while the other regenerates — steady = the time-average of the pair).  Competitive extended-Langmuir loadings on activated carbon at P_high/P_low from the pair catalogue (`parameters/adsorption/equilibria/`), split by working capacity Δq; a species with no record adsorbs nothing and reports to the raffinate, announced.  Since the A1 migration the unit DELEGATES to the `IsothermModel` factory — the golden is byte-identical across that refactor by construction. |
| `tsa01_co2_twin_bed` | Cycle-averaged twin-bed TSA for removing 1 mol% CO₂ from He.  The same feed partial pressures are evaluated at 298/398 K to obtain the Langmuir working capacity; the case closes captured inventory over a 300 s switch interval, includes product purge, and reports gross sensible + desorption regeneration heat together with the equal adsorption/cooldown duty.  Its case-local adsorbent is explicitly a synthetic teaching anchor, not design data. |
| `tsa02_refused_capacity` | **Exits non-zero on purpose.**  The same TSA feed with insufficient adsorbent exceeds the declared working capacity.  The unit refuses the case with the required and available mol/cycle instead of silently clipping capture or inventing breakthrough behaviour. |
| `sprayDryer01_sugar` | Rotary-atomiser spray dryer (40 wt% sugar solution → powder): SOLIDS-stream PSD + the transport-layer Ranz-Marshall drying coefficients (h, k_c from gas μ/k/D).  **Slice-4 note:** the atomiser surface tension σ is no longer a hardcoded 0.072 N/m — it now comes from the property layer via the honest cascade (explicit `operation.surfaceTension` knob → else `thermo.surfaceTension(T_feed, x_liq)` → else the 0.072 fallback), here `transport { surfaceTension { model BrockBird; } }` giving σ(T) of water (Brock-Bird ≈ 0.107 N/m, which overpredicts H-bonding water — the σ source is announced in the report).  d32 shifts 58.1 → 60.4 µm (σ enters the Friedman correlation to the 0.1 power); the golden was deliberately re-recorded.  A spray-dryer case WITHOUT a `surfaceTension` model (e.g. `sprayDryer02_residence_sweep`) falls to 0.072 and stays byte-stable. |
| `batch01_first_order` | **Runs under `choupoBatch`** (not `choupoSolve`).  Isothermal batch reactor: ethanol → water Arrhenius first-order at 350 K.  RK4 integration with dt = 1 s for 600 s; trajectory CSV written every 30 s.  Final conversion X = 0.883 matches the analytical exp(−kt) (k ≈ 3.58e-3 s⁻¹). |
| `batch02_adiabatic` | **Runs under `choupoBatch`.**  Adiabatic exothermic batch (mode `adiabatic`, dH_rxn = −15 kJ/mol).  Cold start at 320 K (k₀ ≈ 3.7×10⁻⁴ s⁻¹, τ₀ ≈ 2700 s); slow induction lifts T over the first ~200 s, then thermal runaway carries T from 345 → 482 K between t = 230 s and t = 330 s, reaching full conversion + plateau.  Demonstrates the energy-balance branch. |
| `batch06_adaptive_runaway` | **Runs under `choupoBatch`.**  The SAME adiabatic esterification runaway as `batch02_adiabatic`, but with **OPT-IN adaptive time-stepping** (`timeStepping adaptive;`).  Like OpenFOAM's `adjustTimeStep`, but the control criterion is the integrator's LOCAL ERROR (rtol/atol), not a Courant number — there is no mesh.  The step starts LOW (`deltaT0 = 1 ms`) and the stiff Rosenbrock23 integrator GROWS it through the cold induction (toward `deltaTmax = 10 s`), then SHRINKS it (down to ~0.3 s) through the thermal runaway, then re-grows on the plateau.  At verbosity 3 the adaptive **step history is printed** (h doubling, then shrinking) — the student SEES h adapt.  Trajectory rows still land on the clean `writeInterval` grid (the integrator is clamped so it never overshoots a write time); the final state (T = 444.61 K, full conversion) agrees with a fine fixed-step RK4 run to ~4×10⁻⁷ relative. |
| `batch03_consecutive` | **Runs under `choupoBatch`.**  Isothermal series reaction compA → compB → compC at 350 K with k₁ = 10⁻² s⁻¹, k₂ = 2×10⁻³ s⁻¹.  Intermediate B peaks at n_B/n_A0 = 0.669 around t = 200 s, matching the analytical (k₁/k₂)^(k₂/(k₂−k₁)) and t_max = ln(k₁/k₂)/(k₁−k₂) to 0.1 %.  Demonstrates the multi-reaction branch of (`reactions (A_to_B  B_to_C );`). |
| `batch05_crystalliser` | **Runs under `choupoBatch`.**  Isothermal batch cooling-crystalliser: sucrose from water by the method-of-moments PBE (μ0..μ3 + dissolved solute; unseeded primary nucleation bootstraps from S₀ > 1; growth drains the solute as V·ρ_c·k_v·dμ3/dt).  The all-phase inventory hook keeps the campaign mass balance closed (crystal magma counted — without it crystallisation reads as mass loss).  Carries the latent energy-ledger duty Q = −ΔH_cryst·Δn_cryst (−2932 kJ over 3 h, golden-pinned), with ΔH_cryst = 4377.5 J/mol resolved through the SAME shared resolver as the steady crystalliser (solution tier: dHsoln + Cp-path correction); the vessel-H half stays a NAMED balance gap (solid-rung pricing pending). |
| `batch08_isothermal_duty` | **Runs under `choupoBatch`.**  The energy ledger's OVERRIDE branch: isothermal lumped A → B whose species carry NO formation data but whose reaction declares `dH_rxn = −50 kJ/mol`.  The jacket duty is priced per segment from the reaction EXTENT solved out of the committed mole numbers (`E = ΔH·Δξ = −49.876 kJ` over 600 s, matching the analytic first-order decay to the digit), with the "dict OVERRIDE — no formation datum" announcement visible in the log.  The campaign energy balance stays UNAVAILABLE naming compA/compB: a declared reaction heat prices the duty but cannot place vessel inventories on the elements datum.  Contrast batch01 (formation branch: `Q = ΔH = Σ Δn_i·h_i(T)`, balance closes to 0) and batch03 (neither datum nor declaration → duty record invalid, reactions named). |
| `batch09_adsorber_co2` | **Runs under `choupoBatch`.**  The A2 anchor: closed isothermal CO₂ uptake on zeolite 13X (`batchAdsorber`, LDF solid film `dq/dt = k(q* − q)`, k = EQUIPMENT data in the case dict).  The vessel relaxes to the INVENTORY equilibrium; the golden pins the independently-solved root p_∞ = 7229.128 Pa / q_∞ = 1.970838 mol/kg (spec pins, not run ulps), gas+solid conservation `n + m_ads·q = const` at machine level, and the isosteric heat released (−88.7 kJ).  The RK4 stability bound is printed at start-up. |
| `batch10_adsorber_co2_n2` | **Runs under `choupoBatch`.**  Competitive CO₂/N₂ uptake on 13X via the extended-Langmuir mixing rule: site competition gives q_CO2 = 0.49350 / q_N2 = 0.77282 mol/kg and equilibrium selectivity S = 71.43 — pinned against the independent binary Newton of the A2 spec.  The multicomponent gate is the final inventory + conservation, not a trajectory (no closed form exists — the adsorption contract §8.3 says so). |
| `batch11_adsorber_henry` | **Runs under `choupoBatch`.**  The CLOSED-FORM gate: in the Henry limit the coupled gas+solid vessel is LINEAR, q(t) = q_∞(1 − e^(−k_eff·t)) with k_eff = 0.0995791 1/s, and the run must match the analytic solution to 1e-8 (trajectory rows land exactly on the analytic checkpoints).  Golden pins q(60 s) = 0.4966211107 at 1e-6. |
| `batch12_adsorber_refused_P` | **Runs under `choupoBatch`**, `.expect-nonconvergence`.  The REFUSAL lesson: a declared `initial.P` (10 bar) contradicting n·R·T/V (4.958 bar) FATALs on the honest cross-check — the pressure of a closed ideal-gas headspace is a RESULT, not an input (the duty-DOF credo, adsorbed). |
| `batch13_breakthrough_co2` | **Runs under `choupoBatch`.**  THE A3 anchor: 15 % CO₂/He breakthrough on a 0.5 m zeolite-13X fixed bed (`fixedBedAdsorber` — conservative FV, nCells 100 DECLARED, Danckwerts inlet/outlet, Pe = 250).  The engine announces the front BEFORE integrating: R_f = ε + ρ_b·q*/c_in = 304.05, t_st = (L/u)·R_f = 3040.51 s; the run reproduces t_b(5 %) = 2890.67 s, t_50 = 3036.45 s and the telescopic integral anchor ∫(1 − c_out/c_in)dt = t_st.  The DECLARED constant-(u,P,T) closure FABRICATES carrier He at exactly the net uptake rate (9.19 mol) — announced in the run header, pinned as KPIs (`carrier_fabricated_mol`, `physical_mass_closure_rel` = 0.108) and discounted: closure vs the DECLARED residual sits at 1e-14.  Intrinsically stiff (|λ| = 89 1/s, t_st·|λ| ≈ 2.7×10⁵): adaptive Rosenbrock23; `timeStepping fixed` REFUSES any deltaT above the printed Gershgorin bound. |
| `batch16_ergun_profile` | **A4 F1.** Pure-He inert bed with case-local μ, particle diameter and sphericity. The integrated pressure profile matches the closed-form compressible Ergun solution `P²(z)` at all three pinned cell locations to below 1e-8 relative; viscous and inertial terms are both active. |
| `batch17_dilute_ergun_limit` | **A4 F2.** Side-by-side A3 constant-velocity and A4 Ergun beds at 400 ppm CO₂. The 5% breakthrough times differ by only 0.031%, demonstrating the declared dilute limit while the A4 carrier residual closes at machine precision. |
| `batch18_ergun_conservation` | **A4 F3.** The 15% CO₂ case with every gas species integrated and face velocity obtained from Ergun. At the same N=25 reference mesh, t_b5 moves later than the A3 mesh anchor (2767.87 vs 2732.03 s), the campaign balance closes near 1e-14, and `declaredMaterialResidual()` is empty. |
| `batch14_transport_only` | **Runs under `choupoBatch`.**  The genuinely CLEAN transport diagnostic: k_LDF = 0 DECLARED on the batch13 bed at N = 200 — a tracer front arriving at ε·L/u = 4.0 s, t_50 pinned against the Ogata-Banks analytic ADE solution and the conservative-FV identity ∫(1 − c_out/c_in)dt = 4.0 s to 1e-11 (zero fabricated carrier, physical closure exact).  Runs FIXED RK4 on purpose: the Gershgorin criterion is printed term by term (advection 100 + dispersion 160 + LDF 0 1/s → dt ≤ 0.005346 s) and a deltaT above the bound is refused — the CFL lesson without a mesh Courant number. |
| `batch15_mesh_study` | **Runs under `choupoBatch`.**  The batch13 breakthrough on the COARSEST mesh of the 25/50/100/200 family (nCells 25), publishing the full mesh study in `reports/meshStudy.md`: t_b(5 %) converges monotonically 2732 → 2839 → 2891 → 2916 s with observed order 1.03–1.05 (first-order upwind) + the Richardson extrapolation, while the integral anchor stays on t_st at every N (conservation is mesh-independent; front SHAPE is not).  N = 100 is the batch13 production choice — justified by this study, not asserted. |
| `still01_benzene_toluene` | **Runs under `choupoBatch`.**  Rayleigh batch distillation, ideal Raoult.  1 mol of equimolar benzene/toluene at 1 atm, F_vap = 1.5×10⁻⁶ kmol/s for 600 s → 90 % distilled, residue x_benzene = 0.082 matches the closed-form Rayleigh integral with α ≈ 2.4 to ~2 %.  Carries the pot's energy-ledger duties: reboiler +30.3 kJ (first law over the pot, package-by-package vapour enthalpy) and condenser −29.4 kJ (golden-pinned). |
| `still02_ethanol_water_azeo` | **Runs under `choupoBatch`.**  Rayleigh distillation with NRTL ethanol/water, x_eth_0 = 0.40 (well below the 0.894 azeotrope).  Same F_vap and endTime as still01; the pot enriches monotonically in water and ends near-pure water (x_eth ≈ 3×10⁻¹¹), T → 372.4 K (1 K below Tb_water).  Demonstrates that a Rayleigh still cannot cross the azeotrope from below: the pot trajectory moves AWAY from the azeotrope. |
| `still03_with_receiver` | **Runs under `choupoBatch`.**  still01's Rayleigh pot distilling INTO a receiver: `dischargeTo receiver;` routes the condensed distillate step-by-step into a passive `batchAccumulator`.  Pot + receiver conserve the initial 1 mol at all times; the receiver is benzene-rich early and its cumulative composition drifts toward 0.5 as the pot empties. |
| `still04_rectifier_benzene_toluene` | **Runs under `choupoBatch`.**  Classical batch rectification (`model rectifier;`): 3 ideal stages + total condenser at constant R = 3 on the still01 pot.  F_vap is the internal boilup V; product D = V/(R+1); pot loses D·x_D only.  x_D starts 0.949 (vs 0.71 raw pot vapour) and drifts to 0.906 — the constant-R purity trade; receiver accumulates 0.931.  nStages 0 + R 0 reproduces still01 to the last digit; R = 200 lands inside the independent Fenske band α^(N+1). |
| `still05_rectifier_constant_xD` | **Runs under `choupoBatch`.**  The constant-composition policy on still04's column: R(t) solved each step (bracketed over [0, refluxMax] on a feasibility map) to hold x_D[benzene] = 0.93.  x_D pinned to ±1e-4 while R climbs 2.0→7.9; at t = 863 s the ceiling can no longer reach the target → `onRefluxLimit stopDistillation` (D = 0, pot inventory frozen, event visible in trajectory + KPIs).  Receiver ends with the whole cut on-spec at 0.930.  Distinct refusals for target-below-floor, unreachable-at-refluxMax, and the ethanol/water azeotrope barrier (computed VLE). |
| `still06_ledger_mixed_validity` | **Runs under `choupoBatch`.**  The adversarial case of the material ledger's enthalpy-validity contract.  Two stills poison their distillate records in OPPOSITE temporal orders: potA starts with 10 % `compA` (a lumped species with no formation datum) and is dumped + recharged clean at t = 200 s (unpriceable → priceable packages on ONE continuous record); potB starts clean and is spiked with pure compA at t = 200 s (the reverse).  Both records must end H-invalid naming `compA`, with NO partial integral emitted — validity is monotonic, a later priceable package never resurrects a poisoned record.  Golden pins the campaign census `transfers_H_valid` = 1 / `transfers_H_invalid` = 4; mass still closes to 10⁻¹⁶. |
| `recipe01_react_then_distill` | **Runs under `choupoBatch`.**  Two-vessel batch recipe.  Reactor: ethanol → water Arrhenius first-order at 350 K, 76 % conversion at t = 400 s.  Recipe `transfer` event at t = 400 s moves the entire reactor contents into an initially-empty still.  Still then distils at F_vap = 2×10⁻⁵ kmol/s for 800 s, vaporising ~85 % of the charge; final residue is water-rich (x_eth = 3.7 %), T → 371 K.  Single recipe action covers both inter-unit material flow and "vessel starts empty" handling.  Since the energy ledger landed this is the END-TO-END showcase: reactor duty + H-equality charge + still reboiler/condenser + the unrouted distillate close the campaign energy balance to 6×10⁻¹⁶ (golden-pinned). |
| `recipe02_setpoint_ramp` | **Runs under `choupoBatch`.**  Single reactor with a staged temperature programme via two `setParameter` events: 320 K → 350 K @ t = 200 s, 350 K → 380 K @ t = 500 s.  Reaction is the same ethanol → water Arrhenius first-order, giving k₃₂₀ ≈ 3.7×10⁻⁴ / k₃₅₀ ≈ 3.6×10⁻³ / k₃₈₀ ≈ 2.5×10⁻² s⁻¹.  Trajectory shows three regimes (slow / moderate / fast); final conversion ~100 % at t = 900 s.  Cleanest test of the recipe layer without inter-unit transfers.  The energy ledger reads the programme as its full story: 3 reaction segments (−17.4/−145.3/−74.6 kJ, each at its held T) with 2 `impulse` records for the jumps (+65.5/+67.4 kJ, priced as H(n,T_new)−H(n,T_old) state differences, never Σn·Cp·ΔT), and the campaign balance closes across all five at 2×10⁻¹⁵ (golden-pinned). |
| `recipe04_charge_enthalpy` | **Runs under `choupoBatch`.**  Mixing is a FIRST-LAW act: 0.5 mol of ethanol at 340 K transferred into 1.0 mol of water at 300 K.  The charge solves T_mix from H-equality on the elements datum — `T_mix = 317.06 K`, ~4 K ABOVE the equal-Cp molar average (313.3 K), because Cp_ethanol ≈ 112 vs Cp_water ≈ 75 J/(mol·K).  With both species priced, no jacket and nothing external, the campaign energy balance is AVAILABLE and closes at 2×10⁻¹² (scaled by the 273 kJ that MOVED, not the ~0 signed sum) — the first campaign where a transfer survives inside a CLOSED balance.  A species without a formation datum would flip the charge to the announced molar-average fallback and the balance to unavailable, NAMED. |
| `ctrl01_cstr_temp_control` | **Runs under `choupoCtrl`.**  DynamicCSTR with continuous compA → compB Arrhenius reaction (mild exotherm, ΔH = −50 kJ/mol).  Cool feed (T_in = 320 K, F_in = 5×10⁻⁵ kmol/s into a 1 L reactor); without the jacket controller the natural steady state is T ≈ 320.8 K.  A PID writes T_jacket to drive T to a setpoint of 350 K.  Tuning Kp = 4.0, Ki = 0.04, Kd = 0.0 (proportional band ≈ 8 K, integral time ≈ 100 s); output clamped to [280, 420] K with bias 320 K; anti-windup keeps the integral from accumulating against the upper bound during the initial saturated phase.  Closed-loop tracks SP to within 10⁻⁴ K at t = 1500 s with the jacket held at ≈ 349.8 K (≈ 30 K of jacket bias above the cool feed). |
| `ctrl02_disturbance_rejection` | **Runs under `choupoCtrl`.**  Same DynamicCSTR + PID as ctrl01, plus a `Schedule` (open-loop) controller that injects step disturbances in T_in: 320 → 305 K at t = 700 s, then 305 → 335 K at t = 1300 s.  The PID has no foreknowledge of these disturbances --- it only sees T_reactor drift away from SP and reacts via the jacket.  Peak excursion after each step is ≈ 0.7 K; recovery to within 10⁻³ K of SP in ≈ 200 s.  Textbook disturbance-rejection demonstration without any new dynamic-unit code. |
| `ctrl06_sine_disturbance` | **Runs under `choupoCtrl`.**  The frequency-response seed.  Same DynamicCSTR + PID as ctrl02, but the disturbance is a `Signal` controller injecting a **sinusoid** into T_in: `T_in = 320 + 15·sin(2π·t/600)` K (mean 320 K, amplitude 15 K, period 600 s → f = 1.667×10⁻³ Hz, ω = 0.01047 rad/s).  The PID chases the moving target; after the first ~period the reactor settles into a **forced sinusoidal response** — same period, but strongly attenuated (the tight loop rejects nearly all of it: T_reactor peak-to-peak ≈ 3×10⁻³ K against the 30 K input swing, amplitude ratio ≈ −80 dB) and phase-lagged.  Sweep the `period` and re-run to trace one Bode point at a time — the closed-loop frequency response built by trial-and-error.  `Schedule` (the staircase, ctrl02) and `Signal` are both first-class; the staircase delegates to the same `staircase` signal so legacy cases stay byte-identical. |
| `ctrl08_prbs_ident` | **Runs under `choupoCtrl`.**  The classic system-identification input: a **PRBS** on T_in (320 ± 10 K) from a maximal-length Fibonacci LFSR — n = 4 registers, canonical taps (4 3) from the versioned `prbsTaps-1` table, DECLARED seed 1, period 2⁴−1 = 15 bits × 120 s.  The hand-derived sequence `000100110101111` is in the case header AND printed by the announce line — walk the LFSR yourself and compare.  Deterministic by construction (never runtime randomness): seed + taps reproduce the experiment bit for bit.  The initialiser VERIFIES the cycle length is exactly 2ⁿ−1 (taps `(4 2)` refuse aloud: cycle 6, a non-primitive polynomial) and refuses seed 0 (the LFSR fixed point).  Unlike ctrl06's single-frequency sine, the PRBS is broadband — one record carries a full low-order model fit. |
| `ctrl09_stream_disturbance` | **Runs under `choupoCtrl`.**  The INLET-FIELD actuator: `actuator { unit reactor; inletField componentMolarFlow.compA; }` steps compA's molar flow 3.5→5.25×10⁻⁵ kmol/s at t = 600 s while compB's flow stays UNTOUCHED.  The inlet's authoritative state is the per-component flow vector, so F and z change by DERIVED arithmetic — golden-pinned to the hand values F = 6.75×10⁻⁵, z_A = 7/9, z_B = 2/9.  The sibling `moleFraction.<c>` semantics instead holds F and renormalises the others proportionally (validated [0,1], degenerate case refused, machine-precision sum check); both semantics announced at start-up.  `F_in` and `z_in_*` are first-class trajectory columns. |
| `ctrl03_adaptive_disturbance` | **Runs under `choupoCtrl`.**  The SAME PID disturbance-rejection loop as `ctrl02`, but with **OPT-IN adaptive plant integration** (`timeStepping adaptive;`).  Demonstrates THE CONTROL SUBTLETY: the digital PID samples at the FIXED `deltaT = 1 s` grid (the sampler must NOT adapt), while BETWEEN samples — with the manipulated variable (jacket T) HELD — the stiff Rosenbrock23 integrator sub-steps the plant ODE with the step set by local error.  Two Δt, both correct.  Writes land on the clean 20 s grid; the closed-loop response matches the fixed-RK4 `ctrl02` sibling to ~7 significant figures (final T = 349.981 K, MV = 347.881 K), confirming adaptive stepping changes the integration cost, not the control. |

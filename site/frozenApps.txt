#  THE FROZEN APP COPIES THAT ACTUALLY EXIST, declared.
#
#  A release page offers "Run Choupo-YYMM" at /vYYMM/app/ -- the URL a student
#  cites, teaches from and archives.  NOTHING in bin/buildSite produces that
#  tree: it is a hand-uploaded archive living in the SITE repository
#  (choupo-admin/choupo-admin.github.io), which this repository cannot see.
#
#  On 2026-09-02 that gap became a live 404 on the homepage's own release
#  button: the label was renamed 2607 -> 2608 and the copy behind it had never
#  been uploaded.  The link was correct-looking, the gate said in writing that
#  it could not check it, and it shipped anyway.
#
#  So the fact is DECLARED here instead of remembered.  One release id per
#  line.  check_release_identity arm (f) refuses any /vYYMM/app/ link in the
#  site sources whose id is not listed, and refuses a listed id that has no
#  release artefact.  Upload the copy, add the line, and the button may exist
#  -- in that order.
#
#  A line here is a claim that the copy EXISTS, and the workflow that builds
#  it (.github/workflows/freeze-app.yml) is what makes the claim true: it
#  builds from the tag with --base=/vYYMM/app/ and refuses to publish unless
#  the engine compiled into the WASM states that release's own version.
#
#  THE LIST IS EMPTY, AND THAT IS A FINDING, NOT AN OVERSIGHT.
#
#  v2608's copy was built and published on 2026-09-02 and DECLARED here, and
#  the declaration was withdrawn the same day.  A four-way audit drove the
#  published copy in a browser and recorded every request it makes: the front
#  end is correctly its own, but on RUNNING A CASE the bundle fetches
#  /workers/solverWorker.js and /wasm/choupoSolve.js from the SITE ROOT.  The
#  frozen tree ships its own correct engines and they are never loaded.
#
#  Two independent causes, both verified by reading the published files:
#  the app bundle carries the literals "/wasm/choupoSolve.js" and
#  "/workers/solverWorker.js", which vite's --base never rewrites; and the
#  worker itself hardcodes `/wasm/${binary}.js`, so fixing the first is not
#  enough.  /v2607/app/workers/solverWorker.js carries the same lines -- this
#  has never worked for any release.
#
#  A frozen shell around a live engine is worse than no frozen app, because
#  the version badge reads its own frozen version.json and stays reassuringly
#  correct while the answers come from elsewhere.  Nothing is listed here
#  until the engine a copy loads is provably its own.
#
#  v2607 is also absent because it was WITHDRAWN (2026-09-02).

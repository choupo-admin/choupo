# GoatCounter site code for www.choupo.org -- configuration, never code.
#
# The single non-comment line of this file is the <code> in
# https://<code>.goatcounter.com.  bin/buildSite injects the cookieless
# counter script into the landing page and the app when it is set; when this
# file holds only comments, NOTHING is injected and the build log says so.
#
# It is public by nature (it appears in every page's source), so committing
# it here is fine.  Creating the account is the owner's act.

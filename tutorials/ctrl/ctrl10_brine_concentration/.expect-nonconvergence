# ctrl10 is REFUSED, on purpose, and the refusal is the lesson.
#
# The dynamic CSTR needs a liquidHeatCapacity per component: whichever
# energy route it takes, its temperature ODE divides by Sum n_i*Cp_i, and a
# DISSOLVED SALT has no liquid Cp -- NaCl's enthalpy rides the ionic route
# (aqueous inf-dilution reference + the Criss-Cobble ion Cp(T) the builder
# announces two lines earlier), and a "liquid Cp of NaCl" would be a
# fabricated number, not a missing one.
#
# So this case runs far enough to prove the PACKAGE reaches choupoCtrl -- the
# Pitzer banner, the ion Cp(T) load, the molality VLE world all appear -- and
# then stops, by name, at the one thing that cannot follow it:
#
#   ERROR: DynamicCSTR: component 'NaCl' has no liquidHeatCapacity entry
#
# HISTORY, so this file cannot mislead again -- it has now been wrong about
# its own blocker TWICE, and both corrections are worth keeping:
#
#   1. It blamed roadmap #1 (the stored-H reformulation).  That landed
#      (ctrl11) and did not unblock this case.
#   2. It then blamed the per-component Cp requirement alone.  The vessel
#      Cp is now route-aware (cpSurface_: declared liquid Cp, else the
#      T-derivative of the component's own stored-H leg) -- and this case
#      is STILL refused, correctly, because the probe-based route decision
#      asks the SURFACE what it can serve, and the surface answers in its
#      own words (quoted in the run's [energy] line): an electrolyte
#      salt's enthalpy is MIXTURE-level -- aqueousSaltEnthalpy(m, T),
#      molality-dependent -- and the per-species leg refuses to pretend
#      otherwise (forum #103).  No per-species Cp, numerical or declared,
#      can represent it.
#
# What actually unblocks this case is the MIXTURE-H state formulation for
# the dynamic vessel (store H(n, T) through H_liquid_formation and step
# it, partial-molar terms and all) -- DEV.md roadmap #2's remaining half.
# Delete this file when THAT lands: the case should then run, and its
# streamFaces should carry the two bases like every other stream in the
# project.
the electrolyte salt's enthalpy is mixture-level (aqueousSaltEnthalpy(m,T)); no per-species vessel Cp can represent it -- needs the mixture-H state formulation (DEV.md #2)

/*  Binary liquid-liquid teaching map — water / 1-butanol at 25 C.

    HONEST-FAILURE TUTORIAL (read README.md).  Water/1-butanol is the classic
    partial-miscibility binary, so the student expects the g_mix(x1) curve to
    show a NON-convex region with a common tangent (two binodal points), read
    straight off the diagram.

    But this scan uses PREDICTIVE UNIFAC (gamma from the components' groups, no
    fitted pair).  Predictive UNIFAC MISSES this LLE gap: it returns one convex
    g_mix well, so the liquid-liquid flash finds NO split and the CSV is a single
    smooth curve with ZERO binodal points.  The op announces this aloud
    ("predicts MISCIBILITY ... the g_mix curve stays convex").  That absence IS
    the lesson -- see README.md for why, and for the LLE-specific parameters that
    would open the gap.  Run-only smoke (the CSV is a teaching curve, not pinned
    KPIs). */
application   choupoProps;
description   "Binary g_mix(x) scan, water/1-butanol at 298.15 K -- honest-failure tutorial: predictive UNIFAC misses the LLE gap (convex curve, no split). See README.";
verbosity     2;

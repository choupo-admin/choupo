/*  ILLUSTRATIVE / SYNTHETIC DATA.  compA / compB / compC are NOT real
    species: they are artificial components (case-local constant/components/
    + inline NRTL pairs in thermoPackage) deliberately tuned to guarantee a
    ternary VLLE region, so this case exercises the propertyScanTernary
    classifier on a known-3-phase map.  Architecture (see thermoPackage):
    A and B mutually immiscible (LL gap on the A-B axis), C a volatile bridge
    soluble in both liquids.  At 350 K, 1 bar the equimolar feed gives
    vapour rich in C, L_alpha rich in A, L_beta rich in B.

    Expected map (14 intervals/edge, 78 nodes): VLLE 36, LL 29, VL 0,
    1-phase 13, 22 tie-lines, 0 failures.  The point to SEE: a healthy
    spread of 3-phase (VLLE) and LL nodes with zero solver failures —
    the classifier resolves the engineered gap node-by-node. */
operations
(
    {
        name      lle;
        type      propertyScanTernary;
        // HONEST provenance: there is NO primary literature source -- compA/B/C
        // are artificial components with inline NRTL pairs deliberately tuned to
        // guarantee a VLLE region (see the header).  We record that truthfully
        // rather than fabricate a citation.
        source    "synthetic / illustrative (artificial components, no real system)";
        mode      vlle;                 // full vapour + 2-liquid (3-phase) classification
        state     { T 350 K;  P 1 bar; }
        grid      { n 14; }             // intervals per edge (interior nodes)
        tieStride 3;                    // a tie-line every 3rd split node
        output    { file ternary.csv; }
    }
);

application   choupoSolve;
description   "User-defined componentSplitter (case-local code/): shortcut benzene/toluene split";

verbosity     3;

// The reports work generically over a user-defined unit op --- the
// reporting layer reads streams / KPIs, not the unit-op type.
reports
{
    streamTable    { }
    massBalance    { }
    spreadsheet    { }
}

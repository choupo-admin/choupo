/* SECTOR A (composite of one flash) */
children    ( fa );
boundary    { inlets ( feed );  outlets ( vapor  liquid ); }
connections
(
    { from feed;        to fa/feed;    }
    { from fa/vapor;    to vapor;      }
    { from fa/liquid;   to liquid;     }
);

/* top-level heater: consumes the DOMAIN inlet `feed` (the basename twin
   of A's internal edge A.feed -- two different streams, one basename). */
type        heater;
in          feed;
outputs     ( hot );
operation   { Q 2.0 kW; }

application   choupoSolve;
description   "Rigorous distillation by simultaneous MESH Newton (benzene/toluene; validates against column01)";

verbosity     3;

reports
{
    streamTable    { }
    massBalance    { }
    profiles       { }     // per-stage T, x_i, y_i down the column
    spreadsheet    { }     // coloured .ods (streams + balances + profile)
}

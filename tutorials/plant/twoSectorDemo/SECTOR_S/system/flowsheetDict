/*--- SECTOR_S: one custom distillation (compiled from code/). Build + run:
        bin/buildCode tutorials/plant/twoSectorDemo/SECTOR_S
        runCase       tutorials/plant/twoSectorDemo/SECTOR_S                       ---*/
// stream state lives on disk in 0/ (per-stream files); flowsheetDict is TOPOLOGY only
units
(
    { name C1;  type sharpSplitColumn;  in feed;  outputs ( top bottom );
      operation { topComponents ( compA compB compC );  recovery 0.95; } }
);

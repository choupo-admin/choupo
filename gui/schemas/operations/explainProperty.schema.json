{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://choupo.org/schemas/operations/explainProperty.schema.json",
  "title": "explainProperty operation",
  "description": "The derivation LEDGER of one mixture property at one state: every term with the record field or model it came from (the `s_298`/`dHf_298` datum, the Cp integral as a difference of two engine calls, the mixing and pressure lines, the EoS residual), re-added and checked against the engine's own assembled value at round-off. The op computes no thermodynamics of its own and REFUSES if its explanation and the engine disagree, if the property is not one it can explain, or if a `pureFluids {}` fundamental-equation override answers the state (that route's ledger is the release's own equation, not datum-plus-integral).",
  "type": "object",
  "properties": {
    "property": {
      "type": "string",
      "title": "Property to explain",
      "description": "One of S_real, S_ig, H_real, H_ig. An unknown name is refused, never dropped.",
      "enum": ["S_real", "S_ig", "H_real", "H_ig"]
    },
    "state": {
      "type": "object",
      "title": "Thermodynamic state",
      "description": "The (T, P, composition) whose ledger is published.",
      "properties": {
        "T": {
          "type": "number",
          "exclusiveMinimum": 0,
          "title": "Temperature",
          "unit": "K"
        },
        "P": {
          "type": "number",
          "exclusiveMinimum": 0,
          "title": "Pressure",
          "unit": "Pa"
        },
        "composition": {
          "type": "object",
          "title": "Mole composition (Sum xi = 1)"
        }
      },
      "required": [
        "T",
        "P",
        "composition"
      ]
    }
  },
  "required": [
    "property",
    "state"
  ],
  "additionalProperties": true
}

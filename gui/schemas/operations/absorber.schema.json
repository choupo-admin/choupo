{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://choupo.org/schemas/operations/absorber.schema.json",
  "title": "absorber operation",
  "description": "Counter-current multistage gas absorber solved by the Kremser group method. Per-solute recovery follows from the absorption factor A_i = L/(K_i V); a soluble gas with a Henry's-law entry uses K = H(T)/P.",
  "type": "object",
  "properties": {
    "stages": {
      "type": "number",
      "exclusiveMinimum": 0,
      "title": "Theoretical stages",
      "description": "Number of equilibrium stages.",
      "unit": "-"
    }
  },
  "required": ["stages"],
  "additionalProperties": false
}

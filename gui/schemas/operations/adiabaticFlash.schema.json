{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://choupo.org/schemas/operations/adiabaticFlash.schema.json",
  "title": "adiabaticFlash operation",
  "description": "Adiabatic flash (Q = 0). The feed flashes to the outlet pressure; outlet temperature, vapour fraction and phase follow from the energy balance.",
  "type": "object",
  "required": ["P"],
  "properties": {
    "P": {
      "type": "number",
      "exclusiveMinimum": 0,
      "title": "Outlet pressure",
      "description": "Pressure at which the flash is solved.",
      "unit": "Pa"
    }
  },
  "additionalProperties": false
}

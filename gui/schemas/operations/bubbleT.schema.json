{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://choupo.org/schemas/operations/bubbleT.schema.json",
  "title": "bubbleT operation",
  "description": "Bubble-point temperature at fixed pressure: the T at which the first vapour bubble forms from a saturated liquid. Solved by Newton-1D in T on Sum_i K_i(T) x_i = 1.",
  "type": "object",
  "properties": {
    "P": {
      "type": "number",
      "exclusiveMinimum": 0,
      "title": "Pressure",
      "description": "System pressure at which the bubble point is evaluated.",
      "unit": "Pa"
    }
  },
  "required": ["P"],
  "additionalProperties": false
}

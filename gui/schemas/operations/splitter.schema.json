{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://choupo.org/schemas/operations/splitter.schema.json",
  "title": "splitter operation",
  "description": "Stream splitter: divides one inlet into several outlets by the given split fractions (which sum to 1). All branches keep the inlet temperature, pressure and composition; this is a flow split, not a separation.",
  "type": "object",
  "properties": {
    "fractions": {
      "type": "array",
      "title": "Split fractions",
      "description": "Per-outlet split fraction, one per output stream, summing to 1. Ordering matches the `outputs` list.",
      "items": {
        "type": "number",
        "minimum": 0,
        "maximum": 1
      },
      "minItems": 2
    }
  },
  "required": ["fractions"],
  "additionalProperties": false
}

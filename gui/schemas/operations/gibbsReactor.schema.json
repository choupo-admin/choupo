{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://choupo.org/schemas/operations/gibbsReactor.schema.json",
  "title": "gibbsReactor operation",
  "description": "Gibbs reactor: equilibrium composition from minimising the total Gibbs free energy subject to atom-balance constraints. Three selectable `model` paths: elementPotential (gas), reactiveFlash (multi-condensable, NRTL) and directMin (multi-start Nelder-Mead).",
  "type": "object",
  "properties": {
    "T": {
      "type": "number",
      "exclusiveMinimum": 0,
      "title": "Operating temperature",
      "description": "Temperature at which the equilibrium is evaluated.",
      "unit": "K"
    },
    "P": {
      "type": "number",
      "exclusiveMinimum": 0,
      "title": "Operating pressure",
      "description": "Pressure at which the equilibrium is evaluated; enters the ideal-gas chemical potential mu = mu_pure(T) + RT ln(y_i P / 1 bar).",
      "unit": "Pa"
    },
    "elements": {
      "type": "array",
      "title": "Atomic elements",
      "description": "Chemical elements for the atom-balance constraints (one per element). Each species lists its atom counts in this order.",
      "items": {
        "type": "string"
      },
      "minItems": 1
    },
    "species": {
      "type": "array",
      "title": "Candidate species",
      "description": "Candidate species in the equilibrium mixture, each with its atom counts (one per element).",
      "items": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "title": "Component name (must be in the declared component set)"
          },
          "atoms": {
            "type": "array",
            "title": "Atom counts (one per element)",
            "items": {
              "type": "integer",
              "minimum": 0
            }
          }
        },
        "required": [
          "name",
          "atoms"
        ],
        "additionalProperties": false
      },
      "minItems": 1
    },
    "mode": {
      "type": "string",
      "enum": [
        "isothermal",
        "adiabatic"
      ],
      "title": "Mode",
      "description": "isothermal (default; `T` holds and the duty is the result) or adiabatic. In adiabatic mode `T` is the Newton's SEED, not the answer.",
      "unit": "-"
    },
    "Q": {
      "type": "number",
      "title": "Imposed duty",
      "description": "Heat added per kmol of feed, for a non-adiabatic non-isothermal case; defaults to 0.",
      "unit": "kJ/kmol"
    },
    "approachTemperature": {
      "type": "number",
      "minimum": 0,
      "title": "Equilibrium approach",
      "description": "Solve the equilibrium at (T − this), the standard way of admitting that a real reactor does not reach it. Defaults to 0 (full equilibrium) and is ANNOUNCED when non-zero.",
      "unit": "K"
    }
  },
  "required": [
    "T",
    "P",
    "elements",
    "species"
  ],
  "additionalProperties": false
}

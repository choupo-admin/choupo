{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://choupo.org/schemas/operations/stripper.schema.json",
  "title": "stripper operation",
  "description": "Counter-current multistage gas stripper solved by the Kremser method (the inverse of the absorber). A rich liquid is contacted with a lean gas to drive the solute out; per-solute removal follows from the stripping factor S = K·V/L.",
  "type": "object",
  "properties": {
    "stages": {
      "type": "number",
      "exclusiveMinimum": 0,
      "title": "Theoretical stages",
      "description": "Number of equilibrium stages.",
      "unit": "-"
    }
  },
  "required": ["stages"],
  "additionalProperties": false
}

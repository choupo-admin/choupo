{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://choupo.org/schemas/operations/pump.schema.json",
  "title": "pump operation",
  "description": "Incompressible-liquid pump. Given the shaft power and efficiency, the discharge pressure P_in + eta·W_shaft/(F·v_molar) and the small dissipation temperature rise follow from the energy balance.",
  "type": "object",
  "properties": {
    "W_shaft": {
      "type": "number",
      "exclusiveMinimum": 0,
      "title": "Shaft power",
      "description": "Mechanical power delivered to the liquid (positive). One of W_shaft / P_out / dP fixes the duty. It may also arrive over an ENERGY WIRE instead of being written here: a consumer declaring `energyInputs ( { from <unit>.<port>; kind <work|heat>; target <key>; } )` has the value written into its operation block before solve, and the unit reads it exactly as if the author had typed it.  That is why this key is not marked required — the schema describes what an AUTHOR writes, and a wired value is not authored.",
      "unit": "W"
    },
    "eta": {
      "type": "number",
      "exclusiveMinimum": 0,
      "maximum": 1,
      "title": "Pump efficiency",
      "description": "Hydraulic and mechanical efficiency, useful v·dP work / shaft work (0 < eta <= 1). The (1-eta) fraction heats the liquid.",
      "unit": "-"
    },
    "P_out": {
      "type": "number",
      "exclusiveMinimum": 0,
      "title": "Discharge pressure",
      "description": "Specify the outlet pressure instead of the power; the shaft work follows from v·dP/eta.",
      "unit": "Pa"
    },
    "dP": {
      "type": "number",
      "exclusiveMinimum": 0,
      "title": "Pressure rise",
      "description": "Specify the rise instead of the outlet pressure or the power.",
      "unit": "Pa"
    }
  },
  "required": [
    "eta"
  ],
  "additionalProperties": false
}

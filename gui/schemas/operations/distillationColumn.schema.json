{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://choupo.org/schemas/operations/distillationColumn.schema.json",
  "title": "distillationColumn operation",
  "description": "Multistage distillation column with constant molar overflow. Solved by the sequential bubble-point method (Wang-Henke, default) or the rigorous simultaneous MESH Newton, which is stable on azeotropes.",
  "type": "object",
  "required": ["nStages", "feedStage", "refluxRatio", "distillateRate"],
  "properties": {
    "nStages": {
      "type": "integer",
      "minimum": 2,
      "title": "Internal stages",
      "description": "Equilibrium stages between condenser and reboiler. Total stages = nStages + 2.",
      "default": 20
    },
    "feedStage": {
      "type": "integer",
      "minimum": 1,
      "title": "Feed stage",
      "description": "1-indexed stage where the feed enters (1 = top tray below the condenser).",
      "default": 10
    },
    "refluxRatio": {
      "type": "number",
      "exclusiveMinimum": 0,
      "title": "Reflux ratio",
      "description": "L/D, liquid returned to the column over distillate withdrawn.",
      "default": 2.0
    },
    "distillateRate": {
      "type": "number",
      "exclusiveMinimum": 0,
      "title": "Distillate flow",
      "description": "Top product molar flow rate.",
      "unit": "kmol/s",
      "default": 0.5
    },
    "P": {
      "type": "number",
      "exclusiveMinimum": 0,
      "title": "Operating pressure",
      "description": "Column pressure, constant across all stages. Falls back to the feed-stream pressure if omitted.",
      "unit": "Pa"
    },
    "feedQuality": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "title": "Feed quality q",
      "description": "Feed thermal condition: q = 1 saturated liquid, q = 0 saturated vapour.",
      "default": 1.0
    }
  },
  "additionalProperties": false
}

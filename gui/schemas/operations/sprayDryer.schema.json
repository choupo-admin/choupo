{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://choupo.org/schemas/operations/sprayDryer.schema.json",
  "title": "sprayDryer operation",
  "description": "Single-stage rotary-atomiser spray dryer producing powder and exhaust air. Computes the exhaust temperature, droplet and particle sizes, transfer coefficients and drying time from the energy balance, the Friedman rotary-disc correlation and Ranz-Marshall transfer. Requires a transport model in the declared thermophysical system.",
  "type": "object",
  "properties": {
    "wheelDiameter": {
      "type": "number",
      "exclusiveMinimum": 0,
      "title": "Atomiser disc diameter",
      "description": "Rotary-wheel atomiser only, and only in the flat form — inside an `atomiser { model rotaryWheel; }` block it lives there instead. A pressure-nozzle dryer declares neither.",
      "unit": "m"
    },
    "wheelSpeed": {
      "type": "number",
      "exclusiveMinimum": 0,
      "title": "Atomiser disc speed",
      "description": "Rotational speed of the atomiser wheel; faster spinning gives smaller droplets (d32 ~ N^-0.6, Friedman correlation).",
      "unit": "rpm",
      "default": 10000
    },
    "liquidViscosity": {
      "type": "number",
      "exclusiveMinimum": 0,
      "title": "Feed liquid viscosity",
      "description": "Dynamic viscosity of the feed liquid, used in the atomisation correlation.",
      "unit": "Pa.s",
      "default": 0.001
    },
    "surfaceTension": {
      "type": "number",
      "exclusiveMinimum": 0,
      "title": "Feed surface tension",
      "description": "Surface tension of the feed liquid, used in the atomisation correlation.",
      "unit": "N/m",
      "default": 0.072
    },
    "spreadParameter": {
      "type": "number",
      "exclusiveMinimum": 0,
      "title": "PSD spread (Rosin-Rammler n)",
      "description": "Width of the powder particle-size distribution; the droplet sizes follow a Rosin-Rammler Q(d) = 1 - exp(-(d/d')^n), larger n giving a narrower spread.",
      "unit": "-",
      "default": 2.2
    },
    "chamberDiameter": {
      "type": "number",
      "exclusiveMinimum": 0,
      "title": "Drying-chamber diameter",
      "description": "Diameter of the drying chamber; sets the gas superficial velocity v_gas = Q_gas/(pi D^2/4) and hence the residence time.",
      "unit": "m"
    },
    "chamberHeight": {
      "type": "number",
      "exclusiveMinimum": 0,
      "title": "Drying-chamber height",
      "description": "Height the particle falls through; residence time = chamberHeight / particle fall velocity, which sets the residual moisture.",
      "unit": "m"
    },
    "flow": {
      "type": "string",
      "enum": [
        "co",
        "counter"
      ],
      "title": "Gas/droplet flow direction",
      "description": "Co-current (v_particle = v_gas + v_terminal) or counter-current (v_particle = v_terminal - v_gas).",
      "default": "co"
    },
    "atomiser": {
      "type": "object",
      "title": "Atomiser sub-model",
      "description": "Selects HOW the feed is broken into droplets, which sets the droplet size and therefore the drying time. Absent, the rotary-wheel keys are read from the operation block itself (the historical flat form).",
      "properties": {
        "model": {
          "type": "string",
          "enum": [
            "rotaryWheel",
            "pressureNozzle"
          ],
          "title": "Atomiser model",
          "description": "rotaryWheel needs wheelDiameter and wheelSpeed; pressureNozzle takes its pressure drop from the feed stream against the chamber pressure."
        },
        "wheelDiameter": {
          "type": "number",
          "exclusiveMinimum": 0,
          "title": "Wheel diameter",
          "unit": "m"
        },
        "wheelSpeed": {
          "type": "number",
          "exclusiveMinimum": 0,
          "title": "Wheel speed",
          "unit": "rpm"
        }
      }
    }
  },
  "required": [],
  "additionalProperties": false
}

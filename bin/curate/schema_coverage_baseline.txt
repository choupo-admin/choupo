# Schema/corpus disagreements that PRE-DATE bin/curate/check_schema_coverage.py
# (written 2026-08-02).  Every line is a schema that would reject a case the
# engine runs, or that marks required a key a running case omits -- so the GUI
# property editor and docs/ai/schemas-reference.md are both wrong about it.
#
# This file is a RATCHET, not a whitelist.  A disagreement NOT listed here
# fails the gate immediately; and a line here that stops firing ALSO fails,
# so landing a fix forces its line out.  The file can only shrink.  When it is
# empty, delete it and the load_baseline() mechanism with it.
#
# Clearing one means reading the operation's source and a case that runs it,
# then correcting the SCHEMA -- never the case, which is the arbiter.
FALSE REQUIRED: compressor.schema.json marks W_shaft required, but tutorials/steady/rotating/compressorTurbine01_turbo_booster/system/flowsheetDict runs without it -- the engine defaults it, so the schema is over-strict
FALSE REQUIRED: distillationColumn.schema.json marks feedStage required, but tutorials/steady/distillation/column04_multifeed_sidedraw/system/flowsheetDict runs without it -- the engine defaults it, so the schema is over-strict
FALSE REQUIRED: distillationColumn.schema.json marks feedStage required, but tutorials/steady/distillation/column05_reactive_methylacetate/system/flowsheetDict runs without it -- the engine defaults it, so the schema is over-strict
FALSE REQUIRED: distillationColumn.schema.json marks feedStage required, but tutorials/steady/distillation/column08_radfrac_multidraw/system/flowsheetDict runs without it -- the engine defaults it, so the schema is over-strict
FALSE REQUIRED: evaporator.schema.json marks P required, but tutorials/plant/ChemicalPlantTutorial/CONCENTRATION/Evap1/system/flowsheetDict runs without it -- the engine defaults it, so the schema is over-strict
FALSE REQUIRED: evaporator.schema.json marks P required, but tutorials/plant/ChemicalPlantTutorial/CONCENTRATION/Evap2/system/flowsheetDict runs without it -- the engine defaults it, so the schema is over-strict
FALSE REQUIRED: evaporator.schema.json marks P required, but tutorials/steady/evaporation/evaporator01_brine/system/flowsheetDict runs without it -- the engine defaults it, so the schema is over-strict
FALSE REQUIRED: evaporator.schema.json marks P required, but tutorials/steady/evaporation/evaporator02_triple_effect_sugar/system/flowsheetDict runs without it -- the engine defaults it, so the schema is over-strict
FALSE REQUIRED: evaporator.schema.json marks P required, but tutorials/steady/evaporation/evaporator03_co_current/system/flowsheetDict runs without it -- the engine defaults it, so the schema is over-strict
FALSE REQUIRED: evaporator.schema.json marks P required, but tutorials/steady/evaporation/evaporator04_orange_juice_simple/system/flowsheetDict runs without it -- the engine defaults it, so the schema is over-strict
FALSE REQUIRED: evaporator.schema.json marks P required, but tutorials/steady/evaporation/evaporator05_counter_current/system/flowsheetDict runs without it -- the engine defaults it, so the schema is over-strict
FALSE REQUIRED: evaporator.schema.json marks P required, but tutorials/steady/evaporation/evaporator06_nacl_pitzer/system/flowsheetDict runs without it -- the engine defaults it, so the schema is over-strict
FALSE REQUIRED: evaporator.schema.json marks P required, but tutorials/steady/evaporation/evaporator07_nacl_enrtl/system/flowsheetDict runs without it -- the engine defaults it, so the schema is over-strict
FALSE REQUIRED: evaporator.schema.json marks P required, but tutorials/steady/evaporation/evaporator08_naoh_dilution_heat/system/flowsheetDict runs without it -- the engine defaults it, so the schema is over-strict
FALSE REQUIRED: evaporator.schema.json marks P required, but tutorials/steady/evaporation/evaporator09_nacl_sucrose_brine/system/flowsheetDict runs without it -- the engine defaults it, so the schema is over-strict
FALSE REQUIRED: evaporator.schema.json marks P required, but tutorials/steady/optimisation/designSpec01_triple_equal_areas/system/flowsheetDict runs without it -- the engine defaults it, so the schema is over-strict
FALSE REQUIRED: evaporator.schema.json marks P required, but tutorials/steady/thermoTest/model1_lumped_evaporator/system/flowsheetDict runs without it -- the engine defaults it, so the schema is over-strict
FALSE REQUIRED: evaporator.schema.json marks P required, but tutorials/steady/thermoTest/model2_pitzer_evaporator/system/flowsheetDict runs without it -- the engine defaults it, so the schema is over-strict
FALSE REQUIRED: fitParameters.schema.json marks parameters required, but tutorials/props/molecular/eval_nrtl_catalogue/system/propsDict runs without it -- the engine defaults it, so the schema is over-strict
FALSE REQUIRED: fitParameters.schema.json marks parameters required, but tutorials/props/molecular/eval_nrtl_pinned/system/propsDict runs without it -- the engine defaults it, so the schema is over-strict
FALSE REQUIRED: heatExchanger.schema.json marks U, area required, but tutorials/steady/heat/heatExchanger02_geometry_U/system/flowsheetDict runs without it -- the engine defaults it, so the schema is over-strict
FALSE REQUIRED: heatExchanger.schema.json marks U, area required, but tutorials/steady/heat/hxWorkflow1_design_from_duty/system/flowsheetDict runs without it -- the engine defaults it, so the schema is over-strict
FALSE REQUIRED: heatExchanger.schema.json marks U, area required, but tutorials/steady/heat/hxWorkflow2_rate_designed/system/flowsheetDict runs without it -- the engine defaults it, so the schema is over-strict
FALSE REQUIRED: heater.schema.json marks Q required, but tutorials/steady/heat/heatlink01_condenser_to_heater/system/flowsheetDict runs without it -- the engine defaults it, so the schema is over-strict
FALSE REQUIRED: heater.schema.json marks Q required, but tutorials/steady/heat/heatlink02_condenser_feed_preheat/system/flowsheetDict runs without it -- the engine defaults it, so the schema is over-strict
FALSE REQUIRED: pump.schema.json marks W_shaft required, but tutorials/steady/power/rankine02_water/system/flowsheetDict runs without it -- the engine defaults it, so the schema is over-strict
FALSE REQUIRED: pump.schema.json marks W_shaft required, but tutorials/steady/rotating/pump02_pressure_spec/system/flowsheetDict runs without it -- the engine defaults it, so the schema is over-strict
FALSE REQUIRED: solidDryer.schema.json marks airTemperature required, but tutorials/plant/ChemicalPlantTutorial/DRYING/BD/system/flowsheetDict runs without it -- the engine defaults it, so the schema is over-strict
FALSE REQUIRED: solidDryer.schema.json marks airTemperature required, but tutorials/steady/drying/solidDryer01_sugar/system/flowsheetDict runs without it -- the engine defaults it, so the schema is over-strict
FALSE REQUIRED: sprayDryer.schema.json marks wheelDiameter required, but tutorials/steady/drying/sprayDryer03_pressure_nozzle/system/flowsheetDict runs without it -- the engine defaults it, so the schema is over-strict
UNKNOWN KEY: crystalliser in tutorials/steady/crystallisation/crystalliser03_pbe_size_independent/system/flowsheetDict uses Lmax, bins -- absent from crystalliser.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: crystalliser in tutorials/steady/crystallisation/crystalliser04_size_dependent_growth/system/flowsheetDict uses Lmax, bins -- absent from crystalliser.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: crystalliser in tutorials/steady/crystallisation/crystalliser06_nacl_antisolvent/system/flowsheetDict uses cakeMoisture, solidsRecovery -- absent from crystalliser.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: crystalliser in tutorials/steady/crystallisation/crystalliser07_nacl_antisolvent_cooled/system/flowsheetDict uses cakeMoisture, solidsRecovery -- absent from crystalliser.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: crystalliser in tutorials/steady/crystallisation/crystalliser09_KHT_KCl_series/system/flowsheetDict uses cakeMoisture, dHcryst, solute -- absent from crystalliser.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: crystalliser in tutorials/steady/crystallisation/crystalliser09_KHT_KCl_series/system/flowsheetDict uses cakeMoisture, solute -- absent from crystalliser.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: crystalliser in tutorials/steady/crystallisation/crystalliser10_naoh_antisolvent/system/flowsheetDict uses cakeMoisture, dHcryst, solute -- absent from crystalliser.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: crystalliser in tutorials/steady/crystallisation/crystalliser11_kcl_antisolvent_ethanol/system/flowsheetDict uses cakeMoisture, solidsRecovery -- absent from crystalliser.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: crystalliser in tutorials/steady/crystallisation/crystalliser12_nacl_pitzer_antisolvent/system/flowsheetDict uses cakeMoisture, solidsRecovery -- absent from crystalliser.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: crystalliser in tutorials/steady/thermoTest/model3_enrtl_antisolvent_crystalliser/system/flowsheetDict uses cakeMoisture, solidsRecovery -- absent from crystalliser.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: cstr in tutorials/plant/ChemicalPlantTutorial/FERMENTATION/Fermentor/system/flowsheetDict uses T -- absent from cstr.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: cstr in tutorials/steady/reactors/cstr04_adiabatic/system/flowsheetDict uses T_guess, thermalMode -- absent from cstr.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: cstr in tutorials/steady/reactors/cstr05_multiplicity/system/flowsheetDict uses T_guess, thermalMode -- absent from cstr.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: cstr in tutorials/steady/reactors/cstr06_jacketed/system/flowsheetDict uses T_coolant, T_guess, UA, thermalMode -- absent from cstr.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: cstr in tutorials/steady/reactors/cstr07_lhhw_methylAcetate/system/flowsheetDict uses catalystLoading -- absent from cstr.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: distillationColumn in tutorials/steady/distillation/column04_multifeed_sidedraw/system/flowsheetDict uses feeds, sideDraws -- absent from distillationColumn.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: distillationColumn in tutorials/steady/distillation/column05_reactive_methylacetate/system/flowsheetDict uses feeds -- absent from distillationColumn.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: distillationColumn in tutorials/steady/distillation/column08_radfrac_multidraw/system/flowsheetDict uses feeds, sideDraws -- absent from distillationColumn.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: distillationColumn in tutorials/steady/distillation/column09_tray_hydraulics/system/flowsheetDict uses hydraulics -- absent from distillationColumn.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: distillationColumn in tutorials/steady/distillation/column10_flooding/system/flowsheetDict uses hydraulics -- absent from distillationColumn.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: distillationColumn in tutorials/steady/distillation/column11_murphree/system/flowsheetDict uses hydraulics, murphreeEfficiency -- absent from distillationColumn.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: fitParameters in tutorials/props/adsorption/isotherm02_fit_jaschik/system/propsDict uses output -- absent from fitParameters.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: fitParameters in tutorials/props/adsorption/isotherm03_fit_refusals/system/propsDict uses output -- absent from fitParameters.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: fitParameters in tutorials/props/molecular/eval_nrtl_catalogue/system/propsDict uses mode, output -- absent from fitParameters.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: fitParameters in tutorials/props/molecular/eval_nrtl_pinned/system/propsDict uses mode, output -- absent from fitParameters.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: fitParameters in tutorials/steady/optimisation/fitNRTL01_ethanol_water/system/propsDict uses output -- absent from fitParameters.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: gibbsReactor in tutorials/plant/lithiumBrinePlant/sectors/HOTAIR/unitOperations/burner/system/flowsheetDict uses mode -- absent from gibbsReactor.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: gibbsReactor in tutorials/steady/flowsheets/ammonia02_full_plant/system/flowsheetDict uses mode -- absent from gibbsReactor.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: gibbsReactor in tutorials/steady/gibbs/gibbs05_adiabatic_flame/system/flowsheetDict uses mode -- absent from gibbsReactor.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: gibbsReactor in tutorials/steady/gibbs/gibbs06_h2_flame_radicals/system/flowsheetDict uses mode -- absent from gibbsReactor.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: heatExchanger in tutorials/steady/heat/heatExchanger02_geometry_U/system/flowsheetDict uses geometry, shellSide, tubeSide, tubeStream -- absent from heatExchanger.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: heatExchanger in tutorials/steady/heat/hxWorkflow1_design_from_duty/system/flowsheetDict uses design, geometry, shellSide, tubeSide, tubeStream -- absent from heatExchanger.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: heatExchanger in tutorials/steady/heat/hxWorkflow2_rate_designed/system/flowsheetDict uses geometry, shellSide, tubeSide, tubeStream -- absent from heatExchanger.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: isothermalFlash in tutorials/plant/hda/system/flowsheetDict uses phaseSet -- absent from isothermalFlash.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: isothermalFlash in tutorials/plant/lithiumBrinePlant/sectors/EXTRACTION/unitOperations/settler01/system/flowsheetDict uses alphaRich, betaRich, phaseSet -- absent from isothermalFlash.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: isothermalFlash in tutorials/plant/polycaprolactonePlant/system/flowsheetDict uses phaseSet -- absent from isothermalFlash.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: isothermalFlash in tutorials/steady/absorption/extraction01_waterButanol/system/flowsheetDict uses alphaRich, betaRich, phaseSet -- absent from isothermalFlash.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: isothermalFlash in tutorials/steady/flash/vlle01_waterButanol/system/flowsheetDict uses alphaRich, betaRich, phaseSet -- absent from isothermalFlash.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: isothermalFlash in tutorials/steady/flash/vlle03_audit_artificial/system/flowsheetDict uses alphaRich, betaRich, phaseSet -- absent from isothermalFlash.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: mixer in tutorials/plant/hda/system/flowsheetDict uses T -- absent from mixer.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: mixer in tutorials/plant/polycaprolactonePlant/system/flowsheetDict uses T -- absent from mixer.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: pfr in tutorials/steady/reactors/pfr04_adiabatic/system/flowsheetDict uses thermalMode -- absent from pfr.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: propertyPoint in tutorials/props/molecular/pcsaft01_pure_nhexane/system/propsDict uses properties -- absent from propertyPoint.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: propertyPoint in tutorials/props/molecular/pcsaft02_states_crosscheck/system/propsDict uses properties -- absent from propertyPoint.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: propertyPoint in tutorials/props/steam/steam02_water_transport/system/propsDict uses properties -- absent from propertyPoint.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: propertyScan1D in tutorials/plant/esterification2sector/sectors/SEPARATION/system/propsDict uses NRTL, rationale -- absent from propertyScan1D.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: propertyScan1D in tutorials/plant/esterification2sector/sectors/SEPARATION/system/propsDict uses compare, rationale -- absent from propertyScan1D.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: propertyScan1D in tutorials/plant/esterification2sector/sectors/SEPARATION/system/propsDict uses rationale -- absent from propertyScan1D.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: pump in tutorials/steady/power/rankine02_water/system/flowsheetDict uses P_out -- absent from pump.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: pump in tutorials/steady/rotating/pump02_pressure_spec/system/flowsheetDict uses P_out -- absent from pump.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: sprayDryer in tutorials/steady/drying/sprayDryer03_pressure_nozzle/system/flowsheetDict uses atomiser -- absent from sprayDryer.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
UNKNOWN KEY: steamTables in tutorials/props/steam/steam01_if97_verification/system/propsDict uses output -- absent from steamTables.schema.json, whose additionalProperties is false, so the schema would reject a case the engine runs
